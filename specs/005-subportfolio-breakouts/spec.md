# Feature Specification: One-Click Portfolio Breakouts by LOB & Geography (Iteration 4)

**Branch**: `005-subportfolio-breakouts` | **Created**: 2026-07-29

## Status

**Phase:** Planning
**Blocking:** Nothing

## Outcome

From the EDM detail page, an analyst can split a source portfolio into one sub-portfolio per line of business, or one per state or state-equivalent, in a single confirmed action — no Risk Modeler UI, no RiskLink, no free-text entry. Each generated portfolio appears in the EDM's portfolio list — its base portfolio and breakout criteria shown in its expanded row — and its exposure figures fill in automatically.

## In scope

- Break out by line of business: one sub-portfolio per distinct LOB present in the source portfolio
- Break out by geography: one sub-portfolio per distinct state or state-equivalent (first-level administrative division) present in the source portfolio — global portfolios included
- Break out by country (follow-on, 2026-08-10 — the extension research.md P-03 anticipated): one sub-portfolio per distinct country present in the source portfolio; values are the `Address.CountryCode`/`CountryRMSCode` codes, no name column exists in the EDM
- **Custom grouping** (follow-on, Aug 6 CIC demo — D12/D13/D17): named groups of selected values combined across dimensions (state, LOB, country, peril) into one sub-portfolio each, built in a cart and confirmed once (FR-018–021)
- Peril as a grouping-only dimension (P-19) — values are `loccvg.PERIL` codes
- Preview and confirm before anything is created, with the account-overlap and blank-value disclosures
- Lineage (source portfolio + dimension + value) stored, and shown in each generated row's expanded panel (P-27); deterministic collision-safe naming
- Automatic exposure-detail refresh for generated portfolios; audit of each breakout run
- PRD documentation pass recording the 2026-07-29 product direction (O6-1/O6-2 register, §10A.5, §21)

## Out of scope

- Complement split ("X vs. not-X"), "do the opposite"
- Standalone one-per-peril quick breakout — Deferred pending the D15 team validation (P-19)
- CRESTA or ZIP geography grain — state/state-equivalent and country only
- Portfolio edit, delete, or merge; rollback of created portfolios
- A new current-split view — spec 004's per-portfolio table already serves that purpose and hosts these actions

## Non-negotiable behavior

1. Nothing is created until the analyst confirms the previewed sub-portfolio list — every value, generated name, and the count (constitution Art. 5). What the analyst confirmed is persisted and executed; the worker never recomputes it (AGENTS.md rule 8, approved plans are immutable — a project rule, not a constitution article).
2. The preview disclosures state that Risk Modeler assigns whole accounts, so an account matching several values lands in full in each matching sub-portfolio, and that blank-value exposure lands in none — with the overlap quantified for the portfolio being broken out, not as a fixed warning.
3. Offered values come only from the stored exposure summary — no free text, no Risk Modeler or DataBridge read on the request path.
4. A confirm is refused before any job row is created when the summary is stale (Risk Modeler `stampDate` mismatch) or when the summary was rewritten by a Sync since the preview rendered.
5. No rollback: the app never deletes a created portfolio; recovery from partial failure is idempotent re-run.

## Open product decisions

| ID | Decision | Status | Where |
|---|---|---|---|
| P-01 | Scope is the two one-click breakouts only; filtered builder, complement split, "do the opposite" are follow-ons | Approved | [research.md#session-2026-07-29](research.md#session-2026-07-29) |
| P-02 | Geography breakout ships now; whole-account bucketing (overlapping sub-portfolios) accepted and disclosed, no location-level toggle | Approved | [research.md#session-2026-07-29](research.md#session-2026-07-29) |
| P-03 | Geography grain is state or state-equivalent (first-level administrative division) only this iteration | Approved | [research.md#session-2026-07-29](research.md#session-2026-07-29), [#session-2026-07-31](research.md#session-2026-07-31) |
| P-04 | Values come from the stored spec-004 summary; a portfolio without a backfilled summary cannot be broken out (Sync is the recovery) | Approved | [research.md#session-2026-07-29](research.md#session-2026-07-29) |
| P-05 | A dimension with fewer than two distinct values is offered disabled ("only one value present") | Approved | [research.md#session-2026-07-29](research.md#session-2026-07-29) |
| P-06 | A zero-match sub-portfolio fails and creates nothing; staleness is refused at confirm via `stampDate` equality | Approved | [research.md#session-2026-07-30](research.md#session-2026-07-30) |
| P-07 | No rollback; partial failure is recovered by idempotent re-run (portfolio deletion is out of MVP) | Approved | [research.md#carried-from-the-design-record](research.md#carried-from-the-design-record) |
| P-08 | Audit uses existing conventions (business-event logs, row provenance, job input/output); no audit-log table | Approved | [research.md#carried-from-the-design-record](research.md#carried-from-the-design-record), [#session-2026-08-04](research.md#session-2026-08-04) |
| P-09 | The PRD documentation pass happens within this iteration, same branch | Approved | [research.md#R9](research.md) |
| P-10 | Generated names read `{source portfolio name} - {display label, else value}`, with the lowest free ` (2)`/` (3)` suffix on collision | Approved | [research.md#session-2026-07-31](research.md#session-2026-07-31), [#session-2026-08-05](research.md#session-2026-08-05) |
| P-11 | Names cap at 40 characters: the **source name** absorbs the truncation and the name token is kept whole, with the full source name in the Risk Modeler description. `portfolio_number` is composed separately (≤ 20 chars, from the source portfolio's RM id + dimension + value) and **adoption resolves on the number, not the name** | Approved | [research.md#R4](research.md), [research.md#session-2026-08-03](research.md#session-2026-08-03) |
| P-12 | State values are `Admin1Code` as the filter value, the stored `breakout_value`, and the number token. Names and every display render `Admin1Name` where the stored summary carries one, falling back to the code (revised 2026-08-05 — never `cbhu - 200`). `Admin1Name` is a separate exposure attribute carried as a nullable display label, absent until the EDM is geocoded, never synthesized from the code and never a filter input | Approved | [research.md#R6](research.md), [research.md#session-2026-08-03](research.md#session-2026-08-03), [#session-2026-08-05](research.md#session-2026-08-05) |
| P-13 | The preview quantifies the overlap for the portfolio being broken out — the accounts carrying more than one value and the accounts carrying none, both counted per account at Sync — not a fixed warning and not arithmetic over the per-value counts | Approved | [research.md#R11](research.md), [research.md#session-2026-08-03](research.md#session-2026-08-03), [#session-2026-08-05](research.md#session-2026-08-05) |
| P-14 | The confirm POST composes the plan it persists; the previewed **name** is indicative and the generated `portfolio_number` is the identity. From the moment the plan is persisted it is authoritative | Approved | [research.md#session-2026-08-04](research.md#session-2026-08-04) |
| P-15 | No cap on the number of sub-portfolios. Above a named threshold (25) the preview states that the run takes several minutes — no mention of the job queue; the analyst still confirms once | Approved | [research.md#session-2026-08-04](research.md#session-2026-08-04) |
| P-16 | A Sync in flight disables the breakout action, and a confirm whose stored summary was rewritten since the preview rendered is refused — the summary is what the preview and the persisted plan both read | Approved | [research.md#session-2026-08-04](research.md#session-2026-08-04) |
| P-17 | Custom grouping ships (D12/D13/D17): analysts combine selected values into named groups in a persistent cart; **each group in the cart is one `rwb_job`**, and quick mode survives alongside | Approved | [research.md#session-2026-08-09](research.md#session-2026-08-09) |
| P-18 | Groups may overlap — **warn, never block** (CIC resolves overlap loss-side); the cart's overlap note is a may-overlap heuristic | Approved | [research.md#session-2026-08-09](research.md#session-2026-08-09) |
| P-19 | Peril ships as a **grouping-only** dimension (D14): a pill in the custom-grouping pane, never a quick-mode breakout — no `portfolio_number` letter, no `run_breakout_peril` job type. Standalone peril quick mode **Deferred** pending the D15 team validation | Approved | [research.md#R14](research.md), [probe-findings.md#W-21](probe-findings.md) |
| P-20 | Combined dimensions: **AND across dimensions, OR within** — a group holds selected values per dimension (`state IN (FL, GA) AND peril IN (2)`); quick mode stays single-dimension | Approved | [research.md#session-2026-08-09](research.md#session-2026-08-09) |
| P-22 | Group identity is the **member set** (canonical hash), not the label: the same members re-confirmed **adopt the existing group row — no duplicate row, no duplicate portfolio**; the row takes the name as typed each confirm (rev. 2026-08-10 — the earlier no-rename rule is dropped; the cart always shows what the analyst entered, P-24) | Approved | [research.md#session-2026-08-09](research.md#session-2026-08-09) |
| P-23 | The cart previews **"up to N accounts"** per group — an upper bound from stored per-value counts (min over dimensions of the summed selections); exact counts arrive in completion outcomes | Approved | [research.md#session-2026-08-09](research.md#session-2026-08-09) |
| P-21 | Each preview disclosure is one short quantified line — the overlap warning and the blank-value line — with qualitative one-liners when the summary carries no measured coverage; the exposure-inflation and geography-specific prose is deleted (D11) | Approved | [research.md#session-2026-08-09](research.md#session-2026-08-09) |
| P-24 | A custom group's sub-portfolio name is **exactly the label the analyst types** — no source-name prefix, no pre-population, no collision suffix; the 40-character Risk Modeler limit applies to the label directly | Approved | [research.md#session-2026-08-10](research.md#session-2026-08-10) |
| P-25 | **Duplicate portfolio names are blocked, never suffixed**: checked as the analyst types and again at Add — against the workbench rows, the cart's earlier rows, and Risk Modeler within the EDM (the EDM import name-check pattern, fail-open). A deliberate divergence from Risk Modeler's own permissiveness | Approved | [research.md#session-2026-08-10](research.md#session-2026-08-10) |
| P-26 | A group's `portfolio_number` is its **name truncated to 20 characters** — the `P{rm id}-G-{key hash}` scheme is dropped; adoption still resolves on the number in the duplicate-name heal, and `group_key` stays the group identity (P-22). Quick-mode numbers are unchanged | Approved | [research.md#session-2026-08-10](research.md#session-2026-08-10) |
| P-27 | Lineage displays in the **expanded row only**: expanding a generated sub-portfolio shows its base portfolio and its breakout criteria in the Risk Modeler description format (`Line of business IN (Homeowners)`; a custom group's AND-joined filter set). The collapsed table row carries no lineage badge; base and broker-arrived rows render none of it | Approved | [research.md#session-2026-08-11](research.md#session-2026-08-11) |
| P-28 | **Caribbean geography maps CB → country, island → state**: an address whose `CountryRMSCode` is `CB` takes `CB` as its country value and its own ISO3A `CountryCode` as its state value, in place of `Admin1Code`. One value breaks out the whole region; `PRI`/`VIR` break out single islands. Caribbean state values carry no display label | Approved | [research.md#session-2026-08-11](research.md#session-2026-08-11) |
| O-01 | `loccvg.PERIL` vocabulary in the sandbox EDMs — numeric RMS codes (smallint), no blanks, one distinct peril per sandbox portfolio; sub-peril detail rides parent perils | Closed | [probe-findings.md#W-21](probe-findings.md), [research.md#R14](research.md) |
| O-02 | Peril display is the numeric code only (no in-EDM label source; P-12 forbids synthesizing one) — acceptable to analysts? Decided at the grouping-modal preview approval | Open | [research.md#R14](research.md) |

---

## User Stories

### 1. One-click breakout by line of business (P1)

An analyst has an imported EDM whose portfolio mixes lines of business the treaty structure needs separated. On the EDM detail page's portfolio table, they open the breakout action on the source portfolio and choose **By line of business**. The workbench shows the distinct LOB values actually present in that portfolio (from the stored exposure summary — no typing), the sub-portfolio name each value will get, and the standing account-bucketing disclosure (mixed-LOB accounts appear in full in every sub-portfolio they match). The analyst confirms; the workbench creates one sub-portfolio per LOB in Risk Modeler and each new portfolio appears in the EDM's portfolio list, linked back to its source. Exposure figures fill in via the same backfill that populated the rest of the page. This is the PRD's explicitly unblocked, ship-first case (§21 Iteration 4) and the simplest (LOB is policy-level; one policy = one LOB); it exercises account selection, sub-portfolio creation and population, value enumeration, naming, lineage, the prerequisite gate, and partial-failure handling end to end.

**Acceptance**

1. **Given** an imported, ready EDM whose source portfolio has a backfilled exposure summary listing N ≥ 2 distinct LOBs, **When** the analyst opens the breakout action and chooses By line of business, **Then** the preview lists exactly those N values with the generated sub-portfolio name for each, and nothing is created yet.
2. **Given** the preview, **When** the analyst confirms, **Then** N sub-portfolios are created in Risk Modeler — each containing exactly the source portfolio's accounts matching its LOB — and N new `irp_portfolio` rows appear in the EDM's portfolio list, each carrying its Risk Modeler portfolio id and lineage (source portfolio, dimension = LOB, value).
3. **Given** a completed breakout, **When** the EDM detail page is viewed after the follow-up detail refresh, **Then** the new sub-portfolios show their own exposure figures (the refresh auto-fires; the analyst does not click Sync).
4. **Given** the preview's disclosure, **When** the analyst reads it, **Then** it states how many of the source portfolio's accounts land in more than one sub-portfolio (or that none do), that such accounts appear in full in every sub-portfolio they match, and that the generated sub-portfolios together cover the entire source portfolio.
5. **Given** a source portfolio whose stored summary shows fewer than two distinct LOBs, **When** the analyst opens the breakout action, **Then** the LOB dimension is offered disabled with the reason ("only one line of business present").
6. **Given** a source portfolio with no backfilled exposure summary, **When** the analyst looks at the breakout action, **Then** it is disabled with a reason pointing at the EDM's Sync action rather than failing later.
7. **Given** an EDM that already contains a portfolio named like a would-be sub-portfolio, **When** the breakout runs, **Then** the generated name is made collision-safe (lowest free ` (2)`, ` (3)`… suffix) rather than failing Risk Modeler's duplicate-name guard.
8. **Given** an EDM whose detail refresh is running, **When** the analyst looks at the breakout action, **Then** it is disabled with the reason that the exposure summary is being rewritten, and it becomes available again when the refresh finishes without the analyst reloading the page.
9. **Given** a preview the analyst is still reading, **When** a detail refresh completes and rewrites the summary before they confirm, **Then** the confirm is refused, the preview is re-rendered from the new summary for them to confirm again, and no breakout job row is created — including when the Risk Modeler `stampDate` is unchanged.

### 2. One-click breakout by geography (state or state-equivalent) (P2)

An analyst needs a cedant's book split by state — e.g. a treaty carries a different retention for one state. From the same breakout action they choose **By geography (state)**; the workbench shows the states actually present in the portfolio (from the stored summary — the state code, with the state name beside it where the EDM has been geocoded), the name each sub-portfolio will get, and the account-bucketing disclosure — which matters most here, because a commercial account with locations in several states lands **in full** in each of those state sub-portfolios. For non-US exposure the same attribute applies: the values are the exposure's first-level administrative divisions (Canadian provinces, German Länder, and equivalents — the same Moody's field), so a global portfolio breaks out the same way with no separate mode. On confirm, one sub-portfolio per value is created and listed with lineage, exactly as in the LOB case. Geography is the second of the two directed breakouts and the one the PRD had gated on O6-1/O6-2 — now unblocked by product direction (P-02) with the account-bucketing semantics accepted and disclosed. It shares all of user story 1's behavior; what it adds is the state attribute and the sharper disclosure.

**Acceptance**

1. **Given** a source portfolio whose stored summary lists M ≥ 2 distinct states or state-equivalents, **When** the analyst chooses By geography (state), **Then** the preview lists exactly those M values with generated names, nothing created until confirm.
2. **Given** the preview, **When** the analyst confirms, **Then** M sub-portfolios are created — each containing exactly the source portfolio's accounts matching its state or state-equivalent — persisted and listed with lineage (dimension = state, value), same as user story 1.
3. **Given** a multi-state commercial account in the source, **When** sub-portfolios are created, **Then** that account's full exposure appears in every state sub-portfolio it touches — and the preview's disclosure said so before the analyst confirmed.
4. **Given** a global portfolio mixing US and non-US exposure, **When** the analyst chooses By geography (state), **Then** the preview lists every first-level administrative division present — US states and non-US equivalents side by side — with no US-only restriction.
5. **Given** the granularity cap (§10A.2), **When** the analyst looks for finer-than-state options (CRESTA, ZIP), **Then** none are offered — state/state-equivalent and country are the only geography grains.
6. **Given** a source portfolio whose EDM has not been geocoded, **When** the analyst chooses By geography (state), **Then** the preview still lists every state present (codes arrive with the exposure import, names do not) and the breakout creates correctly populated sub-portfolios — geocoding is not a prerequisite.
7. **Given** a global portfolio carrying more than 25 first-level administrative divisions, **When** the analyst opens the preview, **Then** every value is listed with nothing truncated and nothing refused, and the preview states that the run takes several minutes — one confirm, no extra gate.

### 3. Generated sub-portfolios are identifiable: lineage, naming, and audit (P3)

An analyst (or a colleague opening the EDM later) can tell which portfolios were generated by a breakout, from which source, by which criteria — by expanding the generated portfolio's row (P-27, 2026-08-11: the collapsed table row carries no lineage) and, for quick-mode breakouts, from each generated name. The breakout action is recorded in the audit trail (who ran it, when, source portfolio, dimension, per-portfolio outcomes). Lineage is what keeps a 25-portfolio EDM legible after two breakouts and is the hook later iterations (batch analysis over generated portfolios, grouping results) build on.

**Acceptance**

1. **Given** an EDM where a breakout has run, **When** the analyst expands a generated portfolio's row, **Then** the expanded panel shows the base portfolio it was broken out from and its breakout criteria in the Risk Modeler description format — `Line of business IN (Homeowners)` for a quick breakout, the full AND-joined filter set (`lob IN (a, b) AND state IN (x, y)`) for a custom group — while the collapsed row, base portfolios, and broker-arrived portfolios render no lineage (P-27).
2. **Given** a generated portfolio, **When** its name is read, **Then** it is `{source portfolio name} - {display label, else value}` with the source name truncated to fit 40 characters and the name token whole (e.g. `TY2607 Cedant Book - Homeowners`, `cbhu - Puerto Rico` — never `cbhu - 200`), collision-suffixed ` (2)`, ` (3)`… when needed, so it is recognizable in Risk Modeler too, where the lineage columns don't exist — and its Risk Modeler description names the source portfolio, dimension, and value (with its label, when one exists) in full.
3. **Given** a completed (or partially failed) breakout, **When** the audit trail is consulted, **Then** it records the actor, timestamp, source portfolio, dimension, and per-portfolio outcomes.

### 4. Custom grouping: named member sets across dimensions (follow-on)

An analyst needs "these three LOBs as one portfolio" or "Florida and Georgia hurricane only" — combinations the one-per-value breakouts cannot express (Aug 6 CIC demo, D12/D13/D14/D17). In the same breakout modal they switch to **Custom groups**: dimension pills (line of business, state, peril) toggle checkbox lists of the stored summary's values; ticking values across dimensions and naming the selection adds a **group** to a cart (values OR within a dimension, AND across dimensions). The typed name is the sub-portfolio's name, exactly as entered (P-24), and a name already present in the EDM is refused as they type (P-25). Each cart row shows the name, its member filters, and "up to N accounts"; groups sharing a selected value carry a may-overlap note (warn, never block — P-18). One confirm creates one sub-portfolio per group, each as its own job, with progress and the completion banner aggregated per cart.

**Acceptance**

1. **Given** an eligible source portfolio, **When** the analyst opens Custom groups, **Then** every dimension whose stored summary carries ≥ 2 distinct values renders as a pill with checkbox values (peril included), switching pills loses no ticked state, and nothing is created before the cart confirm.
2. **Given** a cart of N groups, **When** the analyst confirms, **Then** N sub-portfolios are created — each holding exactly the source accounts matching **every** dimension of its group (whole accounts; a value list within a dimension is an OR) — and each appears in the portfolio list with lineage to its source and its group label.
3. **Given** a confirmed group whose member set matches an earlier group, **When** the cart is confirmed again, **Then** the existing group row is adopted under the newly typed name — no duplicate portfolio, the run skipping or healing idempotently (P-22).
4. **Given** a group whose filters no single account satisfies, **When** the run executes, **Then** that group fails with the recorded reason "no account matches every filter" and creates nothing, while the cart's other groups proceed.
5. **Given** a live cart, **When** the analyst tries another breakout on the same portfolio (quick or custom), **Then** it is refused as already running — one breakout episode per portfolio, either direction.
6. **Given** a group name that a portfolio in the EDM already carries, **When** the analyst types it, **Then** the name is flagged as taken, and **When** they try to add the group anyway, **Then** the add is refused with the name called out — with Risk Modeler unreachable, the check warns and the add proceeds (fail-open), the run's duplicate-name handling being the backstop.

## Requirements

**Prerequisite gate & entry point**

- **FR-001**: The breakout action MUST be offered per source portfolio on the existing per-portfolio table on the EDM detail page (spec 004) — no new page.
- **FR-002**: The prerequisite gate (§13.1 pattern, computed in code per constitution Art. 2) MUST enable the breakout action only when the EDM is imported/ready and the source portfolio exists; the per-dimension option MUST additionally require a stored exposure summary for the source portfolio carrying breakout values for that dimension, with ≥ 2 distinct values. A summary backfilled before this iteration carries no breakout values at all — its state list holds a mixed vocabulary of state names and codes that MUST NOT be read as filter values (P-12) — so it MUST be treated as a missing summary and point at Sync. The action MUST also be disabled while a detail refresh for the EDM is pending or running, because that job rewrites the very summary the preview reads — the same condition the EDM's Sync action already applies to itself. Ineligible states MUST render disabled-with-reason, never a hidden control or a mid-flow error.
- **FR-002a**: At confirm — and only there; the modal renders from stored state — the system MUST verify the stored summary is fresh: the source portfolio's current Risk Modeler `stampDate` (read via `search_portfolios`, the submit-time name-resolution pattern of constitution Art. 2 — never a `get_*` poll) MUST equal the `stampDate` captured when the summary was backfilled. On mismatch, or when no stamp is stored, the confirm MUST be refused with a reason pointing at Sync and **no breakout job row may be created**. To anchor the comparison, the spec-004 summary backfill captures the portfolio's `stampDate` alongside the summary it writes.
- **FR-002b**: The confirm MUST also verify that the stored summary is the one the preview was rendered from: the preview carries its summary's `as_of` into the confirm, and a confirm whose stored `as_of` no longer matches MUST be refused with the re-rendered preview and **no breakout job row**. The `stampDate` check of FR-002a does not cover this — a detail refresh that leaves the Risk Modeler portfolio untouched writes back an equal `stampDate`, so a summary rewritten mid-preview passes it. The two checks answer different questions: FR-002a asks whether Risk Modeler moved under the summary, FR-002b asks whether the summary moved under the analyst.
- **FR-003**: The gate rule MUST be computed in code (the Iteration-4 gate work item from PRD §21), with unit tests (constitution Art. 12 names the gate as a must-test). There is no central gate module today — existing operations gate via service-side guard functions plus template-side disabled states; whether this feature follows that pattern or introduces a small shared gate helper is a plan decision, but the rule itself MUST live in one testable place, not be duplicated across templates.

**Breakout dimensions & value enumeration**

- **FR-004**: The system MUST offer exactly three one-click breakout dimensions: **line of business**, **geography at state-or-state-equivalent grain** — the exposure's first-level administrative division (a US state, Canadian province, or equivalent), the same Moody's attribute for US and non-US exposure alike, so global portfolios break out with no separate mode — and **country** (added 2026-08-10; the follow-on research.md P-03 anticipated). Country values are the `Address` country codes (`CountryCode`, falling back to `CountryRMSCode` — and `CB` for the Caribbean, whichever the ISO code says, P-28); the EDM carries no country-name column, so the code is its own display and no label is synthesized (P-12). No custom filter builder, no complement split, no "do the opposite", no CRESTA/ZIP grain.
- **FR-005**: The values fanned out over MUST be the distinct values actually present in the source portfolio, read from the **stored** per-portfolio exposure summary maintained by the Iteration-3 backfill. Each value MUST carry the account count behind it (FR-007) and, for geography, an optional display label. Geography values are `Admin1Code` — the first-level administrative division's code, the same field for US and non-US exposure, so a global portfolio enumerates through it with no separate mode; the state *name* is a separate exposure attribute that is absent until the EDM is geocoded and MUST NOT be used as a value (P-12). Caribbean exposure is the one exception: `Admin1Code` sits below the island there, so the island's ISO3A country code is the state value and carries no label (P-28). The request path MUST NOT query DataBridge or Risk Modeler to enumerate values (constitution Art. 11), and the analyst MUST NOT be able to type a value (pick-list only, FR §3). The enumeration source excludes blank/unassigned values (scrubbed in the summary SQL), so blank-value exposure lands in no sub-portfolio — disclosed per FR-007; a remainder sub-portfolio is complement-split territory, out of scope.

**Preview, confirmation & disclosure**

- **FR-006**: Before anything is created, the system MUST present a preview: the dimension, every sub-portfolio to be created (value, display label where one exists, generated portfolio name, and the number of source accounts it will hold) with no truncation of the list regardless of count, the count itself, the source portfolio's account total where the stored summary carries one (P-13 — the denominator behind the per-value counts; added at UI review 2026-08-04), and the disclosures of FR-007. Creation MUST NOT begin until the analyst explicitly confirms (constitution Art. 5 — judgment waits for a click).
- **FR-006a**: On confirm, the list of sub-portfolios — every value with its generated name and generated number — MUST be persisted as the approved plan, and the sub-portfolio creation MUST execute that persisted list (AGENTS.md rule 8). The names MUST NOT be recomputed at execution time: collision suffixing depends on the portfolio names present in the EDM, which the run itself changes, so a recomputed name can differ from the approved one. Account selection is resolved at execution time and is not part of the approved plan.
- **FR-006b**: The confirm POST composes the plan it persists, from the same stored summary and the same naming rule the preview used. Portfolio names in the EDM can change between the analyst opening the preview and confirming it, so a generated name's collision suffix MAY differ from the suffix displayed; the value, the account count, the set of sub-portfolios, and the generated `portfolio_number` MUST NOT differ, because none of them read anything outside the source portfolio's stored summary — which FR-002b holds constant across the window by refusing a confirm whose summary was rewritten. The differing suffix is acceptable because the `portfolio_number` — not the name — identifies the sub-portfolio for adoption and re-run (P-11/P-14). From the moment it is persisted the plan is authoritative: nothing downstream recomputes it.
- **FR-006c**: There is no maximum number of sub-portfolios — a global portfolio's state breakout runs to as many first-level administrative divisions as the exposure carries, and refusing it would leave the analyst no in-app path. Above a threshold held as one named constant (25 sub-portfolios) the preview MUST additionally state that the run takes several minutes — and no more than that; the queue-occupancy consequence stays out of analyst-facing copy (P-15 as amended 2026-08-04). The confirmation stays a single click — the statement informs the analyst, it does not add a second gate.
- **FR-007**: The preview MUST disclose, in plain language: (a) accounts matching more than one value are assigned **in full to every sub-portfolio they match** (Risk Modeler portfolios hold whole accounts; generated sub-portfolios can overlap and are not "pure"); (b) together the sub-portfolios cover the entire source portfolio **except** exposure whose value is blank/unassigned, which lands in none. Both MUST be **quantified for the portfolio being broken out** (P-13) — (a) as how many of the source's accounts land in more than one sub-portfolio, or that none do, because real overlap ranges from none at all to several times the source; (b) as how many accounts carry no value for the dimension and therefore land in no sub-portfolio, or that none do. Both counts MUST be measured **per account** at Sync: the number of accounts carrying more than one value, and the number carrying at least one. Neither may be derived from the per-value account counts, which count sub-portfolio memberships — an account carrying three values contributes three, and an account carrying none contributes nothing while still being in the portfolio total, so the two errors cancel and the difference can read as a clean partition for a portfolio where most accounts land nowhere. A stored summary carrying no measured counts MUST fall back to the qualitative disclosure rather than state a derived number. Each disclosure is **one short line** (P-21, revised 2026-08-09): the overlap as `Warning: overlapping accounts — N of M accounts match more than one {dimension} and are included in full in each matching sub-portfolio` (or that none do), the blank-value line as `N of M accounts carry no {dimension} value and are left out` (or that none are). The previously mandated prose — the exposure-inflation statement and the geography-specific multi-state call-out — is deleted (rationale in research.md, session 2026-08-09).

**Sub-portfolio creation**

- **FR-008**: On confirm, the system MUST create one sub-portfolio per value, populated with **exactly the source portfolio's accounts matching that value** (selection scoped to the source portfolio, never the whole EDM) — an app-side loop, one sub-portfolio at a time, so each has an individually captured outcome. A sub-portfolio whose selection matches zero accounts MUST fail with a recorded reason and create nothing for it — the run-time backstop behind the FR-002a freshness check, and the visible failure mode for a selection-filter regression (which returns zero accounts against a perfectly fresh summary).
- **FR-009**: Each created sub-portfolio MUST be persisted as an `irp_portfolio` row carrying its Risk Modeler portfolio id and its breakout lineage: source portfolio, dimension, and value (the filter value — the state *code* for geography, per P-12). Both the create (HTTP 201) and the account add (HTTP 200) are synchronous — confirmed live, with no `202` and no workflow URL on either call — so the id is written at creation time with no `irp_job` row and no poller involvement (constitution Art. 11).
- **FR-010**: Generated names MUST be `{source portfolio name} - {display label, else value}` (P-10; the label half is the P-12 revision of 2026-08-05 — a geography sub-portfolio is named `cbhu - Puerto Rico`, never `cbhu - 200`) and MUST fit Risk Modeler's **40-character** portfolio-name limit, which the source portfolio name absorbs: the name token is kept whole and the source name is truncated to fit, with room reserved for a collision suffix (P-11). Names MUST be made collision-safe against every existing portfolio name in the EDM and the run's other planned names by appending the lowest free ` (2)`, ` (3)`… suffix (RM rejects a duplicate name). Each generated sub-portfolio MUST additionally carry a generated `portfolio_number` of at most 20 characters, composed from the source portfolio's Risk Modeler id, the dimension, and the breakout value — inputs that do not change between a preview and a later re-run — which is what identifies the sub-portfolio for adoption (FR-011), since the name is not stable across runs. The Risk Modeler description MUST name the source portfolio, dimension, and value **in full and untruncated** — with the display label beside the value when the plan carries one — so nothing the name loses is lost outright and both the code and the label stay searchable inside Risk Modeler.
- **FR-011**: A breakout MUST be idempotent at the sub-portfolio level: re-running the same source + dimension breakout MUST detect already-created sub-portfolios (by lineage) and create only the missing ones — the recovery path for partial failure and double-submission (a double-submitted confirm creates no duplicates). When Risk Modeler already holds a sub-portfolio the app has no row for, the system MUST resolve it by its generated `portfolio_number`, adopt it, and complete its account population rather than creating a second one; if more than one portfolio carries that number, that sub-portfolio MUST fail with a recorded reason rather than adopt an arbitrary one. A re-run after the sub-portfolios were deleted in Risk Modeler MUST reuse each soft-deleted lineage row — `deleted_at` cleared, the new Risk Modeler id and name stamped onto the same row — never insert a second row for the same (source, dimension, value) (T-16). There is NO rollback: created portfolios are never deleted by the app (P-07).
- **FR-012**: Per-sub-portfolio outcomes (created / failed with reason / already existed) MUST be reported to the analyst at completion, progress MUST be visible while a long run is underway (a 40-state fan-out must not appear hung), and a partial failure MUST leave the app state consistent with Risk Modeler (every row written corresponds to a real RM portfolio — including when the source was deleted in RM between preview and confirm, where each create fails cleanly with the RM error recorded). Failure reporting MUST be durable-state-derived: beyond the transient completion toast, failed sub-portfolios render as an error line on the source portfolio's row — derived from the latest terminal breakout job's outcomes, surviving refresh and navigation, with no dismissal state — until superseded by the next terminal run for that portfolio + dimension (Sync → re-run for drift; plain re-run for transient failures).

**After creation**

- **FR-013**: On breakout completion (including partial success), the system MUST auto-enqueue the existing EDM detail refresh (the spec-004 backfill worker) so generated portfolios acquire their exposure figures without analyst action — mechanical follow-up auto-fires (constitution Art. 5). The web request MUST NOT wait on the refresh.
- **FR-014**: A generated portfolio's collapsed row MUST carry a `↳` mark under its name, and expanding the row MUST show the base portfolio it was broken out from and its breakout criteria in the Risk Modeler description format (P-27, revised 2026-08-11 — the marker identifies, the expanded panel explains): for a quick breakout `{dimension label} IN ({value})`, the value rendered as its display label where the source portfolio's stored summary carries one, falling back to the stored code (P-12 as revised 2026-08-05); for a custom group the full filter set, `{dimension} IN ({values})` AND-joined across dimensions, exactly as the generated portfolio's Risk Modeler description records it. Base and broker-arrived portfolios render no lineage. A generated portfolio is an ordinary portfolio — once its own summary is backfilled it can itself be broken out — and a portfolio generated from a generated portfolio MUST show its immediate source only, never a rendered chain.
- **FR-015**: The breakout action MUST be audited: actor, timestamp, source portfolio, dimension, and per-sub-portfolio outcomes. No audit-log table is added (P-08) — each field is recoverable from a convention that already exists: the actor from the breakout job's `input_data.actor_id` and the business-event log line, the timestamp from the job row, the source portfolio from `requestor_id` — for a group job, through `breakout_group.source_portfolio_id` (the job's `requestor_id` is the group row's UUID) — the dimension from `rwb_job_type`, and the outcomes from `output_data.sub_portfolios`. Generated portfolios additionally carry the confirming analyst in `inserted_by`.

**Custom grouping (follow-on — user story 4)**

- **FR-018**: The breakout modal MUST offer a **Custom groups** mode beside the quick one: dimension pills for every value dimension whose stored summary carries ≥ 2 distinct values (line of business, state, peril — P-19), checkbox value lists that keep their ticked state across pill switches, a group-name input, and a **cart** the composed groups accumulate in. The typed name **is** the sub-portfolio name (P-24 — no prefix, no pre-population) and MUST be refused when a portfolio with that name already exists in the EDM, in the cart, or in Risk Modeler: checked as the analyst types and blocked at Add, with the Risk Modeler leg failing open when unreachable (P-25); an adopted member set's own approved name is never refused, and the Risk Modeler leg is skipped for it (the run's duplicate-name handling backstops). Every cart row is composed **server-side** from the stored summary (name, canonical member set, upper-bound account count — P-23) and every submitted value MUST be validated against the stored summary at confirm; the confirm applies the same ordered refusals as the quick one (gate re-check, FR-002b `as_of`, FR-002a stamp — once per cart) and creates **no job row on any refusal**.
- **FR-019**: A group's identity is its **canonical member set** — dimensions and values sorted and deduped, hashed into a `group_key` (P-22). A group's `portfolio_number` is its name truncated to 20 characters (P-26). Re-confirming the same member set MUST adopt the existing group: same `breakout_group` row, never a second portfolio, the row taking the newly typed name and number (P-22 rev. 2026-08-10); the run over an adopted group skips or heals idempotently exactly as FR-011 prescribes, including reclaiming a soft-deleted lineage row (T-16). Two groups with the same member set in one cart MUST be refused. A group's generated portfolio records lineage as dimension `custom` with the `group_key` as its `breakout_value` plus a link to the `breakout_group` row, whose label and filters are the single source of display truth.
- **FR-020**: Each group in a confirmed cart runs as **one `rwb_job`** (`run_breakout_custom`, requestor `breakout_group` keyed on the group row's UUID), all stamped with a shared `cart_id`. One breakout episode per portfolio: a live cart MUST block a new confirm on that portfolio in either direction (quick blocks custom, custom blocks quick and further carts). Progress renders per portfolio as "custom groups: k of n done" over the cart, and the completion banner aggregates the cart's terminal jobs into one summary.
- **FR-021**: Groups MAY overlap — the cart warns (a may-overlap note on rows sharing a selected value, plus the standing whole-account disclosure) and never blocks (P-18). The membership rule is **OR within a dimension, AND across dimensions** (P-20): a group's sub-portfolio holds exactly the source accounts carrying at least one selected value in **every** filtered dimension. Preview counts are upper bounds ("up to N accounts" — P-23); a group no account satisfies fails at run time with a recorded reason and creates nothing (FR-008 semantics). The Risk Modeler description lists the full filter set untruncated (FR-010 pattern).

**Cross-cutting**

- **FR-016**: The breakout flow MUST follow the shell conventions (real URLs, `hx-boost` nav, CSRF on the state-changing POST, idle-timeout handling) and the graceful-empty doctrine everywhere a summary may be missing.
- **FR-017**: Everything else on the EDM page stays read-only as shipped in spec 004 — this iteration adds the breakout actions and lineage display, and MUST NOT add portfolio edit/delete/merge or any other §10A.7 out-of-scope capability.

## Key Entities

- **Portfolio (`irp_portfolio`)**: gains **breakout lineage** — a nullable self-reference to the source portfolio plus the breakout dimension and value (null for broker-arrived portfolios and, per DATA_MODEL, still no `created_by_irp_job` lineage — creation is synchronous). The stored value is the one the selection filter uses: the state code for geography, the LOB name for line of business. The dimension is an app-defined closed set → kind table per constitution Art. 3 (exact shape in data-model.md).
- **Breakout (the action, not a stored aggregate)**: a confirmed fan-out of one source portfolio over one dimension; its durable trace is the approved plan and per-sub-portfolio outcomes on the breakout job row, plus the lineage on the generated `irp_portfolio` rows. Planning decided lineage + audit only (research R3) — no breakout table.
- **Exposure summary (stored, spec 004)**: the read source for value enumeration; its absence gates the action. This spec adds four things to its capture: per-dimension breakout values, each with its account count and (for geography) a nullable state-name label; the portfolio's total account count; per-dimension **coverage** — the accounts carrying at least one value and the accounts carrying more than one, counted per account, which together with the total are the FR-007 overlap statement; and the source portfolio's RM `stampDate`, the comparison anchor for the FR-002a freshness check. The breakout values are a **new** key — a summary written before this iteration has none, which is what makes it detectably stale (FR-002).
- **Prerequisite gate rule**: the Create-subportfolio row of the §13.1 gate ("EDM + ≥1 portfolio exist", extended per FR-002), computed in code.

## Success Criteria

- **SC-001**: From the EDM detail page, an analyst produces one sub-portfolio per distinct LOB (or per distinct state or state-equivalent) of a source portfolio in a single confirmed action — no Risk Modeler UI, no RiskLink; 100% of offered values come from the source portfolio's stored summary and 0 free-text value entry paths exist.
- **SC-002**: The created sub-portfolios together cover the entire source portfolio (every account appears in ≥ 1 of them), with the blank-value exception and the **measured** account overlap for that portfolio disclosed before confirmation — the FR §3 "sum to 100%" goal, honestly stated for an account-bucketed filter. A source portfolio with no multi-value accounts is disclosed as a clean partition; one where most accounts repeat is disclosed as such, from the same arithmetic.
- **SC-003**: A typical breakout (≤ 15 values) completes and is reflected in the portfolio list within 30 seconds of confirmation; larger fan-outs (40+ values) complete reliably with per-sub-portfolio outcomes visible, and no fan-out is refused for its size (P-15); generated portfolios then show exposure figures without further analyst action, within the spec-004 post-import backfill time bound.
- **SC-004**: A partial failure never leaves app state inconsistent with Risk Modeler, and re-running the breakout completes the missing sub-portfolios without duplicating existing ones — demonstrable by killing the loop mid-run and re-running.
- **SC-005**: The prerequisite gate enables/disables the action correctly from entity state alone (imported EDM, portfolio presence, no detail refresh in flight, summary presence, ≥ 2 values) — the PRD Iteration-4 exit criterion, verified by unit tests. The confirm POST additionally refuses when the summary was rewritten since the preview rendered (FR-002b) and when Risk Modeler has moved under the summary (`stampDate` match, FR-002a) — both before any job row is created, and the first fires even where the second passes.
