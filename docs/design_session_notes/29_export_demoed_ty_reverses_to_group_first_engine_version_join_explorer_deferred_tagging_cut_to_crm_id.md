# IRP Workbench — Design Notes: The Results Export Demoed End to End; TY Reverses to Group-First and `28` O28-9's Owed Test Is What Reversed It; the Lookup Join Is the **Engine** Version, Correcting `23` O23-10; the Results Explorer Is Deferred on an Analyze Re Dependency Nobody Had Costed; and Tagging Collapses to CRM ID Alone

**Source:** Design session, September 11, 2026 (~87 min, ending because Ben, Anil and Wendy had a deployment-team call immediately after) — Ben Bailey (PremiumIQ) demoing throughout, Cheryl TeHennepe (CIC) and Wendy Hayes (CIC) for the full call, **Anil Venugopal (PremiumIQ) from ~6 minutes** and present to the end. Cross-checked against the full transcript (439 speaker turns; Ben 549 caption cues, Wendy 321, Cheryl 319, Anil 242 — Anil a primary participant rather than the late arrival he was on 9/10). **This is the session `28` §11 booked**, and it delivered both named items: the **loss results export demo** and **tagging**. Shape: ~45 minutes of live export walkthrough (form → status screens → the loss-repository data rows), ~22 minutes reopening **TY** on new evidence, ~17 minutes on tagging, ~3 minutes of wrap.
**Status:** Working design notes. **The export exists and was shown working, which makes this the first session in the export thread where the subject is running code rather than a specification.** Three things happened that change prior decisions rather than adding to them. **(1) `28` D10 / `21` O21-4(b)'s answer is REVERSED: TY export goes group-first.** Cheryl ran the test `28` **O28-9** said she owed, and the result is negative — Risk Modeler does **not** aggregate treaties across a multi-analysis export; three analyses produced three zip files. Anil carried the architecture argument and CIC accepted the workflow change. **(2) `23` O23-10's stated key for the historical lookup table is WRONG** — it is keyed on the **engine** version, not the data model version, and Ben's one failed export in the demo was exactly that mis-join. This is `23` **D19**'s defect recurring in a second place. **(3) Tagging collapses to CRM ID alone**, which guts `27` **D22** and re-scopes **D23** — and collides head-on with `28` **O28-13**, because the one tag they kept is the one group import depends on and **spec 012 FR-017 says a group cannot carry it**. **Also new:** the Loss Repository Explorer was proposed and deferred on an **Analyze Re** dependency that appears nowhere in 28 prior design notes and only as a one-line MVP exclusion in the FR; the **manifest and staging tables must become treaty-grained**, which is `25` **O25-1**'s grain question arriving with an answer for one mode and not the other; and the **duplicate-export block is relaxed to a warning**, a subject with zero prior coverage in the series. **Advances** `23` **O23-5**, **O23-7**, **O23-10**; `25` **O25-1**; `27` **O27-7**, **O27-8**, **O27-9**; `28` **O28-2**, **O28-9**, **O28-13**. **Answers** `28` **O28-9**. **Does not advance** — **fourth consecutive note** — the **staging DDL**, **deployment**, or **status**.
**Where this sits in the series.** `28` is **September 10** (TY as an output level, the burst requirement, the treaty key) — its decisions are cited as **9/10 D**n and its open items as **O28-n**. `27` is **September 4** (**9/4 D**n / **O27-n**); `26` is **September 3**; `25` is the **September 2** technical session on the loss-repo write; `24` is **September 2**; `23` is **September 1** (**9/1 D**n / **O23-n**) and is the note this session's first 45 minutes are the direct continuation of; `21` is **August 27**, the original export walkthrough. **The September 8–9 gap `28` §1 identified is still unrecorded** — and this session adds a second undocumented contact: Ben refers to the target SQL Server's missing tables as something *"we called out this morning,"* so there was a **9/11 morning call** with no note, and the **deployment-team call immediately after this one** has no note either.
**Related:** `23_status_yellow_export_staging_architecture_loss_repo_field_mapping_ep_is_stochastic.md` (**§6 the `data` table field by field D9–D18, §7 the ELT tables D19–D22, §9 CIC's historical lookup table D22/D25/D26, O23-2 the staging concept, O23-5 write the mapping down, O23-7 `data name`, O23-10 the lookup table**), `25_loss_repo_write_approved_staging_tables_in_loss_db_parquet_transient_no_lower_environment.md` (**D2 the manifest/driver table, O25-1 its home and grain, O25-2 the load/batch ID, O25-5 the job types**), `28_ty_is_an_output_level_treaty_export_bursts_per_treaty_cross_analysis_aggregation_treaty_key_answered.md` (**D5 the burst, D6 cross-analysis aggregation, D10 no Risk Modeler group, O28-2, O28-9 the owed test, O28-13 groups carry no tag**), `27_finish_no_summary_sim_set_defaults_reversed_group_import_crm_tagging_sequenced.md` (**D21 group import, D22 the tagging design, D23 the sequencing, O27-7, O27-8, O27-9 sorting and filtering**), `21_export_walkthrough_workflow_tool_ruled_out_client_table_perspective_validity_stochastic_historical_blocker.md` (**O21-3 per-analysis perspective validity, D14 WX/QS are not uploaded, O21-10 rewrite FR §7 Delivery**), `22_viewing_signed_off_grouping_event_rate_defect_edm_terminology_client_table_read_only_parquet_pivot.md` (**D25 / O22-9 the client table is read-only**), `18_suite_execution_event_rate_active_flag_analysis_naming_notes_tags_grid.md` (**D4/D5 failures on top, D6 multi-select delete, O18-6 legible failure reasons**), `../FUNCTIONAL_REQUIREMENTS.md` (**§7 Delivery lines 594–599 — two rows; line 601 the MVP exclusions incl. Analyze Re and CRM-ID tags; line 116 AND semantics scoped to submission search; line 118 search/sort/filter on every list section, Partial since 7/14; line 367 template tags; line 446 the submission-name tag marked Not implemented against code that implements it; line 562 "engine / model version"**), `../DATA_MODEL.md` (**§9 lines 567–618 — `analysis_result_meta`, the exactly-one CHECK at 617, `perspective_code` "GR / RL / WX / QS / GU" at 585, the Parquet path at 618; §12 the table manifest at 755–796 with no staging row; line 822 the loss/exposure schemas**), `../PRD.md` (**§15.2 the six caches that exist; §19 lines 1224–1241 global search, analyses deferred; line 1189 the per-analysis version gap**), `../../specs/012-grouping-execution/spec.md` (**FR-017 line 124 — groups carry no tag; O-07 line 53**), `app/services/analysis_service.py:386` (`engine_version=_text(_first(p, "engineVersion", "modelVersion"))` — **the two versions coalesced into one field**), `app/workers/analysis_jobs.py:350–351`, `app/services/grouping_service.py:315` (`_SELECTION_KEY = ("peril_code", "region_code", "model_version")`), `app/services/analysis_execution_service.py:252–254` (the submission-name tag, implemented), `../../../../CIC/minutes/Risk_Modeler_Interface_Design_Minutes_9-11-26.md`, `../../../../CIC/transcripts/Risk Modeler Interface _ Design 9-11-26.vtt`

> Decision IDs (**D1**–**D29**) below refer to the tables in the 9/11 design minutes. Decisions from 9/10 are cited as **9/10 D**n (note `28`), from 9/4 as **9/4 D**n (note `27`), from 9/2 as **9/2 D**n (notes `24` / `25`), from 9/1 as **9/1 D**n (note `23`), from 8/27 as **8/27 D**n (note `21`), from 8/19 as **8/19 D**n (note `18`). Open-item IDs are **O29-n**. **This session is both a demo and a design session** — the first since `27` where the Workbench build advanced on screen — so the delivery thread resumes here after two notes away.
> *Transcript note: the transcription garbles product names, domain vocabulary and two personal names consistently, and interpretations reflect context: **"Rose Mueller" = Risk Modeler**; **"3D" / "3Ds" / "3D type" / "TUI" = TY** (treaty); **"sip files" = zip files**; **"seed name" / "seed" = cedent name**; **"all state" = Allstate** (an example cedent); **"parallel" = peril** ("average annual loss by parallel" = AAL by peril); **"R lookup table" = our lookup table**; **"PR2" / "PRL1" = PPR2 / PPR1** (per-risk layers); **"Sinfin.rms.com" = the CinFin tenant**; **"the date of vintage" = data vintage**; **"outloaded" = loaded**; **"market is closed" = mark it as closed**; **"society in perspective" = analysis ID and perspective**; **"Trial ELT repository" = the ELT repository**; **"Top tackling" / "taking" = tagging**; **"unanalysis" = an analysis**; Wendy's *"we spent a lot of time talking about TeHennepe yesterday"* = **talking about tagging yesterday**. Ben's *"Can we get James his logic?"* is read as **Cheng's** logic — Cheryl had just said *"I don't know how Cheng handles that"* and answered *"we can look at his query"*; **Cheng is a real person** on the CIC workflow-tool side, consistent with `23` and `28`. Stray tokens ("Bumb", "Bill", "Tim", "Paul", "Joe", "Who?", "Do you risk it?") carry no content; Ben's closing *"Thanks, Tim"* is read as **thanks, team**.*

---

## 0. TL;DR

The export is real. That is the headline and it changes the character of the thread: for six sessions the loss-repository push has been specified, architected and argued over with nothing to look at, and today Ben ran it front to back — select analyses, fill a form, watch the status screens, open the resulting rows in the data table. What the client did with that is the interesting part. **They spent most of the demo removing scope rather than adding it**, and the one failure on screen turned out to be a recurrence of a defect `23` already caught once.

- **THE EXPORT WAS DEMOED END TO END.** Submission → select analyses → a full-page export form (cart selection, perspective, per-analysis data name, client, treaty inception date, CRM ID, data vintage) → an export summary screen → live status → data-table rows in the loss repository. Ben on the runtime: *"It actually is really quick."* **Every field in `23` §6's mapping was walked on screen and confirmed or corrected** — the first time that table has been checked against something that exists. (§2)
- **WENDY REMOVED A SUBSYSTEM, AND THE REASON HAS NEVER BEEN COSTED IN THIS SERIES. ⚑⚑** Anil proposed a **Loss Repository Explorer** — a dedicated page against the loss repository rather than the Workbench database, filterable by submission and perspective, reachable without navigating a submission. Cheryl wanted it (it mirrors the live editable grid she depends on). Wendy killed it: CIC must use their own workflow tool anyway to push results to **Analyze Re**, which is out of scope, *"So I have to go there anyway."* Cheryl conceded immediately — *"Solid point… I wasn't thinking about that, but you're right."* **`Analyze Re` appears in ZERO of the 28 prior design notes.** It exists in the repo only as an MVP exclusion (`FUNCTIONAL_REQUIREMENTS.md:601` — *"uploading loss sets to Analyze Re (separate API)"*; `PRD.md:1132`). **A one-line exclusion just determined a scope decision, which means the exclusion is load-bearing and nobody has written down what it costs the user.** → **O29-1**. (§3)
- **AND IT PUT A DATE ON THE TABLE.** Wendy, on when the Explorer might come back: *"Probably just not by October 1st."* **October 1 is the first delivery date named anywhere in the series.** Nothing in the repo carries it. → **O29-14**. (§3)
- **WHAT THE STATUS SCREENS ACTUALLY NEED IS FAILURE TRIAGE — third raising, new surface (D5, D6, D7).** Wendy: *"nine times out of 10, it always successfully loads… the most valuable thing right here is knowing how many failed, which ones failed."* Agreed shape: quick filters **all / failed / loaded**; a **Close / Resolved** action so a handled failure drops off the list; status and data ID moved left; **AAL by peril** added; the zip file-path column removed. **`18` D4/D5 settled the analyses grid the same way (failures on top, group-by not sort) and `18` O18-6 — legible failure reasons — has been open since 8/12 and unadvanced in every note since.** **But "resolve / close / dismiss" is genuinely new: a grep of all 28 prior notes returns no dismissal concept at all** — the failure lifecycle as designed is failures-on-top plus multi-select delete (`18` D6). → **O29-2**. (§3.2)
- **THE ONE FAILED EXPORT WAS `23` D19's DEFECT IN A SECOND PLACE, AND IT CORRECTS `23` O23-10. ⚑⚑** Ben was joining the historical-events lookup on the analysis's **data model version** (18.1) rather than the **engine version** (25). Wendy: *"what we track in our lookup table, even though it says model version… it's engine version… every analysis I run, every peril, every country, everything has to say version 25 on it"*, because the Analyze Re catalog requires one consistent version per result set. **`23` O23-10 records the lookup as *"keyed on event ID / peril / data model version"* — three times, sourced to Cheryl being *"ninety-nine percent sure"* with the caveat that it needed verifying with Cheng. It was verified today, and it is wrong.** Cheryl herself corrected it: *"we want the engine version there… we don't maintain that data model version."* → **O29-5**. (§4)
- **WHICH MAKES `23` D19 SHARPER, NOT WEAKER.** D19 required *"the ELT table's `model version` takes the data table's `data model version`"* — Ben having caught an event-rate-scheme version code of **17** against a DLM version of **25**. **The same class of bug, in a different join, six weeks later.** `23` O23-5's instruction was *"Assert it, don't document it."* **It was not asserted, and the second occurrence cost a live demo failure.** → **O29-5**. (§4)
- **THE VALIDATION THAT FAILED IS BEING DELETED, AND IT SHOULD BE (D8).** Ben, on his own code: *"this is what would be considered an unnecessary validation of this model version 23, where we're checking to almost make sure if it's a valid model version, which this is not the right place to do it. **This is a totally invalid error.**"* Cheryl confirmed the premise is false: *"it would never not be historical data because of a mismatch of this model version."* (§4)
- **CIC WILL HAND OVER THE REAL LOOKUP TABLE — a cheaper path than `23` O23-10 contemplated (D10). ⚑** O23-10 asked Ross for the **DDL** and Cheng for the **usage**; neither arrived in ten days. Today Anil asked for the table itself and got a yes in under a minute: ~**2,500 rows**, CSV with **headers preserved**, or a `.bak`. Wendy: *"we could literally export it and send it to a CSV file."* Cheryl on sensitivity: *"nothing more than what you've pulled from the software today."* **It carries the PCS column that the Moody's reference data lacks** — which is the reason CIC built it (`23` §9). **This retires the DDL half of O23-10 and most of the usage half, because the shape and the join column come with the data.** → **O29-5**. (§4.2)
- **AND IT CONFIRMS `23` D22 FROM THE OTHER SIDE.** D22: *"`PCS` 'is a lookup' and is **not required**"* — Wendy, *"we don't have PCS numbers for everything."* Today Ben notes the handover *"would give us some PCS values too,"* and is visibly relieved at the size: *"I was like, oh wow, there's only 2,500 historical events… it's not a big table."* (§4.2)
- **`23`'s FIELD MAPPING SURVIVED CONTACT, WITH THREE CHANGES — and one of them is a reversal. ⚑** `data name` never required — **CONFIRMED**, closing `23` **O23-7** (D3). `client ID` read-only search — **CONFIRMED** (`22` D25 holds). `analysis ID` the user-facing one — **CONFIRMED** (`23` D17). But: **`database` reverses from "not populated" to the Workbench database name** (D11); **`data vintage` moves from "leave blank" to *required*** (D2); and **`server` has drifted** — `23` said the tenant base URL, the build has the API URL, and Cheryl waved it off. → **O29-4**. (§2.3)
- **THE `database` REVERSAL IS WORTH MORE THAN IT LOOKS, BECAUSE OF WHY WENDY WANTED IT.** `23` retired the column (*"Held the RDM name; 'no longer valid'"*). Wendy re-purposed it as provenance, from experience: *"we found a problem in a piece of code… and it's like everything up until that date was wrong… that's why it's helpful to be like, **everything that has the Workbench label on it came through the Workbench**, and if we identify a problem later, we can query for that and find them."* **That is a request for a bad-data blast-radius query on a production repository the Workbench writes to. Record it as a requirement with a reason, not a field default.** → **O29-4**. (§2.3)
- **AND IT RE-OPENS SOMETHING `23` CLOSED: data vintage on the submission. ⚑** `23` §6 recorded *"Cheryl confirmed it is not captured on the submission."* Today Cheryl's instinct was the opposite — *"Don't we enter that on the submission page? I thought we did"* — and Wendy later put **data vintage and expiration date** on the list of fields the submission page is missing. **The 9/1 confirmation is being revisited by the person who gave it.** → **O29-4**, **O29-12**. (§2.3, §7)
- **DUPLICATE EXPORTS: A BLOCK BECOMES A WARNING, ON A SUBJECT WITH NO PRIOR COVERAGE AT ALL (D13, D14). ⚑** Ben hit his own hard block mid-demo. Cheryl: *"Today we don't have anything that stops us from doing that"* — the normal case is realising a flag was off, deleting, rerunning, and getting the same name — and the remedy is a name, not a block: *"I could put a V2. That's common for us to do."* Ben: *"I'll drop it then. And allow duplicates, but warn."* **The staging check keys on `analysis ID + perspective`** — a new fact about what is built — **and Anil flagged it has to be relaxed to match**, with the honest reason: *"I was trying to think of a way to safely check for duplicates. **There isn't one.**"* **A grep of note 23 for `duplicate` / `re-export` / `idempot` returns nothing; the only idempotency in the repo is `DATA_MODEL.md:616`'s upsert on `(rdm_id, analysis_name, perspective_code)`, which governs retrieval, not the push.** → **O29-3**. (§2.4)
- **TY REVERSES TO GROUP-FIRST, AND `28` O28-9 IS WHAT REVERSED IT. ⚑⚑⚑ Supersedes 9/10 D10; re-opens `21` O21-4(b)'s answer.** `28` **O28-9** recorded that *"the Moody's-side cross-analysis case is asserted, not observed"* and that Cheryl owed the test. **She ran it, and the answer is no.** Ben read her message in: three analyses exported together at gross loss + treaty output level → **three separate zip files, one per analysis**, no aggregation. *"This confirms that we need to create a group first and then export to the group to get one set of output."* **`28` built its whole design on behaviour CIC's workflow tool performs; the platform does not perform it.** → **O29-6**. (§5)
- **ANIL CARRIED THE ARCHITECTURE ARGUMENT, AND IT IS A GOOD ONE (D16).** Group first and the multi-file problem disappears: one grouped analysis → one export job → one zip → one manifest row, with the aggregation done by Moody's. *"We don't even have to worry about aggregating anything. **We don't need the aggregation formulas. It's already aggregated on Moody's.** It seems to solve two problems at the same time."* **His description of the alternative is the clearest statement of the cost that has been made:** a treaty-grouping concept on the manifest that expects N files, holds the aggregation until all N arrive, and **times out and abandons the export if one never does** — because three succeeding and one failing means the aggregate is wrong, so *"collectively they failed."* Ben: *"It is easiest to group first. I agree."* (§5.2)
- **CIC ACCEPTED THE WORKFLOW CHANGE, AND `28` O28-10's BANKED FREEDOM IS NOW SPENT — DELIBERATELY (D17).** `28` **O28-10** recorded Cheryl releasing the group-on-the-fly constraint unprompted and advised *"bank the freedom rather than spending it."* **It was spent today, for a reason O28-10 did not have: the platform forced it.** Cheryl: *"that grouping process is not as difficult in Risk Modeler theoretically… **I'm going to go a little bit on faith that grouping continues to be not painful.** And so I'm completely okay with having to do that step first."* Wendy: *"Grouping is a huge pain point in RiskLink that in theory does not exist in Risk Modeler to the same extent."* **Anil named the causation exactly: the workflow tool's on-the-fly aggregation exists *because* RiskLink grouping is painful** — *"that is because of the pain that exists in the grouping. So we chose to take path B."* (§5.3)
- **LOSING CROSS-ANALYSIS AGGREGATION IS JUDGED ACCEPTABLE, AND CHERYL'S REASON IS BETTER THAN THE RISK (D18).** Ben flagged it as the real hazard: *"which is not how the workflow tool operates today, which could be confusing."* Cheryl: *"I don't think that that will be particularly confusing… **truthfully, the interface looks completely different. So there isn't this ability to select your treaties.** If it looked exactly the same as workflow and had different results, that would be confusing. This looks different."* Ben: *"That is truly the hardest part in my opinion. So if that's fine, then that's fine."* **A UI that is visibly not the old tool is being used as the mitigation for a behavioural difference — which is a design constraint on the export screen, not just a reassurance.** → **O29-6**. (§5.3)
- **BUT BOTH OPTIONS STAY OPEN, ON ANIL'S INSTRUCTION (D19). ⚑** *"Preserve both the options. It comes down to whether we trust Moody's to do the grouping right on Risk Modeler versus RiskLink, or do we trust ourselves more to be able to do that without messing anything up. So keep, preserve the options. **Let's pick one, tread that path, and if it doesn't work**"* — fall back. Ben: *"there's a couple options. **Let's document them, the trade-offs, decide what we want to do after doing that.**"* **D16 is a working direction with a documentation action attached, not a closed decision. Write the trade-off document before the worker is built, because the two paths differ in where the aggregation lives and that decides the manifest's grain.** → **O29-6**. (§5.4)
- **THE BURST REQUIREMENT SURVIVES UNTOUCHED, AND CHERYL MADE SURE OF IT (D20). ⚑** She separated the two problems the moment the group agreed: *"It takes care of the aggregation issue. **It does not resolve the issue, Ben, of needing to explode this file into multiple files for each treaty layer** that you see in the file, unlike at the portfolio level."* Echoed by both CIC participants when Anil suggested grouping might collapse it — *"you still get multiple treaties"* / *"you still have two TYs that come out."* **9/10 D5 stands verbatim.** (§5.5)
- **AND THE MANIFEST STILL HAS TO GO TREATY-GRAINED — WHICH IS `25` O25-1's QUESTION, ANSWERED FOR ONE MODE (D21). ⚑⚑** Ben separated this from grouping explicitly: *"that's not what I'm foreseeing as the difficult part. It's rather **our manifest table, which currently defines the rows we need to write at the analysis level, which is not what TY perspective is. It's at the treaty level.**"* And: *"the manifest table has to be amended to account for TY perspective. **As it is right now, it will not handle it appropriately.**"* **`25` O25-1 asked *"What is the grain? Fix the grain explicitly, because it decides whether the load ID in O25-2 is per-archive or per-perspective."* Today adds a third candidate — per-treaty — that applies to exactly one export mode.** And `23` **D9**'s *"one `data` row per analysis"* is now one `data` row per analysis **except** at TY, where it is one per treaty. → **O29-7**. (§5.6, §8)
- **THE AGGREGATION MATH IS ON RECORD IN CASE THE FALLBACK IS TAKEN (D22).** Ben asked for the formulas. Cheryl: losses *"sum of the loss"*; standard deviations split independent and correlated — *"independent is additive… and then correlated, you have to take the square root of the sum of the squares."* **The exposure value is the unresolved one, and it is not additive:** *"in this case, it's the max for that treaty. So your exposure value is going to get kind of goofed up. **I don't know how Cheng handles that.**"* Ben asked for Cheng's query; Cheryl agreed. **This is the third outstanding ask of Cheng** — `23` O23-10 (how he uses the lookup), `24` O24-11 (the reference table owner / the 9/2 meeting, still no recorded outcome), and now this. **Batch them or they will keep not happening.** → **O29-8**. (§5.7)
- **TAGGING COLLAPSES TO CRM ID ALONE (D24, D25). ⚑⚑** The premise: Cheryl discovered Risk Modeler's tag search **ORs** rather than ANDs, so adding tags widens the result set. Her worked example — databases tagged X, Y and XY; asking for X **and** Y returns all three. Wendy: *"the tagging functionality in Risk Modeler to search by tags sucks… **our whole sort of thinking around tagging has somewhat gone out the window**."* Compounding it, the picker is one flat dropdown: Cheryl, *"Can you imagine if there are 5,000 tags in here?"* Anil's diagnosis: *"the tag as a **key value pair** would have been a better way to implement this."* **Dropped as Risk Modeler tags: analyst name, treaty year+month (TY2605), cedent name, reinsurance type, expiration date.** Ben went furthest — *"I don't want to tag at all on Risk Modeler"* — and Wendy stopped just short: *"we're just going to tag CRM ID. That's it… **because CRM ID gets us to everything we need, like every link in every system**… But I would hate to say zero."* (§6)
- **WHICH GUTS `27` D22 AND RE-SCOPES D23 — AND CIC's OWN EXTERNAL EVIDENCE NOW POINTS THE OTHER WAY. ⚑⚑** `27` **D22** was Cheryl's unprompted tagging design, and `27` **O27-8** called it *"better than the session that was deferred."* `27` **D23** sequenced three pieces: import-by-CRM-ID (**low**), auto-tag everything (**medium**), submission-level sync (**medium-high**). **Today cancels the second and effectively the third**, seven days after Cheryl told the group that peer companies already on the product said tagging *"was one of the things that they said they wish they would have tackled sooner rather than later, because it was a big deal."* **That is not a contradiction — the peers' regret was about findability, and today's answer relocates findability rather than abandoning it — but it must be written down as a reversal with its reason, or it reads as capitulation.** → **O29-9**. (§6.2)
- **AND HERE IS THE COLLISION. ⚑⚑⚑** The one tag CIC kept is **CRM ID**, and `27` **D21/D23** made **import groups by CRM ID tag** the low-effort, high-impact first move — the thing `27` **D24** says grouping v1 cannot be done without. **`28` O28-13 already flagged the problem; today makes it acute**, because CRM ID is no longer one tag among many but the *only* one. **`specs/012-grouping-execution/spec.md:124`, FR-017:** *"Every Workbench-submitted individual analysis carries a submission-level IRP tag whose value is the submission name (O-05)… **Groups carry no tag — the platform grouping job accepts none (O-07)**."* And **O-07 at line 53**: *"the platform grouping job schema has no tag field and **no endpoint tags an analysis after creation**."* **If no endpoint tags an analysis after creation, then a group can never carry a CRM ID tag, and import-by-CRM-ID cannot work for the one entity type it exists to import.** Anil's closing reassurance — *"if you are tagging EDMs and RDMs, then it should be okay… when you add or remove a CRM, that's when we fire that API"* — is about EDMs and RDMs, and **groups are neither**. **This must be checked before the low-effort estimate is spent.** → **O29-10**. (§6.3)
- **AND CHERYL NAMED THE SAME PROBLEM FROM THE OTHER END, WITHOUT CONNECTING IT.** *"I think the big challenge is **groups**… that is trying to **sync a group back to a submission**. We have to have something to hang on to to link that back to this submission."* **That is `27` O27-7 restated, and it is exactly what FR-017 forbids the obvious solution to. Two people described the same wall from opposite sides in the same three minutes and neither noticed.** → **O29-10**. (§6.3)
- **FINDABILITY MOVES TO WORKBENCH SEARCH — WHICH PROMOTES `27` O27-9 FROM A NICETY TO A DEPENDENCY. ⚑⚑ (D26, D27)** Wendy: *"the searching is so much better in the Workbench that if I was truly looking for something… I'm going to find that quicker in the Workbench than I would find it in Risk Modeler… **people will continue to keep coming to the Workbench because that tagging thing over there is ridiculous.**"* Ben: *"we have all of this searching for submissions, but **this essentially needs to be extended to EDMs, RDMs, and analyses too**."* **The repo says this does not exist.** `FUNCTIONAL_REQUIREMENTS.md:118` — *"Search, sort, and filter are available on every list section" | **Partial***, open since 7/14, with the note that **"no list section but the submissions master list has search or filter."** `PRD.md:1235` defers the analyses provider explicitly: *"**Analyses in particular has no unscoped list function yet** — every existing query is scoped to one EDM/RDM/submission — and no single per-analysis detail URL."* **`27` O27-9 has been carried as an unadvanced nicety for two notes. Today it became the mitigation for a scope cut. Re-rank it.** → **O29-11**. (§6.4)
- **AND THE "AND SEMANTICS" REQUIREMENT CIC IS RELYING ON DOES NOT COVER THIS. ⚑** `FUNCTIONAL_REQUIREMENTS.md:116` — *"**Multi-term search uses AND semantics, not OR**… Applies to the **name search, the cedant filter, and the 'links to' picker**"* — is scoped by its own text to three surfaces. Ben's design is per-purpose tag dropdowns ANDed together, which is a fourth. **Write the requirement rather than assuming line 116 stretches.** → **O29-11**. (§6.4)
- **BEN'S TAG MECHANICS ARE RIGHT AND SHOULD SURVIVE THE CUT (D28, D29).** Anil proposed wiping all tags on every object and reapplying on any submission change, *"for safety's sake."* Ben rejected deletion: *"I don't use delete tag. **I use a get-or-create approach only**"* — because a tag legitimately belongs to other submissions, *"so if there's a submission year that applies to multiple submissions… just leave it."* Plus a prefixed naming convention per attribute so tags can be matched by pattern. **Even at CRM-ID-only, get-or-create and the convention are the correct mechanics and cost nothing to record.** On ownership change, Cheryl: *"Given that it's OR functionality, maybe you don't have to drop it… I think it might be okay to just add."* (§6.5)
- **TWO NEW THREADS OPENED AND WERE IMMEDIATELY DEFERRED TO NEXT WEEK.** **(1)** Wendy: *"I feel like we're **missing a couple fields in the Workbench that would allow us to identify our bound business and our in-force business**. And we need those things for various different reasons."* **In-force / bound / not-bound status has never appeared in this series** — a grep of all 28 prior notes returns nothing — and `FUNCTIONAL_REQUIREMENTS.md` has no such field. **(2)** Group-to-submission sync, above. → **O29-12**, **O29-10**. (§7)
- **STATUS, THE STAGING DDL AND DEPLOYMENT: FOURTH CONSECUTIVE NOTE. ⚑⚑** Not raised on 9/4, 9/10, or today. **What today adds is that the target is still empty:** Ben, in the middle of the demo, *"I am not planning on using the Moody's data… I'm counting on you to populate it in the SQL Server that we're connecting to, right, **which doesn't have any of these tables right now, which we called out this morning**."* **A morning call on 9/11 exists and has no note; the deployment-team call immediately after this session has no note either.** `23` **D2**'s exit condition depends on the DDL; `25` closed the approval and nothing has been built against it. → **O29-13**. (§9)

**The shape of the reconciliation this session forces.** `28` said the problem was that everything agreed was *unwritable* into the repo. Today's problem is different and in some ways worse: **the repo now disagrees with itself across three recorded decisions, and two of them were verified wrong by running the code.** `23` **O23-10** states a join key that is wrong. `23` §6 states `database` is not populated and data vintage is not a submission concern; both reversed today by the people who agreed them. `28` **D10** and `21` **O21-4(b)**'s answer — *push-time transform, no new entity* — were correct given what was known and are now superseded by a platform behaviour nobody had observed until Cheryl tested it. **None of this is drift from carelessness; it is what happens when a design thread runs seven sessions ahead of its documentation.** The measurements are unchanged from `28` and one worse: `TY` still appears nowhere in FR, DATA_MODEL or PRD; **there is still no manifest table and no staging table anywhere in `DATA_MODEL.md`** (grep returns only §12's heading and an unrelated `directory_path` note) even though `25` approved them nine days ago; the loss-repo `data` table's columns exist only in `23` §6; the client table exists in no document or code at all; **`PCS` and `stochastic` return zero hits across FR, DATA_MODEL, PRD, specs and `app/`**; and the engine-vs-model distinction that broke the demo is not merely undocumented but **actively collapsed in code** — `analysis_service.py:386`, `engine_version=_text(_first(p, "engineVersion", "modelVersion"))`, with no `model_version` field on `AnalysisSettings` at all, while `grouping_service.py:315` keys partitions on `model_version` and means the other thing. **The sequencing that follows:** fix the lookup join and **assert** the version equality rather than documenting it (**O29-5**), since this is the second occurrence of the same defect and `23` O23-5 already asked for the assertion; **take delivery of CIC's lookup table** while the offer is live (**O29-5**); **write the TY trade-off document** before the manifest is touched, because the two paths put the grain in different places (**O29-6**, **O29-7**); **check FR-017 against import-by-CRM-ID** before the low-effort estimate is spent, and bring group-to-submission sync into the same question (**O29-10**); **re-rank `27` O27-9** now that search is the tagging plan's replacement rather than a backlog item (**O29-11**); and **raise the DDL, deployment and status**, which have now missed four consecutive sessions and two undocumented calls on the same day as this one (**O29-13**).

---

## 1. The gap, again — and it got worse on the same day

`28` §1 established that **September 8 and 9 are undocumented**. That stands. Today adds two more gaps, both on 9/11 itself:

**A morning call.** Ben, mid-demo, referring to the target SQL Server: *"which doesn't have any of these tables right now, **which we called out this morning**."* There was a conversation earlier on 9/11 in which the missing loss-repository tables were raised. No note exists.

**The deployment-team call.** The session ended at ~87 minutes because Ben, Anil and Wendy had a deployment-team meeting immediately following — Ben, at 1:09: *"we have 20 minutes till we're going to get together with the deployment team."* **This is the first mention of a deployment team meeting since `26` recorded a deployment booked for Friday 9/4 afternoon, which was never reported on.** No note exists for it either.

**So the deployment and DDL threads are not dormant — they are being worked in calls that produce no record.** `28` **O28-14** already observed that *"Ben's other outstanding items are being tracked by email (9/9) rather than in these notes, which is where they become invisible."* Today the same is true of meetings. → **O29-13**.

---

## 2. The export, demoed (D1, D2, D3, D11, D12, D13, D14)

This is the first time the export exists as something to look at, so the walkthrough is worth recording in order.

### 2.1 The path

From a submission carrying EDMs, RDMs, broker results, his own analyses and a group, Ben selected two analyses run that morning and opened a **full-page export form**. Its parts:

- **A cart-style selection panel** — the `27` **D4** pattern reused a third time, after grouping. Deselect and the cart empties; add and it fills.
- **Name search across both broker and executed analyses** — Ben was explicit about the scope: *"the only attribute I'm searching on is just the analysis name."*
- **A perspective picker.**
- **A per-analysis data name**, editable per row, optional.
- **A client dropdown** reading the client table.
- **Treaty inception date and CRM ID**, auto-populated from the submission, both editable.
- **Data vintage**, unset.

Submitting lands on a **summary screen** — which analyses, perspective, client, metadata — and then fills in per-analysis results: **data ID**, model and engine version, staged and loaded counts, **stochastic and historical event counts**, and counters for the two auto-corrections agreed earlier in the series (**exposure raised to loss**, **negative standard deviation zeroed** — `24` D-series). Ben: *"I've not run into either of these scenarios yet."*

Runtime: *"It actually is really quick."*

### 2.2 The status views, and Ben's own dissatisfaction

Ben opened the critique himself: *"even after we've exported and we've created a new export, **this table doesn't really tell me a whole lot**. I was kind of struggling on how to display this at the submission level."* The collapsed row carries perspective, requestor, date, client, analysis count and status — and, as he pointed out, *"we have no clue what analyses we exported for this client and this perspective without expanding this row."*

Cheryl's first instinct was volume control — *"imagine if I had 15 failures, I was trying different things… once I see that it failed, I did whatever I needed to do to correct it. **I don't feel like I have to hold on to that information**"* — and her second was that the summary is never enough: *"it doesn't give me enough information to just have just that summary section… so I'm always going to have to expand it. So to me, it feels like just starting with the expanded information probably makes sense."*

Wendy sharpened it into the rule that stuck: **you only need the detail when something failed.** *"if everything succeeded, I actually don't need to see the detail. But if something failed… every single time you would have to click in to figure out how to go to fix it."*

**This is `18` D4/D5 arriving on a second surface.** That session settled the analyses grid on failures-grouped-to-the-top with filter-not-search, driven by Wendy's *"I just want to see three failed."* The export status table has the same job and the same user.

### 2.3 The `data` table, field by field — `23` §6 checked against reality

Ben walked the resulting rows in the loss-repository data table. **Every field in `23` §6's mapping was visible.** The reconciliation:

| Field | `23` decision | Today | Status |
|---|---|---|---|
| `client ID` | Search CIC's client table by name, write the ID; **read-only** (`22` D25) | Reading the client table, searching by name | **CONFIRMED** — but see D1 below |
| `treaty incept` | Auto-populate from the submission; editable | Auto-populated from the submission; editable | **CONFIRMED** |
| `data vintage` | **Leave blank**; analyst enters it; *"not captured on the submission"* | **Required**, no default; and Cheryl now thinks it may belong on the submission | **CHANGED (D2)** → **O29-4** |
| `data name` | Settable per analysis, editable afterwards, **never required** (`23` D12) | Optional, per-analysis, editable in the workflow tool later | **CONFIRMED (D3)** — closes `23` **O23-7** |
| `model vendor` | Auto: `RMS` | `RMS` on every row | **CONFIRMED** |
| `model version` | Auto: `25.0` decimal | 25 on every row; *"That's expected"* | **CONFIRMED** (decimal form not visible on screen) |
| `server` | **Repurpose** — the **tenant** base URL | The **API** URL; Ben offered to change it | **DRIFTED** → **O29-4** |
| `database` | **Not populated** | **The Workbench database name** | **REVERSED (D11)** → **O29-4** |
| `analysis ID` | The **user-facing** Risk Modeler ID (`23` D17) | The user-facing readable ID | **CONFIRMED** |
| `name` / `description` | From the analysis | Analysis name and description; broker results arrive with no description | **CONFIRMED (D12)** |
| `archive file`, `area`, `LOB`, `geography` | Not populated | Not populated | **CONFIRMED** |
| `CRM ID` | From the submission; editable | Populated | **CONFIRMED** |

Cheryl's verdict on the set: ***"You got it all."***

**D1 — the client dropdown stops filtering on the active flag.** Ben's form queries the client table using an active flag *"or the equivalent of whatever exists in the client table."* Wendy caught it immediately: *"I think you want to remove that requirement because **we often price business that's not a current client, but we still have a client ID for it**."*

**This is worth recording precisely because the repo has nothing to record it against.** The client table appears in **no** FR, DATA_MODEL or PRD row and in no code — `22` **O22-9(d)** left open *"whether the client search is scoped/filtered or a raw name search, which decides whether the export screen can pre-select."* **Today answers it: raw name search, no active filter.** → **O29-4**.

**D2 — data vintage becomes required.** Cheryl: *"Should not be optional. **That has to be filled in.**"* It gets no default — the treaty inception date is a different value, as `23` established — and Ben confirmed the analyst would know what to enter. His restatement: *"Data vintage should never be empty."*

**But then Cheryl questioned the premise `23` recorded:** *"Don't we enter that on the submission page? Or we don't enter that on the submission page? **I thought we did.**"* Ben: the submission has the treaty inception date, not data vintage. **`23` §6's note reads *"Cheryl confirmed it is not captured on the submission."* She is now unsure, and by the end of the call Wendy had put data vintage alongside expiration date on the list of missing submission fields (§7).** → **O29-4**, **O29-12**.

**D3 — data name stays optional, and this closes `23` O23-7.** Note the symmetry: the two optional fields went opposite ways in the same five minutes, and the reason is where they can be fixed. Cheryl: *"that's fine because as you mentioned, Wendy, **we have to go into workflow and we can edit it there if we need to**."* Wendy: *"I like being able to put it in here… But I don't think it should be required."* **`23` D12's *"never required"* survives contact with the built screen.** Note the dependency it rests on, which is the same one that killed the Explorer in §3: the field is optional *because* CIC will be in their own tool anyway.

**D11 — `database` records Workbench provenance.** Cheryl's opening position matched `23`: exporting from Risk Modeler, *"there wouldn't be a database that goes with it. We could just put Risk Modeler if you want to have that as a default."* Wendy wanted more:

> *"I think there's maybe some value in saying it either came directly from Risk Modeler or came from the Workbench… we've had instances in the past, Cheryl and I were talking about this yesterday, where it's like **we found a problem in a piece of code, a piece of the query, and it's like everything up until that date was wrong** — and so that's why it's helpful to be like, everything that has the Workbench label on it came through the Workbench, and if we identify a problem later, which I'm sure we won't, **we can query for that and find them**."*

Resolution: Ben writes the **Workbench database name** into the column. Cheryl's caveat, which the requirement has to survive: direct broker uploads may bypass the Workbench and would populate the field the old way — *"if they're direct uploads from the broker, we might not even do it in Workbench anyway."*

**This is a blast-radius requirement on a production table the Workbench writes to, and it is the second time this series has produced one** (`21` D-series established the traceability need from the other direction). **It belongs in FR §7 with the reason attached, not as a field default.** → **O29-4**.

**D12 — `server` drifted and nobody minded.** `23` repurposed it to the Risk Modeler **tenant** base URL, with Cheryl's *"I think that'd be great."* The build has the **API** URL — `api-use1…` rather than `cinfin.rms.com` — and Ben offered to change it. Cheryl waved it off: *"It sort of doesn't matter. This field and the next one become less relevant because **we're not working in a SQL environment anymore**."* **Record which one it is, because a recorded decision quietly became a different thing and the reason given ("it doesn't matter") is not the reason `23` gave for choosing it.** → **O29-4**.

### 2.4 Duplicate exports (D13, D14)

Ben hit his own block mid-demo: having already exported the GU perspective for an analysis, re-exporting was refused. *"Is that expected?"*

Cheryl's answer describes normal practice:

> *"Today we don't have anything that stops us from doing that. And the reason is **sometimes you run something and you're like, oh shoot, I forgot to turn on blah, blah, blah flag. So I delete it and then I run it again and it's going to be given the exact same name**… So it's kind of an error issue in terms of having duplicates. So I don't know that that needs to be a stopper, but **you could maybe just have a flag that says, hey, you're doing it again**."*

And the remedy is a name: *"I could put a **V2**. That's common for us to do."*

Ben: *"I'll drop it then. And allow duplicates, but warn. Throw a warning rather than a hard error at this stage."*

**The new repo fact.** Anil asked what the staging-level check keys on. Ben: *"Neither. It's based on **analysis ID and perspective**."* Anil's response is the design instruction:

> *"We may just need to revisit that, so we allow an intentional export of a duplicate with a different name. I know it's not going to be 100% accurate because you have to give one way or the other. **You allow an accidental but intentional-looking duplicate, or you stop that and prevent a genuine duplicate requirement.** So I think we can relax that."*

And later, plainly: *"I was trying to think of a way to safely check for duplicates. **There isn't one.** It could end up becoming too restrictive and then frustrating at that point."*

**Reconciliation.** A grep of note `23` for `duplicate`, `dupe`, `re-export` and `idempot` returns **nothing** — the subject has no prior coverage in the series. The only idempotency statement in the repo is `DATA_MODEL.md:616`, an upsert on `(rdm_id, analysis_name, perspective_code)` that governs **retrieval into `analysis_result_meta`**, not the push. **So the validation Ben is relaxing exists only in code and the decision to relax it exists only here.** Note also that it interacts with **D2**: if the analyst re-exports and renames to V2, the data name is doing duplicate-disambiguation work for a field that is explicitly **not required** (D3). → **O29-3**.

---

## 3. The Loss Repository Explorer, proposed and deferred (D4) — and the Analyze Re dependency

### 3.1 What was proposed and why it died

Anil's proposal, after watching Cheryl click through to the detail page:

> *"Would it be useful to create its own dedicated page for **Results Explorer**? What does that mean? **It's not going against our Risk Workbench database. It's going after the loss repository.** You're going there to see what succeeded. And when we click on that link, instead of opening a custom page for that particular export, it is opening that Explorer page with filter applied — for that submission, that perspective… you can go directly to this page without navigating through submission."*

**Cheryl wanted it**, because it mirrors what she depends on today: *"That's kind of how it works today… it's basically just a **live view into that data table**… here's what you've loaded, here are the data sets that you have, and there's some editing capabilities… **Personally, I'm pretty dependent on having that to be able to know what I have.**"*

**Wendy killed it, and the reason is a dependency:**

> *"We have to use our workflow tool in order to **push results to our simulation engine, Analyze Re**, because that is not within scope of this project. **And so I have to go there anyway.** So I just, I don't know that taking the time to put an editable window in here when I have to go there anyway is worth [it] — again, future, maybe we get rid of that functionality over there and bring it in here."*

Cheryl conceded in one line — *"Solid point"* — and then again, fully: *"I wasn't thinking about that, but you're right. **We will have to be in that tool anyway** so that we still have all our grid capabilities and all that kind of stuff."*

Anil parked it as a future standalone sub-application: *"a **loss repository explorer**. You can go and see what is out there, you can change things out there, and you can send things to another workflow from there."* Wendy: *"**Yes. Yep, that's the vision. Probably just not by October 1st.**"*

Anil's framing of the outcome is worth keeping because it is the right instinct and the series has needed it: *"**I'm so happy that you're thinking about doing less with the time that we've got.** It certainly helps to not build things that we don't absolutely need."*

### 3.2 The replacement design — failure triage (D5, D6, D7)

What survives is a screen optimised for finding and clearing failures:

- **Quick filters — all / failed / loaded.** Anil's spec is precise: *"failed would show at least everything that has got at least one failure. And loaded will show only those that have at least one loaded."* Note the multi-analysis row can be both.
- **A Close / Resolved action.** Wendy: *"I uploaded 6 and six successfully went. I'm done… to just be like, oh, one failed. Okay, I went, fixed whatever I need to be fixed, re-upload. **I just want to click it and have it go off my list.** So if I come back a week later — I'm like, oh yeah, that's right. There's still a failure. **I just want those to go away.**"* Anil's version: *"add… a flag, a separate state of saying that closed, right? Instead of failed, **mark it as closed**, so people don't waste their time revisiting."* Wendy was explicit it is a manual status, not a workflow: *"I'm not suggesting Ben has to have a path that says I somehow click on the field and it takes me back to step one."*
- **Layout.** Anil: failed/loaded status *"a little bit to the left, aligned with the progress labels,"* data ID *"aligned with the client,"* and a close link beside the existing open link.
- **AAL by peril added; zip file path removed (D6).** Cheryl's ask, from what her current tool gives her: *"having the new data ID, here's the name you gave it, **here's the average annual loss by peril** — that information, that's kind of helpful. **If I did everything right, I don't even need to go on to the next screen.**"* Ben agreed for both views, and to drop the downloaded-zip path, which *"makes this table hard to read."*
- **Retry stays; no click-through to the analysis (D7).** Ben: *"We can retry from here, which will retry from the last spot."* Wendy asked about jumping back to the analysis and withdrew it herself on remembering a row can cover a group or several analyses.

**The reconciliation, and it is a three-part finding.**

**(a) `18` D4/D5/D6 designed this lifecycle once already, on the analyses grid**: failures grouped to the top rather than sorted, filter by status, and multi-select delete for hygiene (Cheryl then: *"these 10 jobs failed and I expected them… **I don't need to have them in my list**"*). **Today's Wendy is asking for the same thing in the same words on a different screen.** Whatever is built should be one pattern, not two.

**(b) "Resolve / close / dismiss" is genuinely new.** A grep of all 28 prior design notes for a dismissal concept returns nothing — the three `dismiss` hits are all "raised and dismissed" in the argumentative sense. **The designed failure lifecycle to date is: failures on top → delete.** A soft *closed* state is a different thing from a delete, and it needs a home: the export status rows are Workbench state, but `25` **O25-1** has not decided whether the manifest they hang off lives in `WORKBENCH` or `LOSS`. **A user-set flag is a strong argument for `WORKBENCH`, which is the side O25-1 already leaned toward.** → **O29-2**, **O29-7**.

**(c) `18` O18-6 — legible failure reasons — is open since 8/12 and this is its third raising.** `14` **D13** established it (*"failures must be surfaced with a reason"*), `18` §5.2 raised it again, and `23` line 365 listed it as unchanged. **Today's export detail screen actually shows a reason** — the model-version validation error was legible enough for Ben to diagnose it live — **so the export surface may deliver what the analyses grid still does not.** Worth checking whether the mechanism generalises rather than building it twice. → **O29-2**.

### 3.3 The Analyze Re dependency is undocumented and now load-bearing

This is the finding the section exists for.

**`Analyze Re` appears in ZERO of the 28 prior design notes.** It appears in the repo exactly twice, both as an MVP exclusion:

- `FUNCTIONAL_REQUIREMENTS.md:601` — *"**Out of scope for MVP:** … carrying CRM ID tags through to the repository upload (Future); **uploading loss sets to Analyze Re (separate API)**."*
- `PRD.md:1132` — *"out of MVP: **uploading loss sets to Analyze Re, which is a separate API**."*

**And `simulation engine` returns zero hits across all 28 notes and all four doc files** — Wendy's *"our simulation engine, Analyze Re"* is the first time the thing has been named in a design conversation.

**Why this matters.** That one-line exclusion now explains: why the Explorer is deferred (§3.1), why `data name` can stay optional (§2.3 D3), why the export status screen only needs failure triage rather than a results browser (§3.2), and — from `28`'s side — why CIC tolerates workflow differences in the Workbench at all. **Four scope decisions rest on an out-of-scope line whose consequence for the user has never been written down: after every export, the analyst leaves the Workbench.** Wendy said it plainly and nobody wrote it down: *"I will then go over to our workflow tool, enter their parameters and upload to our simulation engine."*

**Two things follow.** First, **the exclusion should be restated as a workflow fact in FR §7**, not just an MVP boundary — an implementer reading line 601 today learns what is not built, not that the product's happy path ends in another application. Second, **Wendy's "future" is a real roadmap item with an owner's enthusiasm behind it** — *"I would love at the future to bring these tools together… so to stop having to go back and forth"* — and `PRD.md` §20.4's unscoped list is where it belongs. → **O29-1**.

**And record the date.** *"Probably just not by October 1st"* is the **first delivery date named in the series**. `23` **D1** put status at yellow on 9/1 and no note since has restated it. A date said in passing by the client is not a commitment, but it is the only one there is. → **O29-14**.

---

## 4. The failed export — `23` D19's defect again, and `23` O23-10 is wrong (D8, D9, D10)

### 4.1 What failed and why

The demo's one failure was a validation Ben wrote and immediately disowned:

> *"This error is, I think, what would be considered an **unnecessary validation** of this model version 23, where we're checking to almost make sure if it's a valid model version, **which is not the right place to do it. This is a totally invalid error.** We're checking the historical lookup table to see if this model version is represented in the historical events, **which it doesn't have to be**."*

Cheryl confirmed the premise is false: *"**it would never not be historical data because of a mismatch of this model version.** What happens is the models may not be updated in RiskLink 23. So the most recent peril model might be 22 or 11, whatever, **but that has nothing to do with the engine version**, the actual software version that we're working with."*

Wendy's one-line diagnosis, which is the whole section: *"So that's **model version versus engine version**. So that's the nuance here."*

**D8: the validation is removed.**

### 4.2 The correction — and it overturns `23` O23-10

Wendy then explained how CIC's lookup table actually works, and it is not what `23` recorded:

> *"What we track in our lookup table, **even though it says model version**, but **it's engine version. So it's the version of the software**… because we move from version to version through our process, and those historical events change over time, we build that table out for every version update… So in our table you will see version 25, version 24, version 23, version 22, because every year there were releases, **even if the models didn't change**. So because we have to have a catalog, we have to have a version associated with our results, **it has to be the same version**. So **every analysis I run, every peril, every country, everything has to say version 25 on it**. I can't [mix versions] — it doesn't work in my catalog. So in my simulation engine, they all have to have the same version, which is today, version 25."*

Ben then found the bug on his own screen: *"I'm definitely joining on the wrong attribute. I'm using this attribute [the analysis's **model data version**, 18.1] to join to the lookup table."* Cheryl: *"Correct, we want the **engine version** there."*

**D9: the join is on engine version.** And Cheryl's warning against the quick fix is the useful half:

> *"So again, though, **you're going to have to undo that when we get our lookup table**, because we don't maintain that data model version. We maintain this version, what you have here, which is actually what RMS is referring to as the **engine version**. That's what we maintain in our lookup table. So you can do that for testing, but just know you're going to have to undo it."*

Ben chose to mirror rather than patch: *"then I won't change that join. Rather, **I will mimic the way you set it up**."*

**⚑⚑ `23` O23-10 states the key incorrectly, three times.** Its text: *"it is **keyed on event ID / peril / data model version**."* The same claim at `23` line 225 (*"most of it comes from the lookup table based on the **event ID, the peril, and the data model version**"* — Cheryl, *"ninety-nine percent sure"*, with *"we should verify with Cheng"*) and at line 379 (O23-8). **The verification happened today, by accident, and the answer is the engine version.** Cheryl — the source of the ninety-nine-percent claim — is the one who corrected it. **Amend O23-10 and O23-8 rather than leaving a wrong key in an open item that an implementer will read as settled.** → **O29-5**.

**⚑ And this is `23` D19's defect in a second place.** D19: *"The ELT table's `model version` takes the data table's `data model version`"* — Ben having caught an event-rate-scheme model version code of **17** against a DLM version of **25**, with *"I don't think we want a mismatch."* `23` **O23-5**'s instruction was explicit: *"since Cheryl says the enrichment lookup is keyed on event ID + peril + data model version, a mismatch would mis-join silently. **Assert it, don't document it.**"* **It was not asserted. Six weeks later the same confusion mis-joined a different table and failed an export live in front of the client.** Note the irony: the assertion O23-5 asked for would have been written against the *wrong* key, so writing it then would have encoded the error. **Write it now, against the engine version, as a test.** → **O29-5**.

**The code makes this worse than a documentation gap.** `app/services/analysis_service.py:386` collapses the two concepts into one field:

```python
engine_version=_text(_first(p, "engineVersion", "modelVersion")),
```

`AnalysisSettings` has `engine_type` and `engine_version` and **no `model_version` field at all**; `analysis_jobs.py:350–351` stamps only `engineType` / `engineVersion` into `loss_results`, so `specs/011-analysis-results/spec.md:127`'s FR-021 — *"Every stored result records **the engine and model version**"* — is implemented as engine version only, with `modelVersion` as a display fallback. Meanwhile `grouping_service.py:315` keys partitions on `model_version` and means the genuine model version. **Two concepts, one field on results, a different field in grouping, and `FUNCTIONAL_REQUIREMENTS.md:562`'s *"engine / model version"* treating them as one hyphenated thing.** `PRD.md:1189` already flags a *"Known gap: the per-analysis software/model version (design note 18 O18-10)."* **`27` §11 is the only place in the repo where model version, software version and rate version are named as three things; it is a design note, not a requirement.** → **O29-5**.

### 4.3 The lookup table handover (D10) — a cheaper path than O23-10 asked for

Anil made the move that turns a bug fix into a dependency being closed:

> *"Question — if that metadata is not considered proprietary and confidential, **could we just use that exact data in your test environment, Ben?** Instead of guessing what that might be and potentially going wrong… **if we take that route, then we know for sure that what we're building is just going to work.**"*

Cheryl on sensitivity: *"**Nothing more than what you've pulled from the software today.**"* Wendy on logistics: *"there's only like **2,500 rows** in that table in our lookup table, so we could literally export it and send it to a CSV file… or if you want a **.bak** file, we could do that too."* Anil on shape: *"We also need to know what we call those fields on that table. So it will be on the header."* Cheryl: *"Yeah, **we'll keep the headers**."*

Ben confirmed he was never going to use the Moody's data: *"**I am not planning on using the Moody's data that I get from here.** I'm counting on you to populate it in the SQL Server that we're connecting to, right, which doesn't have any of these tables right now, which we called out this morning."*

**What this retires.** `23` **O23-10** asked for two things: *"the **DDL**, if you can get Ross to produce that,"* plus *"ask Cheng **how he uses it**, so I can basically use it the same way."* **Neither arrived in ten days. A CSV with headers delivers the DDL half outright and most of the usage half implicitly, because the join column is now known (§4.2) and the enrichment shape is visible in the data.** What it does *not* deliver is Cheng's normalisation logic, which `23` D25 recorded applies to historical events only. → **O29-5**.

**And it confirms `23` D22 from the other side.** D22 recorded PCS as *"a lookup"* and **not required**, on Wendy's *"we don't have PCS numbers for everything."* Today Ben notes the handover *"would give us some PCS values too."* **`PCS` still returns zero hits across FR, DATA_MODEL, PRD, `specs/` and `app/`.** → **O29-5**.

Ben's reaction to the size is a small but real finding: *"I'm glad you said that one because I was like, oh wow, there's only 2,500, 2,700 historical events. **I was kind of concerned about that**, so it's not a big table."* `23` **O23-10** left the volume unstated; it is small enough that the "where does it live" question (`21` O21-6's fifth-connection worry, which `23` resolved to the `LOSS` server) carries no weight either way.

---

## 5. TY reverses to group-first (D15–D22)

### 5.1 The test, and what it found — `28` O28-9 ANSWERED

`28` **O28-9** was explicit that the design rested on an unobserved behaviour:

> *"Everything in §5 was demonstrated either within one export of one analysis carrying two treaties, or in CIC's workflow tool. **The Moody's-side cross-analysis case is asserted, not observed** — and it is the exact behaviour O28-2 is specifying."*

Cheryl ran it that evening and messaged Ben. He read it into the call:

> *"Let's come to, Cheryl, your message from yesterday about TY perspective. **Three analyses exported the loss tables all at the same time. Gross loss and treaty output level. [Risk Modeler] created three separate zip files, one for each analysis.** Makes sense. **This confirms that we need to create a group first and then export to the group to get one set of output.**"*

**The answer is negative, and it invalidates the premise of `28` §5.** Risk Modeler does not perform the cross-analysis treaty aggregation that CIC's workflow tool performs. **`28` D6's behaviour is real — but it is the workflow tool's behaviour, not the platform's.** Cheryl demonstrated it on 9/10 *in the workflow tool*, which is precisely why O28-9 was raised.

**Note what did NOT change.** `28` **D6** remains an accurate description of what CIC's analysts experience today, and therefore of the workflow difference D18 has to manage. **What changed is only who performs the aggregation.**

### 5.2 The three options, and Anil's argument (D16)

Ben laid them out:

> *"We can either allow you to do multiple TY exports at the same time for multiple analyses and **do that aggregation under the hood**… Or we can **force you to create an analysis group first**, which would result in us only exporting one analysis, the group. Or we can **implicitly create that analysis group** before exporting this data file… But I think the result is the same."*

Cheryl accepted that with a condition: *"Should be. **If we do the calculations correctly**, yes, it should be."*

**Anil's case for grouping first is the strongest architecture argument in the thread, and it is worth quoting at length because it describes the cost of the alternative precisely:**

> *"Let's say we do not do this grouping ahead of time, and we select at the time of export, these are the four analyses that I want to group and export together with TY perspective. I select those four, export becomes one export row that will expand to four sub rows… **Three of them could succeed and one of them could fail, but collectively they failed and we would not be able to aggregate them** and hence that entire export for treaty would be considered as failed. This would mean that, **in addition to the manifest, we need to have a treaty grouping concept introduced on our manifest**… We do that and we say we expect 4 files and it's only when the manifest says all four are completed that we move the data, perform the aggregation, and load the data tables on loss repository. If we don't get that, then **it times out after a while** and says, this cannot be… we're going to give up on that."*

And the payoff:

> *"So we don't have to aggregate across those two. We get rid of all of the lifting that we had to do on the export side by doing this extra work on the grouping side… **It's already aggregated on Moody's. It seems to solve two problems at the same time.**"*

Ben: *"**It is easiest to group first. I agree.**"*

**This directly supersedes 9/10 D10** (*"Do not create a Risk Modeler analysis group for the TY path — mimic the workflow tool and aggregate on export"*) **and re-opens `21` O21-4(b)'s answer**, which `28` **O28-2** recorded as *"the aggregation is a push-time transform, not a new entity — and Anil and Ben arrived at it independently."* **They did; the platform disagreed.**

**Note what Anil's argument is really about: partial-failure semantics.** `23` line 99 established the principle — *"an export batch may name several analyses (D9 writes one `data` row per analysis), and **a partial failure must be recoverable per analysis rather than per batch**"* — and `23` **O23-2(c)** left the recovery semantics open. **A TY aggregation makes per-analysis recovery impossible by construction: the unit of correctness is the whole set.** `25` addresses failure semantics nowhere at all (a grep of that note for `retry` / `partial` / `recover` returns two hits, neither about export). **So the group-first decision is, underneath, the first concrete answer to `23` O23-2(c) — by removing the case.** → **O29-6**.

### 5.3 CIC accepts the workflow change (D17, D18)

Anil asked directly whether it would be disruptive. Cheryl:

> *"I don't think so. That was kind of what I had said — I feel like **that grouping process is not as difficult in Risk Modeler theoretically**. You know, I don't have tons of giant data sets to lean on like I do in RiskLink, but **I'm going to go a little bit on faith that grouping continues to be not painful**. And so I'm completely okay with having to do that step first and then exporting."*

Wendy: *"**Grouping is a huge pain point in RiskLink that in theory does not exist in Risk Modeler to the same extent.**"*

**Anil named the causation, which is the sentence that explains the whole workflow-tool design:** *"that is **because of the pain that exists in the grouping**. So we chose to take path B."*

**Two caveats to carry.** Cheryl's acceptance is explicitly on faith — she has not grouped at volume in Risk Modeler. And `26` verified grouping against Risk Modeler at small scale only. **If Risk Modeler grouping turns out to be slow or awkward at CIC's real volumes, this decision inverts, which is exactly why D19 keeps both paths open.**

**D18 — the behavioural difference is judged acceptable.** Ben flagged it as the genuine risk:

> *"We come to the export form here and we choose two analyses and we want to export both of these as TY. **In the workflow tool now, that aggregates across both analyses, looks for matching treaties across both. We're kind of proposing to not do that**… which is not how the workflow tool operates today, **which could be confusing, unless that's very clear and people understand that**."*

Cheryl's answer inverts the risk:

> *"I don't think that that will be particularly confusing. I think people will get on board with it quick and truthfully, **the interface looks completely different. So there isn't this ability to select your treaties.** So if it looked exactly the same as workflow and had different results, that would be confusing. **This looks different.** So I think it is a very simple conversation to have with the team."*

Ben: *"That is truly the hardest part in my opinion. So if that's fine, then that's fine."*

**Record the mitigation as a design constraint, not a reassurance.** Cheryl's argument is that the export screen's *dissimilarity* to the workflow tool is what prevents confusion. **That is an argument against ever making the export screen resemble the workflow tool** — which matters, because `21` and `23` repeatedly used the workflow tool as the model to copy. Wendy added the practical self-correction: an analyst picking two analyses with three treaties each *"they're going to go in and see six different data IDs… so they'll be like, oh, yeah, that's right."*

**D23 — and a line of text on the form.** Wendy: *"maybe, Ben, the way to handle it is **when you pick TY, something could pop up and say, you'll get one loss set per treaty, per analysis**. Like just as text."* Ben: *"Yep, we can add some informative text. Totally."* Anil offered context-sensitive help panels he had wanted to build; Wendy declined: *"I don't think it's necessary, honestly. But I appreciate the thought."*

### 5.4 Both options stay open (D19)

Anil, immediately after Cheryl pointed out that grouping does not solve bursting:

> *"Okay, so then **preserve both the options**. It comes down to whether we trust Moody's to do the grouping right on Risk Modeler versus RiskLink, or **do we trust ourselves more to be able to do that without messing anything up**. So preserve the options. **Let's pick one, tread that path, and if it doesn't work**, we can do it [the other way]."*

Ben: *"there's a couple options. **Let's document them, the trade-offs, decide what we want to do after doing that.**"*

**So D16 is a working direction with an explicit documentation action attached.** The trade-off document is the deliverable, and it is not optional: **the two paths put the aggregation in different systems and therefore put the manifest's grain in different places** (§5.6). Writing the worker before the document means picking one by accident. → **O29-6**.

### 5.5 The burst requirement is untouched (D20)

When the group converged on grouping, Anil asked whether it collapsed the multi-file problem entirely. **Both CIC participants said no, in the same breath:**

> Cheryl: *"Yes, but **you still get multiple treaties**."*
> Wendy: *"**You still have two TYs that come out.**"*
> Cheryl again: *"You still get multiple treaties."*

And Cheryl's formal statement, after the group-first decision was made:

> *"It takes care of the aggregation issue. **It does not resolve the issue, Ben, of needing to explode this file into multiple files for each treaty layer that you see in the file, unlike at the portfolio level.**"*

**9/10 D5 stands verbatim: one export at output level = treaty returns one table containing every treaty, and it must be burst into one ELT per unique treaty.** `28` **O28-2**'s burst half is unaffected by today's reversal; only its aggregation half is.

### 5.6 The manifest must go treaty-grained (D21) — `25` O25-1's question, answered for one mode

Ben separated this from the grouping question twice, because Anil kept hearing it as the same thing:

> *"That's not what I'm foreseeing as the difficult part. It's rather **our manifest table, which currently defines the rows we need to write at the analysis level, which is not what TY perspective is. It's at the treaty level.**"*

And: *"I think the result is **the manifest table has to be amended to account for TY perspective. As it is right now, it will not handle it appropriately.**"* Also *"the staging tables"* — he named both.

Cheryl got it immediately: *"Oh, right, because we have multiple treaties."* Ben's earlier framing, when she corrected his aggregation key: *"I'm thinking from our **manifest table** perspective, which right now is at the analysis level, but **for treaty level, it needs to be at the treaty level**."*

**⚑⚑ This is `25` O25-1's grain question arriving with a partial answer.** O25-1 asked: *"What is the grain? §9 keys results on (analysis | rdm) × `perspective_code`. Ben's manifest is per-download-archive, and one archive returns one Parquet file per perspective — so one archive is several perspectives. **Fix the grain explicitly, because it decides whether the load ID in O25-2 is per-archive or per-perspective**, and it is `21` O21-7's unresolved `result_export` grain question arriving from the other end."*

**Today adds a third candidate that applies to exactly one mode.** The grain is:

- **per analysis × perspective** for GU / GR / RL / WX / QS — `23` **D9**, *"one `data` row per analysis"*
- **per treaty** for TY — one row per unique treaty in the grouped export

**Group-first reduces the work but does not remove it.** Anil walked the reduction himself with a two-groups-of-four example: two groups → two export jobs → two zip files → two manifest entries, *"and we don't have to aggregate across those two."* **But one grouped analysis still contains multiple treaties, so a single manifest entry still fans out to N data rows.** That is the part §9's key cannot express.

**And the repo has nothing to amend.** A grep of `DATA_MODEL.md` for `manifest` returns exactly one hit — §12's own heading, `## 12. Table manifest` at line 755 — and **§12's table list contains no staging or manifest row**. `staging` returns one unrelated hit (line 33, a submission's `directory_path`). **`25` approved the staging tables on September 2 and nothing has been written down since.** `23` **O23-2** said the same thing nine days ago: *"§9 has `analysis_result_meta`, `result_export` and `delivery_kind` and **no staging concept whatsoever**."* → **O29-7**.

### 5.7 The aggregation math, banked (D22)

Ben asked for the formulas in case the fallback path is taken. Cheryl:

- **Loss:** *"Sum of the loss."*
- **Standard deviation:** *"Thankfully we have them split between independent and correlated… **independent is additive**, right? And then **correlated, you have to take the square root of the sum of the squares**. It's just a formula."*
- **Exposure value — unresolved:** *"That really is [meant to be] additive. **That's kind of a tricky one**, isn't it, Wendy? Because in this case, it's **the max for that treaty**. So your exposure value is going to get kind of goofed up. **I don't know how Cheng handles that.** Because I don't think it's really additive either."*

Ben asked for Cheng's query; Cheryl and Wendy both agreed: *"We can look at his query."*

**Cheryl also corrected Ben's aggregation key mid-sentence** — he said "for each unique treaty" and she added: ***"You need treaty's event ID and rate."*** Which matches `28` **D6**'s *aggregated by event ID*.

**This is the third outstanding ask of Cheng.** `23` **O23-10**: *"ask Cheng how he uses it [the lookup table], so I can basically use it the same way"* (Cheryl: *"Can do"*) — ten days, no outcome. `24` **O24-11**: the reference table's owner and the 9/2 Cheng meeting — still no recorded outcome per `28` §12. And now the TY aggregation query. **`28` O28-12 made the same argument about Moody's questions: send one consolidated list. The same applies to Cheng.** → **O29-8**.

---

## 6. Tagging collapses (D24–D29)

### 6.1 The discovery

Ben opened intending to walk an Excel sheet entity by entity. Wendy pre-empted him:

> *"So one thing we found out — and of course I say 'we', I really mean Cheryl — is that **the tagging functionality in Risk Modeler to search by tags sucks.** It's **ors instead of ands**, right… so it's not continuing to reduce your list by multiple tags."*

Cheryl's worked example: *"let's say I have a database labeled as X and a database labeled as Y, and I have a database labeled as XY, and I say I want tag X and tag Y, **I get all three of those at one time**."*

Wendy: *"So I think **our whole sort of thinking around tagging has somewhat gone out the window.** There's still some things we believe should be tagged, but **it doesn't behave as elegantly as we had hoped**."*

**The consequences rippled through the candidate list Cheryl and Wendy had pruned between sessions:**

- **Treaty year and month cannot be separate tags.** Cheryl: *"at first we went down the path that we should do treaty year and then we could just do month. But… because it's an or, **when I move to the next treaty year, let's just say it's an April renewal, I will get April for every single year**. So if I don't put that together as a single tag with treaty year and month — **TY2605** — there's no way for me to narrow it down."*
- **The picker does not scale.** Cheryl: *"If you go into Risk Modeler right now and look, do the tag, just click on where it says select tags. It's a drop-down. **Can you imagine if there are 5,000 tags in here?**"* Wendy: *"You would never, ever do this."*
- **Anil's diagnosis:** *"So instead of tag as a label, **the tag as a key value pair** would have been a better way to implement this on the side… we have to come up with some convention to say how do we represent — how do I know it is a CRM ID unless I say CRM ID and then the ID?"*

**The candidate list, from Cheryl's chat post:** analyst name (from the submission owner, with reassignment), **treaty year+month combo**, **cedent name**, **reinsurance type**, **CRM ID**, and **expiration date** (which Cheryl noted *"is not something we capture today, but maybe we should add that as a field in the submission page"* — see §7).

### 6.2 The decision (D25) — and what it does to `27`

Ben, after watching the discussion for five minutes:

> *"Just the past 5 minutes, I'm thinking **I don't want to tag at all on Risk Modeler.** You can just use things that actually matter in the Workbench and get to the Risk Modeler entity that you care about. If you are really caring about looking at it in Risk Modeler, you can get to it. You can open it in Risk Modeler."*

Wendy would not go to zero but landed close:

> *"I am slightly hesitant to say do zero tagging, but I think we could just say, you know what, **we're just going to tag CRM ID. That's it.** And then at least we have a path there if we're like, wow, that's not enough tagging in the future, we're gonna add some more tagging — or to say that was good enough, **because CRM ID gets us to everything we need, like every link in every system**, right? So I don't think we need to go too deep on it. **But I would hate to say zero.**"*

Cheryl: *"Yeah, I think it's **a lot less than what we originally thought**."* Anil confirmed the cost is trivial: *"if you are tagging EDMs and RDMs, then it should be okay. EDMs and RDMs are not many in number. So when you add or remove a CRM, that's when we fire that API."*

**⚑⚑ What this does to `27`.** `27` **D22** was Cheryl's unprompted tagging design — `12` **O12-4**'s deferred session arriving 24 days late — and `27` **O27-8** called the reconciliation *"favourable except for one hole."* `27` **D23** sequenced three pieces with effort sizing:

| `27` D23 work | Effort | Status after today |
|---|---|---|
| **Import groups by CRM ID tag** | Low | **SURVIVES** — and is now the *only* tagging work |
| **Auto-tag everything Workbench creates** | Medium | **CANCELLED** except for CRM ID |
| **Submission-level sync (all entities by tag)** | Medium-to-high | **EFFECTIVELY CANCELLED** — the searching it enabled moves to the Workbench (§6.4) |

**And note the timing against CIC's own evidence.** Seven days ago Cheryl told the group:

> *"It has become really apparent to me in working through this product **how important this tagging is going to be for us**… anytime I have to go into Risk Modeler without that tagging, **it's going to be chaos in there**… we did some debrief with other companies who are already using this. It was one of the things that they said **they wish they would have tackled sooner rather than later, because it was a big deal**."*

**That is not contradicted by today — the peers' regret was about findability, and today relocates findability rather than abandoning it (§6.4).** But the relocation is load-bearing and the reversal needs to be written with its reason, or a future reader finds an enthusiastic design in `27` and a shrug in `29`. → **O29-9**.

### 6.3 ⚑⚑ The collision: CRM ID is the one tag that groups cannot carry

This is the finding of the section, and neither side of the call noticed it.

**What today decided:** tag **CRM ID**, on **EDMs, RDMs and analyses**. Anil asked the scope question directly — *"what objects are we thinking of applying the tag to, EDMs and RDMs and analyses?"* — Ben: *"Those are three options."* Cheryl: *"Yes."*

**What `27` D21/D23 depends on:** importing groups by CRM ID tag, which `27` **D24** made part of grouping v1's own definition of done. Ben's mechanism, from `27`: *"something that says import groups, pass me the analysis IDs from Risk Modeler, pull them in to the submission."* And the asymmetry he identified there: *"it's **the groups that don't live in the context of any EDM. They're independent, then they float around by themselves**… that's the only entity that I don't have syncing right now."*

**What the spec says.** `specs/012-grouping-execution/spec.md:124`, **FR-017**:

> *"Every Workbench-submitted individual analysis carries a submission-level IRP tag whose value is the submission name (O-05), applied at submit time and queryable in the platform and via API. **Groups carry no tag — the platform grouping job accepts none (O-07).**"*

And **O-07** at line 53, which is the harder half:

> *"Groups are submitted without the submission tag: **the platform grouping job schema has no tag field and no endpoint tags an analysis after creation.** Member analyses still carry the tag. Revisit if Moody's adds a tagging endpoint | **Approved** | research.md T-07 and Clarifications, decided 2026-08-27"*

**If no endpoint tags an analysis after creation, a group can never carry a CRM ID tag — at creation or later — and import-by-CRM-ID cannot work for the one entity type it exists to import.** Anil's reassurance that *"EDMs and RDMs are not many in number"* is about EDMs and RDMs; **a group is neither**.

**`28` O28-13 flagged this and today makes it acute**, because CRM ID has gone from one tag among six to the entire tagging surface. **If FR-017 is right, the whole surviving tagging plan does not reach its primary use case.**

**⚑ And Cheryl named the same wall from the other side, three minutes earlier, without connecting it:**

> *"I think the big challenge is **groups**, right? That's, Ben, you and I spent some time talking about — that is trying to **sync a group back to a submission**. We have to have something to hang on to to link that back to this submission."*

**That is `27` O27-7 restated, and "something to hang on to" is exactly the tag FR-017 forbids.** Two people described the same problem from opposite ends within three minutes of each other and neither made the connection. Ben's response was to move on — Wendy interrupted with the in-force/bound thread and the call ran out.

**Three things to check, in order.** **(1)** Verify FR-017/O-07 against the current Moody's API — it was decided 2026-08-27 and `27` D21 was 9/4, so the decision predates the group-import requirement. **(2)** If it holds, decide the alternative discovery key for groups before `27` D23's *low effort* estimate is spent. **(3)** Bring Cheryl's group-to-submission sync into the same question, because it is the same question. → **O29-10**.

### 6.4 Findability moves to Workbench search (D26, D27) — which promotes `27` O27-9

Wendy redirected the whole conversation with the reasoning that makes D25 acceptable:

> *"Kind of where we landed is sort of what you're talking about, but not exactly. But we were saying that **the searching is so much better in the Workbench** that if I was truly looking for something — I was looking for things that Cheryl worked on last year in July — **I'm going to find that quicker in the Workbench than I would find it in Risk Modeler**. So as long as I can find what I need here, and then I hit **go to it in Risk Modeler**… **people will continue to keep coming to the Workbench because that tagging thing over there is ridiculous. And the search is better here because it's more intuitive.**"*

Anil: *"we could put **real fields instead of just tags** — there is a cedent. Yep, **you can search by cedent. You don't have to run the tags.**"*

**D27 — Ben's scope statement:** *"we have all of this searching for submissions, but **this essentially needs to be extended to EDMs, RDMs, and analyses too**."* Wendy hesitated on individual analyses and then agreed: *"Am I searching really for an individual analysis, or am I searching for — I suppose I am in Risk Modeler. **You're right, I am.**"*

**⚑⚑ The repo says this does not exist, and the gap is precisely the one that just became load-bearing.**

- `FUNCTIONAL_REQUIREMENTS.md:118` — *"**Search, sort, and filter are available on every list section.** | **Partial** | 7/14: wanted on portfolios, treaties, analyses, and results — not just the submissions list. **Added 9/4 (D6):** the merged analyses grid carries click-to-sort Peril, Region, Engine, Currency and Submitted headers… Portfolios, treaties and results still have no sort, and **no list section but the submissions master list has search or filter**."*
- `FUNCTIONAL_REQUIREMENTS.md:117` — *"**Global search runs across Submissions, EDMs, RDMs, analyses, jobs, and results.** | **Not implemented**"* — with no notes cell at all.
- `PRD.md:1235` — *"**Deferred providers (not built this pass):** Portfolios, Treaties, **Analyses (own + broker runs)**, Jobs, and Results. **Analyses in particular has no unscoped list function yet** — every existing query is scoped to one EDM/RDM/submission — **and no single per-analysis detail URL** to land a result on."*
- Confirmed in code: `app/services/search_service.py` carries six providers — Pages, Submissions, EDMs, RDMs, Analysis Templates, Users. **No analyses provider.**

**`27` O27-9 has carried FR line 118 as an unadvanced item for two notes, described as *"not in spec 012's scope."* Today it stopped being a backlog nicety and became the replacement for a cancelled subsystem.** The order matters: **D25 is only safe if D27 ships.** If tagging is cut and Workbench search is not extended, CIC's analysts lose findability on both sides. **Re-rank it, and note that the analyses provider in particular needs a per-analysis detail URL that does not exist.** → **O29-11**.

**⚑ And the AND-semantics requirement CIC is implicitly relying on does not cover the new surface.** `FUNCTIONAL_REQUIREMENTS.md:116`:

> *"**Multi-term search uses AND semantics, not OR.** | Implemented | Added 8/4. 'There must be 1000 companies that have American in the name'… **Applies to the name search, the cedant filter, and the 'links to' picker**: each typed word must appear, matched as a substring and no fuzzier than that."*

Scoped by its own text to three surfaces. Ben's design is **per-purpose tag dropdowns ANDed together** — *"we can turn that into a query that works the way that we want it with an AND statement for tag values"* — which is a fourth. And line 114's *"Values OR within a filter and AND across filters"* is the submissions filter model, which may or may not be the right shape here. **Write the requirement; do not assume line 116 stretches.** → **O29-11**.

### 6.5 Tag mechanics, which survive the cut (D28, D29)

Anil proposed the safe-looking option: *"anytime we update one of the values on the submission, we fire a tag refresh on the back end where, **for safety's sake, I will wipe out all of the tags on all of the objects and then reapply the tags**. So there is nothing residual left out there."*

**Ben rejected deletion outright, and the reason is correct:**

> *"**I don't use delete tag, first of all. I use a get-or-create approach only.** If I'm going to apply a tag to some entity, I go and see if it exists. And what we should do for each of these attributes is **define a naming convention** — for example, analyst name needs to be like `AN-` the name. So when we want to look up an EDM by analyst name, we look up EDM has tag `AN-` the analyst name… **We shouldn't delete them because we could have that tag existing elsewhere.** If there's a submission year that applies to multiple submissions — which is definitely the case — if we're adding a new submission with that same year, or we're changing a submission that has that tag, **just leave it**. And if that same tag is going to be used in the future, the get-or-create approach finds it and applies it."*

He also scoped where the search belongs: *"essentially this screen, which is the Workbench EDMs, is the right place to build this. **We don't have to worry about the sync-from-Risk-Modeler screen** — all the EDMs that don't live in the Workbench — **because they don't have tags like this that we're describing.**"*

**D29 — ownership change.** Ben raised it: everything he imports and creates is tagged with his name, and reassigning the submission means the old tag comes off and the new goes on — *"Everything needs to get that old user tag dropped and the new user tag applied."* Cheryl questioned the drop:

> *"The question is, **do you have to drop the old tag?** I don't know. Maybe you don't. **Given that it's OR functionality, maybe you don't have to drop it. You just need to add it.** If I'm working on something and then Wendy picks it up, I might want to go back and find it. We might both want to have it in our list, because maybe we're having to ping-pong it back and forth… **I think it might be okay to just add.**"*

**Largely moot under D25** — analyst name is no longer a tag — **but the underlying question survives into `12` D7's cosmetic-ownership thread and into whatever the Workbench does with owner reassignment.**

**What to keep even at CRM-ID-only:** get-or-create, never delete, and a prefixed naming convention. All three cost nothing and prevent a class of bug. **What to fix while you are there:** `FUNCTIONAL_REQUIREMENTS.md:446` still marks the submission-name tag *"Not implemented"* against code that implements it at `analysis_execution_service.py:252–254` — flagged by `27` **O27-8**, still wrong, and now sitting in the middle of a section being rewritten anyway.

---

## 7. Two new threads, both deferred

**In-force / bound / not-bound business.** Wendy raised it in the last ninety seconds:

> *"We're going to run out of time here. I feel like we need to spend a little bit more time, because I think we need to get to this whole sort of **in-force, bound, not bound** sort of conversation, because as we were talking, Cheryl and I were like, **I feel like we're missing a couple fields in the Workbench that would allow us to identify our bound business and our in-force business.** And we need those things for various different reasons. And so maybe we can come back and talk about that next week."*

Anil: *"adding attributes and searchability on the Risk Workbench doesn't worry me."* Ben, asked directly: *"No, it doesn't."*

**⚑ This has never appeared in this series.** A grep of all 28 prior design notes for `in-force`, `in force`, `bound` in this sense returns nothing relevant, and `FUNCTIONAL_REQUIREMENTS.md` has no such field on the submission. **`21` D8 records an *in-force date* deliberately not populated on the export, which is a different thing.** The submission's status field (§4 of the data model) is a Workbench workflow status, not a business status. → **O29-12**.

**Expiration date and data vintage on the submission page.** Cheryl, during tagging: *"the **expiration date** came up too… it's not something we capture today, but maybe we should add that as a field in the submission page."* Wendy: *"**Along with data vintage.** Now, you said earlier, it's not there. Data vintage is not there… It's not important for tagging. **It's important for other reasons, maybe.**"*

**This is the same reopening as §2.3's D2**, arriving from the tagging side twenty minutes later. **`23` §6 recorded data vintage as confirmed *not* a submission field; two CIC participants now want it there.** → **O29-12**.

---

## 8. Repo reconciliation

| Piece | State after 9/11 |
|---|---|
| **The export itself** | **Built and demoed.** No spec folder covers it — `specs/` runs 001–013 plus two CRs, with **008 missing**, and none of them is export or tagging. Everything in §2 exists only in code and in these notes. |
| **`23` §6's `data` table mapping** | **Verified against the running export for the first time.** Nine fields confirmed, one confirmed-and-closed (`data name`, `23` O23-7), **one reversed** (`database`), **one changed** (`data vintage`), **one drifted** (`server`). Still absent from FR, DATA_MODEL and PRD — `data table`, `data vintage` and `data name` all return **zero hits** across the three. |
| **The client table** | **Still absent from every document and from all code.** Today adds a behavioural fact — **no active filter**, raw name search — which answers half of `22` **O22-9(d)**. The other half (where the selected client ID is recorded against the export) is still open, since §9's `result_export` holds only `delivery_code` and `location`. |
| **Manifest table** | **Still absent from `DATA_MODEL.md`.** Grep returns only §12's own heading (line 755). Approved by `25` on 9/2; described by Ben in `25` D2; **now required to carry a treaty grain for TY (D21)** and, per §3.2, a user-set *closed* state. `25` **O25-1**'s home-and-grain question is nine days old and now has a third grain candidate. |
| **Staging tables** | **Still absent.** `staging` returns one unrelated hit (line 33). `25` approved them into the `LOSS` database; §12's table manifest has no row. |
| **`analysis_result_meta` (§9, lines 567–618)** | **Unchanged and still wrong-shaped for TY.** Exactly-one CHECK at line 617; `perspective_code` *"GR / RL / WX / QS / GU"* at 585 — no `TY`, no `RP` (`22` O22-13); Parquet at `{analysis_id}/{perspective_code}/{result_type}.parquet` at 618. **Group-first improves this slightly** — a grouped analysis *is* an analysis, so `analysis_id` has a value — **but the burst still needs a treaty segment the path does not have.** |
| **`TY` in the requirement docs** | **Still absent.** Unchanged from `28` §10: the only hit is the unrelated EDM naming prefix `TY{YY}{MM}`. |
| **Engine vs model version** | **Worse than absent — collapsed in code.** `analysis_service.py:386` coalesces `engineVersion` and `modelVersion` into one field; `AnalysisSettings` has no `model_version`; `analysis_jobs.py:350–351` stamps engine only, under-delivering `specs/011` FR-021's *"engine **and model** version"*; `grouping_service.py:315` keys partitions on `model_version` meaning the genuine one. `FUNCTIONAL_REQUIREMENTS.md:562` writes *"engine / model version"* as one phrase. **`23` D19 and today's D9 are the same defect twice.** |
| **The historical-events lookup table** | **Still absent from the repo; `PCS` and `stochastic` return zero hits across FR, DATA_MODEL, PRD, specs and `app/`.** `23` **O23-10** documents it — **with the wrong key.** Today: keyed on **engine version**; ~2,500 rows; CIC will hand over a CSV with headers. |
| **FR §7 Delivery (lines 594–599)** | **Still two rows**, one of them the copy/paste requirement. The ELT row still carries *"Open question: how to move data from DataBridge to the Loss Repository"* — an open question `20` answered by removal, `23` **O23-5** asked to rewrite, `21` **O21-10** called *"an entire subsystem,"* and `28` said was *"two items larger."* **Today makes it four items larger:** the burst, the TY grain, the duplicate-warning behaviour, and the Workbench-provenance `database` column. |
| **FR line 601 (MVP exclusions)** | **Now load-bearing and still just an exclusion list.** *"uploading loss sets to Analyze Re (separate API)"* determined the Explorer decision, the optional `data name`, and the status-screen scope. **`Analyze Re` appears in zero prior design notes.** Also still excludes *"carrying CRM ID tags through to the repository upload (Future)"* while CRM ID is populated on every exported row — flagged by `20`, `23` **O23-5** and `28`; **three notes and still wrong.** |
| **Tagging** | **Three tag surfaces exist** — `submission_crm_id` (FR 57, §4), `analysis_template_tag` (FR 367, §7), and the submission-name tag at submit (implemented in code; **FR line 446 still says Not implemented**). **No analysis-level tag table, no `irp_tag` cache** (`PRD.md` §15.2: *"does not exist"*). **No search-by-tag requirement anywhere** — grep for `by tag` / `tag search` returns zero across FR, DATA_MODEL, PRD and specs. |
| **spec 012 FR-017 (line 124) vs today's D25** | **⚑⚑ DIRECT COLLISION.** *"Groups carry no tag — the platform grouping job accepts none (O-07),"* with O-07 adding *"no endpoint tags an analysis after creation."* **CRM ID is now the only tag, and group import by CRM ID tag is `27` D23's first move and `27` D24's definition of done for grouping v1.** |
| **Search (FR 117, 118; PRD §19)** | **Unchanged, and promoted.** Line 118 Partial since 7/14 — *"no list section but the submissions master list has search or filter."* Line 117 Not implemented with no notes. `PRD.md:1235` defers the analyses provider explicitly, noting there is **no per-analysis detail URL**. **D25 depends on D27 shipping.** |
| **Duplicate-export semantics** | **Nowhere.** No coverage in `23`, none in the repo. The only idempotency statement (`DATA_MODEL.md:616`) governs retrieval, not the push. The live check keys on `analysis ID + perspective` and is being relaxed. |
| **Failure lifecycle** | **Designed once (`18` D4/D5/D6) for the analyses grid, now needed on the export screen.** The *closed / resolved* state is new to the series. `18` **O18-6** — legible failure reasons — is open since 8/12, third raising, and **the export screen may already satisfy it**. |
| **In-force / bound** | **Nowhere in 28 notes or in the FR.** New thread. |
| **`CONTEXT.md`, `docs/adr/`** | **Both still absent** (`CLAUDE.md` says created lazily). |

---

## 9. Schedule, status, and what was not raised

**Next session: next week.** No slot was set on the call. Wendy named the agenda — *"maybe we can come back and talk about that next week"* — meaning **in-force / bound / not-bound**, with **group-to-submission sync** and the remaining tagging questions alongside.

**Two undocumented calls on 9/11 itself.** A morning call in which the missing loss-repository tables were raised (*"which we called out this morning"*), and the **deployment-team call** immediately following this session. Neither has a note. §1.

**Status: not restated, fourth consecutive note. Yellow stands from `23` D1.** The staging DDL (`26` O26-13, `27` O27-13, `28` O28-14) and deployment (`26` O26-14, `27` O27-13, `28` O28-14) were not on the agenda. **What today adds is that the target database is still empty** — Ben's *"the SQL Server that we're connecting to, which doesn't have any of these tables right now."* `25` closed the approval on 9/2; nine days later nothing has been created. `23` **D2**'s exit condition depends on it. → **O29-13**.

**Randy was not mentioned.** `28` recorded Wendy taking the action to chase him after that call, with one update in hand (mounting the shared drive to the Linux server). No follow-up today — though the deployment-team call that followed is the obvious place it went.

**One small build fact worth noting:** dark mode exists. Anil: *"We built a dark mode. I don't know if anybody saw the dark mode."* Cheryl: *"I'm a dark mode person."* Wendy: *"I like it, yeah, I do too."* Not a decision; recorded because it is the only UI item in the session that nobody had to argue about.

**Tone.** The session was cooperative and fast, and its character was subtractive: an Explorer proposed and dropped, a tagging plan reduced to one field, a duplicate block relaxed, a validation deleted. Wendy's closing line is the summary the client left with: *"Hopefully you'll sleep easy this weekend. **Not as much tagging needed. So everyone should feel like a weight has been lifted.**"*

---

## 10. Carried-forward (not advanced this session)

- **`28` O28-1 — TY has no home in the repo.** Unchanged, and now needing an extra distinction: under group-first, TY is an export **mode** with its own grain, which strengthens O28-1's option (c) relative to (a) and (b).
- **`28` O28-3 — what financial perspective rides with a TY export.** Not discussed. Ben's value comparison (Anil's 5,512 KB smoke test) was not reported on.
- **`28` O28-4 — the treaty key (treaty number + name).** Not discussed and not contradicted. **Still the aggregation key under either path**, and still the matching key for spec 012 FR-020.
- **`28` O28-5 — turn all treaty perspectives on by default.** A CIC Risk Modeler configuration action; not reported on.
- **`28` O28-6 — record which treaties an analysis applied.** **Third consumer confirmed and unchanged.** Under group-first the export still needs to know a group's treaties to burst correctly, so the requirement survives the reversal intact.
- **`28` O28-7 — `22` O22-14 (a treaty that takes loss) appears cleared.** Not confirmed or closed.
- **`28` O28-8 — Cheryl blocked by run size and runtime.** Not mentioned; she evidently got enough results through to run the 3-analysis test.
- **`28` O28-12 — four outstanding Moody's questions.** Not chased, not mentioned. **A fifth is implied by today**: whether the grouping job will ever accept a tag (spec 012 O-07's *"Revisit if Moody's adds a tagging endpoint"*).
- **`27` O27-3 / O27-5 / O27-6 — simulation sets, the paring mechanism, CIC's defaults.** Not discussed. `PRD.md` §15.2 still says `irp_simulation_set` does not exist.
- **`27` O27-11 — broker-provided event rate schemes.** Ben said on 9/4 it was solved and unrecorded; **still unwritten**, and not mentioned today.
- **`27` O27-12 — the acceptance matrix, HD/DLM equivalence, independent groups.** Not discussed.
- **`28` O28-11 — spec 012 does not carry `27` D14's defaults reversal, and records its opposite as non-negotiable. ⚑⚑** **Not reconciled, and not raised, despite tagging being on the agenda and spec 012 being the document the tagging work sits beside.** `28` explicitly asked for this to be forced onto today's call. It was not.
- **`25` O25-2 — the load / batch / job ID Nagi asked for.** Not discussed. Note the export status screens now display per-export identity, so something is already serving this role in the UI.
- **`25` O25-5 — §8's two export job types under-describe the chain.** Not discussed. **Today's demo makes the chain observable for the first time**, so this is now checkable against something real rather than a description.
- **`23` O23-2 — the staging concept is absent from the data model.** Unchanged, nine days after `25` approved it.
- **`23` O23-9 — implement the historical/stochastic mapping as an explicit set.** Not discussed. The export screen now surfaces **stochastic and historical event counts** per analysis, so the classification is live in code and still unspecified.
- **`23` O23-13 — the exceptions/incidents tab as a notification fallback.** Not discussed, though §3.2's Close/Resolved state is adjacent to it.
- **`24` O24-11 — the reference table's owner and the 9/2 Cheng meeting.** **Still no recorded outcome, and a third Cheng ask was added today.**
- **`21` O21-3 — per-analysis perspective validity.** Not discussed; unchanged.
- **`21` O21-10 — rewrite FR §7 Delivery.** Unwritten, and four items larger after today.
- **`18` O18-4 the notes field (tenth session), `20` O20-4 region granularity (seventh), `20` O20-7 analyst attribution, `22` O22-13 `RP`, `22` O22-15 breadcrumbs.** All untouched.

---

## 11. Open questions & follow-ups

- **O29-1** — **The Analyze Re dependency is out-of-scope in one line and is now deciding scope. Write it down as a workflow fact. ⚑⚑** D4. Wendy deferred the Loss Repository Explorer because *"we have to use our workflow tool in order to push results to our simulation engine, **Analyze Re**, because that is not within scope of this project. **And so I have to go there anyway**."* Cheryl, who wanted the Explorer, conceded on the spot: *"Solid point… We will have to be in that tool anyway."* **`Analyze Re` appears in ZERO of the 28 prior design notes and `simulation engine` in none either; in the repo it exists only as `FUNCTIONAL_REQUIREMENTS.md:601` (*"uploading loss sets to Analyze Re (separate API)"*) and `PRD.md:1132`.** **That one exclusion now explains four separate scope decisions** — the Explorer's deferral (D4), `data name` staying optional (D3), the status screen reducing to failure triage (D5), and CIC's tolerance of the TY workflow change (D18). **Action:** restate it in FR §7 as a workflow consequence — *after every export the analyst leaves the Workbench for CIC's workflow tool* — not merely as an MVP boundary, because an implementer reading line 601 today learns what is unbuilt, not that the happy path ends elsewhere. **And record Wendy's roadmap intent:** *"I would love at the future to bring these tools together… so to stop having to go back and forth"*, which belongs in `PRD.md` §20.4. Anil's parked design — *"a loss repository explorer… you can go and see what is out there, you can change things out there, and you can send things to another workflow from there"* — is the shape it would take. (§3) *Ben — FR §7, PRD §20.4.*
- **O29-2** — **Export failure triage: one pattern, not two — and the *closed* state is new to the series. ⚑** D5, D6, D7. **The agreed design:** quick filters all / failed / loaded (Anil: *"failed would show everything that has got at least one failure… loaded will show only those that have at least one loaded"*); a **Close / Resolved** action per failed entry so it leaves the list (Wendy: *"I just want to click it and have it go off my list… I just want those to go away"*); status and data ID left-aligned; AAL by peril added to both views; the zip file-path column dropped; retry retained; no click-through to the analysis (a row can cover a group or several analyses). **Three reconciliations.** **(a)** `18` **D4/D5/D6** designed this lifecycle for the analyses grid on the same user's same complaint — failures grouped to the top, filter by status, multi-select delete. **Build one pattern.** **(b)** **A soft *closed* state has no precedent**: a grep of all 28 prior notes returns no dismissal concept at all, and the designed lifecycle to date is failures-on-top → delete. **It is user-set Workbench state, which is an argument for the manifest living in `WORKBENCH`** — see **O29-7** and `25` O25-1. **(c)** `18` **O18-6** — legible failure reasons, open since 8/12 via `14` D13, third raising — **may already be satisfied on this surface**: the demo's failure reason was legible enough for Ben to diagnose live. **Check whether the mechanism generalises to the analyses grid before building it twice.** (§3.2) *Ben — FR §7, `18` O18-6.*
- **O29-3** — **Duplicate exports become a warning — and nothing in the repo covers the subject. ⚑** D13, D14. **Decision:** the form-level block is replaced by a warning; the analyst renames (Cheryl: *"I could put a V2. That's common for us to do"*). **The live staging check keys on `analysis ID + perspective`** and Anil requires it relaxed to match: *"we may just need to revisit that, so we allow an intentional export of a duplicate with a different name… **I was trying to think of a way to safely check for duplicates. There isn't one.**"* **Reconciliation:** note `23` has zero coverage of duplicates, re-export or idempotency; the repo's only idempotency statement is `DATA_MODEL.md:616`'s upsert on `(rdm_id, analysis_name, perspective_code)`, which governs **retrieval** into `analysis_result_meta`, not the push. **So both the constraint and its relaxation exist only in code and in this note.** **Note the interaction with D2/D3:** the data name does the disambiguation work, and it is explicitly **not required** — so a duplicate with no name is possible and should be what the warning actually warns about. **Specify what the repository does with two identical rows**, since Cheryl described it as *"an error issue in terms of having duplicates"* rather than something the target rejects. (§2.4) *Ben + Anil — FR §7, the staging/manifest design.*
- **O29-4** — **`23` §6's field mapping: one reversal, one change, one drift — write all three down before the mapping is encoded. ⚑** D1, D2, D3, D11, D12. **Confirmed:** `data name` never required (**closes `23` O23-7**); `client ID` read-only name search (`22` D25 holds); `analysis ID` the user-facing one (`23` D17); vendor `RMS`; version 25; `archive file`/`area`/`LOB`/`geography` unpopulated; CRM ID populated. Cheryl: *"You got it all."* **Changed — `data vintage` is now required** (Cheryl: *"Should not be optional. That has to be filled in"*), against `23` §6's *"Leave blank; analyst enters it."* Required-with-no-default is the shape. **Reversed — `database` now carries the Workbench database name**, against `23` §6's *"Not populated."* **Record Wendy's reason, because it is a requirement not a default:** *"everything that has the Workbench label on it came through the Workbench, and if we identify a problem later, we can query for that and find them"* — a blast-radius query on a production table the Workbench writes to. **Caveat: direct broker uploads may bypass the Workbench and populate it the old way.** **Drifted — `server`:** `23` chose the **tenant** base URL (Cheryl: *"I think that'd be great"*); the build has the **API** URL; Cheryl waved it off with *"it sort of doesn't matter… we're not working in a SQL environment anymore."* **Pick one and say which.** **New fact — no active filter on the client dropdown** (Wendy: *"we often price business that's not a current client, but we still have a client ID for it"*), which answers half of `22` **O22-9(d)**; the other half — where the selected client ID is recorded against the export — is still open. **And `23`'s "data vintage is not a submission field" is being reopened by the person who confirmed it** — see **O29-12**. (§2.3) *Ben — FR §7, `23` §6, `22` O22-9.*
- **O29-5** — **`23` O23-10 STATES THE WRONG KEY, and `23` D19's defect has now recurred. Fix the join, assert the equality, take the table. ⚑⚑ Corrects `23` O23-10 / O23-8; advances `23` O23-5.** D8, D9, D10. **The correction:** CIC's historical-events lookup is keyed on the **ENGINE version**, not the data model version. Wendy: *"what we track in our lookup table, even though it says model version, **it's engine version**… every analysis I run, every peril, every country, everything has to say version 25 on it"* — because the Analyze Re catalog requires one consistent version per result set. Cheryl, who was the source of the original claim: *"we want the **engine version** there… we don't maintain that data model version."* **`23` O23-10 says *"keyed on event ID / peril / data model version"*, repeated at `23` lines 225 and 379, sourced to Cheryl being *"ninety-nine percent sure"* with *"we should verify with Cheng."* It was verified today, by accident, and it is wrong. Amend O23-10 and O23-8.** **The recurrence:** Ben was joining on the analysis's **data model version** (18.1) instead of the engine version (25), and the export failed live. **`23` D19 caught the identical confusion in the ELT table** (event model version code 17 vs DLM 25) and **`23` O23-5 said *"Assert it, don't document it."* It was not asserted.** **Write the assertion now, against the engine version, as a test** — noting the irony that the assertion O23-5 asked for would have encoded the wrong key. **Delete the model-version validation (D8):** Ben, *"this is not the right place to do it. This is a totally invalid error"*; Cheryl, *"it would never not be historical data because of a mismatch of this model version."* **Take delivery of the table (D10):** ~2,500 rows, CSV with headers preserved (or a .bak), non-sensitive (*"nothing more than what you've pulled from the software today"*), carrying the **PCS** column the Moody's reference data lacks. **This retires the DDL half of O23-10 and most of the usage half; what it does not deliver is Cheng's normalisation logic** (`23` D25 — historical events only) — see **O29-8**. **And fix the code, which is the deeper problem:** `analysis_service.py:386` coalesces `engineVersion` and `modelVersion` into one field, `AnalysisSettings` has no `model_version`, `analysis_jobs.py:350–351` under-delivers `specs/011` FR-021's *"engine and model version"*, and `grouping_service.py:315` keys partitions on `model_version` meaning the genuine one. `FUNCTIONAL_REQUIREMENTS.md:562` writes them as one phrase; `PRD.md:1189` flags the gap; `27` §11 is the only place they are named as distinct and it is a design note. **`PCS` and `stochastic` return zero hits across FR, DATA_MODEL, PRD, specs and `app/`.** (§4) *Ben (join, assertion, FR) + Wendy/Cheryl (the CSV) — `23` O23-10, FR §7, `DATA_MODEL.md` §1/§9.*
- **O29-6** — **TY REVERSES TO GROUP-FIRST; `28` O28-9 is ANSWERED and it is the answer that reversed it. Now write the trade-off document. ⚑⚑⚑ Supersedes 9/10 D10; re-opens `21` O21-4(b).** D15–D19, D23. **The evidence:** Cheryl ran the test `28` O28-9 said she owed — three analyses, gross loss + treaty output level, exported together — and Risk Modeler produced **three separate zip files with no aggregation**. **`28` §5's design was built on CIC's workflow tool's behaviour; the platform does not do it.** **The decision:** the user creates the analysis group first, then exports the group as TY. Anil's case — *"we don't even have to worry about aggregating anything. We don't need the aggregation formulas. It's already aggregated on Moody's"* — and his description of the alternative's cost (a treaty-grouping concept on the manifest that expects N files, holds until all N arrive, and **times out and abandons** if one never does, because a partial success is a wrong aggregate). **Underneath, this is the first concrete answer to `23` O23-2(c)'s recovery semantics: a cross-analysis aggregate makes `23`'s per-analysis recovery impossible by construction, and group-first removes the case.** **CIC accepted (D17)**, on faith about Risk Modeler grouping at volume — Cheryl: *"I'm going to go a little bit on faith that grouping continues to be not painful"* — which is the condition that would invert the decision. **The behavioural difference is judged acceptable (D18)** because the screens look nothing alike: Cheryl, *"if it looked exactly the same as workflow and had different results, that would be confusing. **This looks different.**"* **Record that as a design constraint — an argument against ever making the export screen resemble the workflow tool** — since `21` and `23` repeatedly used that tool as the model. **Mitigation (D23):** informative text when TY is picked — *"you'll get one loss set per treaty, per analysis"* (Wendy); Anil's context-sensitive help declined as unnecessary. **THE ACTION (D19):** Anil required both paths preserved — *"let's pick one, tread that path, and if it doesn't work"* — and Ben owes the document: *"Let's document them, the trade-offs, decide what we want to do after doing that."* **Write it before the worker, because the two paths put the aggregation in different systems and therefore the manifest's grain in different places (O29-7).** (§5) *Ben + Anil — new spec, FR §7.*
- **O29-7** — **The manifest and staging tables must go treaty-grained — `25` O25-1's question with a third grain, and still nothing in the repo to amend. ⚑⚑ Advances `25` O25-1.** D21. Ben, twice: *"our manifest table currently defines the rows we need to write at the analysis level, **which is not what TY perspective is. It's at the treaty level**"*; and *"the manifest table has to be amended to account for TY perspective. **As it is right now, it will not handle it appropriately.**"* He named the staging tables too. **The grain is now three-valued:** per analysis × perspective for GU/GR/RL/WX/QS (`23` D9's *"one `data` row per analysis"*), and **per treaty** for TY. **Group-first reduces the fan-out but does not remove it** — one grouped analysis is one manifest entry that still produces N data rows, one per treaty. **`25` O25-1 asked *"What is the grain? Fix the grain explicitly, because it decides whether the load ID in O25-2 is per-archive or per-perspective."* It now also decides per-treaty, for one mode only.** **And there is nothing to amend:** `DATA_MODEL.md` grep for `manifest` returns only §12's own heading (line 755) and for `staging` one unrelated hit (line 33) — **nine days after `25` approved the staging tables into the `LOSS` database**, and repeating `23` **O23-2**'s finding verbatim. **Fold in two more requirements while writing it:** the **Close/Resolved** state from **O29-2** (user-set Workbench state, an argument for O25-1's `WORKBENCH` half), and the relaxed duplicate check from **O29-3**. (§5.6, §8) *Ben + Anil — `DATA_MODEL.md` §9/§12, `25` O25-1.*
- **O29-8** — **Three outstanding asks of Cheng, none chased. Batch them like `28` O28-12 said to batch Moody's. ⚑** D22. **(1)** `23` **O23-10**: how Cheng uses the historical lookup table — *"so I can basically use it the same way"*, Cheryl *"Can do"*, ten days ago, no outcome. **(2)** `24` **O24-11**: the reference table's owner and the 9/2 Cheng meeting — still no recorded outcome per `28` §12. **(3) New today:** the workflow tool's TY aggregation query. Ben asked; Cheryl and Wendy both agreed — *"We can look at his query."* **What (3) is actually needed for:** the exposure value, which is the one term Cheryl could not give a rule for — *"in this case, it's **the max for that treaty**. So your exposure value is going to get kind of goofed up. **I don't know how Cheng handles that.** Because I don't think it's really additive either."* **The rest of the math is on record:** losses sum; independent SD additive; correlated SD the root of the sum of squares; and the key is *"treaty['s] event ID and rate."* **Needed only if the fallback path in O29-6 is taken — but it is also the tie-breaker for that decision, so get it before the trade-off document, not after.** (§5.7) *Ben — via Cheryl/Wendy.*
- **O29-9** — **Tagging collapses to CRM ID — record it as a reversal with its reason, not a shrug. ⚑⚑ Reverses `27` D22; re-scopes `27` D23; advances `27` O27-8.** D24, D25. **The premise:** Risk Modeler tag search ORs rather than ANDs, so adding tags widens the result set (Cheryl's X / Y / XY example), and the picker is one flat dropdown — *"can you imagine if there are 5,000 tags in here?"* Anil: *"the tag as a **key value pair** would have been a better way to implement this."* **Dropped as Risk Modeler tags:** analyst name, treaty year+month (which OR semantics forces into a single `TY2605` token anyway), cedent name, reinsurance type, expiration date. **Kept:** CRM ID, on EDMs, RDMs and analyses. Wendy: *"we're just going to tag CRM ID. That's it… because CRM ID gets us to everything we need, like every link in every system… **But I would hate to say zero.**"* Ben went further: *"I don't want to tag at all on Risk Modeler."* **What this does to `27`:** D23's second item (auto-tag everything, medium) is cancelled; its third (submission-level sync, medium-high) is effectively cancelled because the findability it bought moves to the Workbench (**O29-11**); only the first survives — and see **O29-10**. **Write it with the reason**, because seven days ago Cheryl reported that peer companies on the product *"wish they would have tackled [tagging] sooner… it was a big deal,"* and a future reader will otherwise find an enthusiastic design in `27` and an unexplained retreat here. **The retreat is defensible — findability is relocated, not abandoned — but only if O29-11 ships.** **Fix `FUNCTIONAL_REQUIREMENTS.md:446` while rewriting this section:** it still marks the submission-name tag *"Not implemented"* against code that implements it (`analysis_execution_service.py:252–254`), flagged by `27` O27-8 and still wrong. (§6.1, §6.2) *Ben — FR §6/§7, `27` D22/D23, `12` O12-4.*
- **O29-10** — **⚑⚑⚑ THE COLLISION: CRM ID is the only surviving tag, and spec 012 says a group can never carry it. Advances `28` O28-13 and `27` O27-7.** D25. **`specs/012-grouping-execution/spec.md:124`, FR-017:** *"Groups carry no tag — **the platform grouping job accepts none** (O-07)."* **O-07, line 53:** *"the platform grouping job schema has no tag field and **no endpoint tags an analysis after creation**"* — Approved, decided **2026-08-27**. **`27` D21/D23 made *import groups by CRM ID tag* the low-effort, high-impact first move, and `27` D24 made group import part of grouping v1's own definition of done.** **If no endpoint tags an analysis after creation, a group can never carry a CRM ID tag — at creation or afterwards — and import-by-CRM-ID cannot reach the one entity type it exists to import.** Anil's reassurance (*"if you are tagging EDMs and RDMs, then it should be okay… EDMs and RDMs are not many in number"*) does not cover groups, which are neither. **⚑ And Cheryl described the same wall from the other side three minutes earlier and nobody connected it:** *"I think the big challenge is **groups**… trying to **sync a group back to a submission**. **We have to have something to hang on to** to link that back to this submission."* **That is `27` O27-7, and "something to hang on to" is exactly the tag FR-017 forbids.** **Three actions in order: (1)** re-verify FR-017/O-07 against the current Moody's API — the decision predates `27` D21 by eight days and O-07 itself says *"Revisit if Moody's adds a tagging endpoint"*; **(2)** if it holds, choose the alternative discovery key for groups **before `27` D23's *low effort* estimate is spent**; **(3)** treat Cheryl's group-to-submission sync as the same question, not a separate one. **This is the second note to raise it (`28` O28-13) and the first where it blocks the entire remaining tagging plan.** (§6.3) *Ben — spec 012 FR-017/O-07, `27` D21/D23/D24, `27` O27-7.*
- **O29-11** — **Findability moves to Workbench search — which promotes `27` O27-9 from a backlog nicety to D25's precondition. ⚑⚑ Advances `27` O27-9.** D26, D27. Wendy: *"the searching is so much better in the Workbench… **people will continue to keep coming to the Workbench because that tagging thing over there is ridiculous. And the search is better here because it's more intuitive.**"* Ben: *"we have all of this searching for submissions, but **this essentially needs to be extended to EDMs, RDMs, and analyses too**"* — Wendy agreeing on analyses after hesitating. Anil: *"we could put **real fields instead of just tags**… you can search by cedent. You don't have to run the tags."* **What the repo says:** `FUNCTIONAL_REQUIREMENTS.md:118` — Partial since 7/14, *"**no list section but the submissions master list has search or filter**"*; line 117 global search across EDMs/RDMs/analyses **Not implemented** with no notes cell; `PRD.md:1235` defers the analyses provider explicitly and notes **there is no per-analysis detail URL** to land on; `search_service.py` carries six providers and none for analyses. **`27` O27-9 has been carried as unadvanced and out of spec 012's scope for two notes. Today it became the mitigation for a cancelled subsystem: D25 is only safe if D27 ships.** **Also write the AND-semantics requirement rather than assuming an existing one stretches:** line 116's *"Multi-term search uses AND semantics, not OR"* is scoped by its own text to the name search, the cedant filter and the links-to picker; Ben's design is per-purpose dropdowns ANDed together — *"we can turn that into a query that works the way that we want it with an AND statement for tag values"* — which is a fourth surface, and line 114's *"values OR within a filter and AND across filters"* may or may not be the right model. **Ben also scoped it correctly:** build it on the Workbench EDM/RDM screens, not the sync-from-Risk-Modeler screen, *"because they don't have tags like this that we're describing."* (§6.4) *Ben — FR lines 114/116/117/118, `PRD.md` §19/§20.4, `27` O27-9.*
- **O29-12** — **Two new submission-page threads, both deferred to next week — and one of them reopens a `23` confirmation. ⚑** **(a) In-force / bound / not-bound.** Wendy: *"I feel like we're **missing a couple fields in the Workbench that would allow us to identify our bound business and our in-force business**. And we need those things for various different reasons."* Anil and Ben both said it does not worry them. **A grep of all 28 prior design notes finds nothing on this**, and `FUNCTIONAL_REQUIREMENTS.md` has no such field; the submission's status is a Workbench workflow status, not a business one, and `21` D8's *in-force date* on the export is a different thing. **Establish whether this is a status enum, a date pair, or both, and whether it drives filtering (which folds into O29-11).** **(b) Expiration date and data vintage on the submission.** Cheryl during tagging: expiration date *"is not something we capture today, but maybe we should add that as a field in the submission page"*; Wendy: *"**Along with data vintage.**"* **This reopens `23` §6's *"Cheryl confirmed it is not captured on the submission"*, by Cheryl** — and interacts with **D2**, which made data vintage required at export with no default. **If it becomes a submission field it gets a default and D2's "no default" changes.** Both to next week. (§7, §2.3) *Wendy/Cheryl + Ben — FR §1, `23` §6.*
- **O29-13** — **The staging DDL, deployment and status: FOURTH consecutive note, and two more undocumented calls on the same day. ⚑⚑ `26` O26-13/O26-14, `27` O27-13, `28` O28-14 unchanged.** Not raised on 9/4, 9/10 or today. **What today adds is confirmation the target is still empty:** Ben, mid-demo, *"I'm counting on you to populate it in the SQL Server that we're connecting to, right, **which doesn't have any of these tables right now, which we called out this morning**."* **`25` closed the approval on September 2; nine days later nothing has been created**, and `23` **D2**'s exit condition depends on it. **Two more gaps in the record:** a **9/11 morning call** in which the missing tables were raised, and the **deployment-team call immediately after this session** — neither has a note, on top of the unrecorded 9/8–9/9 window `28` §1 identified and the 9/9 email backlog. **Status not restated for a fourth session; yellow stands from `23` D1.** **Randy was not mentioned**, though `28` recorded Wendy chasing him and the deployment call that followed is the obvious place it went. **Force the DDL, deployment and status onto the next design call rather than leaving them to calls that produce no record** — `28` asked for exactly this and it did not happen. (§1, §9) *Ben + Randy / Ross / Nagi.*
- **O29-14** — **October 1 is the first delivery date named in the series, and it appears nowhere in the repo.** Wendy, deferring the Loss Repository Explorer: *"Yep, that's the vision. **Probably just not by October 1st.**"* **No date appears in `FUNCTIONAL_REQUIREMENTS.md`, `DATA_MODEL.md`, `PRD.md` or any of the 28 prior notes.** A date mentioned in passing by the client is not a commitment and should not be treated as one — **but with status unrestated for four sessions (O29-13) and the DDL and deployment both unstarted, it is the only schedule signal that exists.** **Establish what October 1 refers to and whether PremiumIQ shares the understanding, at the next status conversation.** (§3.1) *Ben.*

**Advances / closes (from prior sessions):** `28` **O28-9** the owed cross-analysis test — **ANSWERED, negatively**: Risk Modeler does not aggregate treaties across a multi-analysis export; three analyses produced three zip files (**O29-6**). `28` **O28-2** the burst-plus-aggregate contract — **HALF SUPERSEDED**: the burst stands verbatim (9/10 D5), the push-time aggregation transform is replaced by group-first, which also re-opens `21` **O21-4(b)**'s answer (**O29-6**, **O29-7**). `28` **O28-10** group-on-export vs group-then-export — **SPENT, deliberately**: the freedom O28-10 advised banking was spent because the platform forced it (**O29-6**). `28` **O28-13** groups cannot be tagged — **ESCALATED to blocking**: CRM ID is now the only tag and spec 012 FR-017/O-07 says a group can never carry it (**O29-10**). `28` **O28-6** which treaties an analysis applied — **UNCHANGED and still required** under group-first, since the burst needs the group's treaty set. `23` **O23-10** / **O23-8** the historical lookup table — **KEY CORRECTED**: engine version, not data model version, by the person who was *"ninety-nine percent sure"* of the original; and the DDL half is retired by CIC handing over the table (**O29-5**). `23` **O23-5** write the mapping down and assert the version equality — **ADVANCED and overdue**: `23` D19's defect recurred in a second join and failed a live demo (**O29-5**, **O29-4**). `23` **O23-7** `data name` never required — **CONFIRMED against the built screen; closeable** (**O29-4**). `23` **O23-2** the staging concept is absent from the data model — **UNCHANGED, nine days after approval** (**O29-7**). `23` **O23-2(c)** recovery semantics — **ANSWERED for TY by removal of the case** (**O29-6**). `25` **O25-1** the manifest's home and grain — **ADVANCED**: a third grain candidate (per treaty, one mode only), plus a user-set *closed* state arguing for the `WORKBENCH` half (**O29-7**, **O29-2**). `27` **D22** the tagging design — **REVERSED to CRM ID alone** (**O29-9**). `27` **D23** the sequencing — **RE-SCOPED**: item 1 survives (and is blocked), items 2 and 3 cancelled (**O29-9**, **O29-10**). `27` **O27-7** group import / group-to-submission sync — **RAISED by Cheryl and unresolved**, now the same question as O29-10. `27` **O27-8** the tagging write-up — **OVERTAKEN by events**; FR line 446 still wrong. `27` **O27-9** search on every list section — **PROMOTED from nicety to precondition** (**O29-11**). `22` **O22-9(d)** the client search — **HALF ANSWERED**: raw name search, no active filter (**O29-4**). `21` **O21-10** rewrite FR §7 Delivery — **four items larger**. `18` **O18-6** legible failure reasons — **third raising, and possibly already satisfied on the export surface** (**O29-2**). `24` **O24-11** the Cheng thread — **a third ask added, still no outcome** (**O29-8**). `28` **O28-11** the spec 012 defaults divergence — **NOT RAISED**, despite `28` asking for it to be forced onto this call. `28` **O28-12** the Moody's batch — **NOT CHASED**, and a fifth question is implied (whether the grouping job will ever accept a tag). `26` **O26-13/14**, `27` **O27-13**, `28` **O28-14** the DDL, deployment and status — **fourth consecutive note** (**O29-13**).

**Next session: next week, no slot set.** Wendy's agenda: **in-force / bound / not-bound fields**, with **group-to-submission sync** and the remaining tagging questions alongside. **Four things should be forced onto that call rather than left to it.** **The DDL, deployment and status** (**O29-13**) — fourth consecutive miss, and now with the demonstrable consequence that the export works against a target that does not exist. **The FR-017 collision** (**O29-10**), because it invalidates the one piece of the tagging plan that survived and because Cheryl will raise group sync anyway. **The TY trade-off document** (**O29-6**), before the manifest is touched. **And `28` O28-11's spec divergence**, which `28` asked to be forced onto *this* call and which was not mentioned. The session itself was the best kind: a working demo, a client who cut scope rather than adding it, a live bug diagnosed on screen, and a design reversal driven by an experiment somebody actually ran. **The cost is that three recorded decisions are now wrong in the notes and two of them were wrong in ways that only running the code revealed — which is the argument for writing the export down before the next thing is built on top of it.**
