# IRP Workbench — Design Notes: The Zero-Loss Filter Lands **Worker-Side, Exactly Where `32` Said to Put It** — the **Contract Grain Is Built Overnight and Accepted Without a Change**, and Then the **CRM ID Must Become Globally Unique**, Which `0001_initial.py` Explicitly Declines: the **Treaty-Year Default Is Reversed Eleven Hours After It Shipped**, Data Vintage Becomes Required, Search Stays Submission-Grained, and a Client Session on 9/21 Left No Record While Ten `017` Requirements Cite It

**Source:** Design session, Tuesday September 22, 2026 (~40 min, ~10:14–10:54 ET — the shortest session of the delivery phase, against a hard stop: Wendy at 38:23, *"we got like 3 minutes here."* Ben Bailey (PremiumIQ) demoing throughout; Wendy Hayes (CIC) and Cheryl TeHennepe (CIC) for the whole call; **Anil Venugopal (PremiumIQ) present but silent until ~34:45**, then five minutes on Power BI and CRM integration — his first contribution in five sessions). Cross-checked against the full transcript (622 caption cues, 178 merged speaker turns; Ben 324 cues, Wendy 149, Cheryl 114, Anil 35). Shape: **~13 min on TY export**, **~25 min on the contract grain and searching**, **~5 min on integration and next steps**.
**Status:** Working design notes. **Reconciled against `016-ty-perspective-export` (head `c2154d3`) for §2–§3 and `017-submission-data-and-search` (head `b1259a2`) for §4–§7; `main` is at `10e3c6a`** — see §1. **This is the third consecutive session to demo work built the night before, and the first in three where the client accepted the built model essentially whole.** **Closes** `32` **O32-2** (the zero-loss filter — built Monday 13:44, demoed today, and **built at the point `32` argued for rather than the one Ben described on 9/18**, so the Article 11 violation never happened), **O32-5** (the contract grain — spec, migration, routes, templates, docs and constitution, all between 23:00 Monday and 08:47 today), **O32-8** (the Client ID label), **O32-9** (expiration required with the one-year-minus-a-day default). **Advances** **O32-4** (the decision table now carries *derived from* and *confirmed on* as separate columns — and six rows have the second column blank), **O32-6** (the view and the pre-fill; the index is gone), **O32-7** (answered in the FR doc, demonstrated on the call, never asked as a question). **Opens** the first genuinely new requirement in three sessions: **global CRM-ID uniqueness**, which the migration written eight hours earlier explicitly declines. **Does not advance:** accumulation, tagging, Analyze Re, October 1, the industry databases, Risk Modeler searching.
**Where this sits in the series.** `32` is **September 18** (the treaty picker held, the zero-loss treaty took its place, and `017` was built, documented and reversed in one morning) — its decisions are cited as **9/18 D**n and its open items as **O32-n**. `31` is September 16 (**9/16 D**n / **O31-n**), `30` September 15, `29` September 11. **⚑ There was a client session on Monday September 21 that produced no transcript, no minutes and no design note, and `017`'s decision table cites it ten times as *"user 2026-09-21"*** — see §1.
**Related:** `32_treaty_picker_holds_zero_loss_filtered_017_reversed_crm_id_becomes_the_contract.md` (**D8–D11 the zero-loss filter, D13–D16 the metadata rebuild, D17–D23 the contract grain, D24–D25 in force and the bulk update, D27 the search grain, O32-1 … O32-14**), `31_treaty_selector_reverses_016_p03_same_day_…md` (**D2–D4, D10–D12 Origin; O31-5, O31-6, O31-11**), `../../specs/016-ty-perspective-export/spec.md` (**P-03, P-08, P-12, P-13, FR-002, FR-007, FR-009, FR-017, O-04**), `../../specs/016-ty-perspective-export/plan.md` (**the design summary, T-13, T-16, T-18, the sandbox check**), `../../specs/017-submission-data-and-search/spec.md` (**P-01 … P-20, FR-001 … FR-022**), `../../specs/017-submission-data-and-search/tasks.md` (**T054–T065, the completion notes**), `../../specs/014-results-export/spec.md` (**P-22, P-27 — both revised 2026-09-21**), `../FUNCTIONAL_REQUIREMENTS.md` (**line 47 the attribute list, line 48 one treaty type per contract, line 49 the treaty-year rule, line 52 required at creation, line 57 *"the CRM ID is the contract"*, line 115 the filters, line 629 the export pre-fill — all amended 2026-09-22 at 08:25, hours before the call**), `../DATA_MODEL.md` (**§4, amended 9/22**), `../PRD.md` (**§7.2a**), `alembic/versions/0001_initial.py:133-142` (`contract_status_kind`), `:144-181` (`submission`, and what it no longer has), `:157` (`data_vintage`, **nullable**), `:182-210` (`contract`, and the uniqueness comment at `:185-186`), `:215-224` (`CREATE VIEW v_contract`), `app/workers/analysis_jobs.py:437-447` (the per-treaty stats read), `app/services/export_service.py:297-307` (the cart's filter), `app/services/submission_service.py:334-340` (the duplicate-CRM-ID check), `:454-468` (`latest_inception_date`), `:917-931` (`submission_filter_clauses`), `app/templates/partials/contract_table.html`, `app/templates/partials/submission_head.html:59` (the "Deal" group label), `app/templates/pages/submission_form.html:111-124` (the treaty-year hint), `.specify/memory/constitution.md` (**v4.2.2, amended 2026-09-22**), `docs/ui_previews/submission_contracts.html`, `../../../../CIC/minutes/Risk_Modeler_Interface_Design_Minutes_9-22-26.md`, `../../../../CIC/transcripts/Risk Modeler Interface _ Design 9-22-26.vtt`

> Decision IDs (**D1**–**D20**) below refer to the tables in the 9/22 design minutes. Decisions from 9/18 are cited as **9/18 D**n (note `32`), from 9/16 as **9/16 D**n (note `31`), from 9/15 as **9/15 D**n (note `30`). Requirement IDs prefixed `014` are `specs/014-results-export/`; `016` is `specs/016-ty-perspective-export/`; `017` is `specs/017-submission-data-and-search/`. Open-item IDs are **O33-n**.
> *Transcript note: the transcription garbles domain vocabulary consistently and interpretations reflect context: **"laws" = losses**; **"trees" / "3 trees" / "tree types" / "3D" / "G3D" = treaties / treaty types**; **"the mall" = them all**; **"risk model" = Risk Modeler**; **"peer premium" = pure premium**; **"working access" = working excess**; **"announcement" = analysis**; **"Outlook profile" = output profile**; **"Seer I mighty" / "CMID" = CRM ID**; **"seedant" = cedant**; **"bonding status" = bound status**; **"enforce" = in force**; **"Section 8 of 8 one… It says 5 one" = the 8/1 and 5/1 inception dates of the two demo contracts**. Three passages resisted resolution and are flagged rather than guessed: the treaty rendered **"GE 2075"** (a quota share treaty in Ben's demo data); Wendy's treaty-year cut-off, rendered **"after 27, after 1127"** (from context, a date in late autumn); and **"then it's 2020"** in the same sentence (from context, the following treaty year). Stray tokens ("Tim", "Hello", "S.", "Thanks, Dean") carry no content.*

---

## 0. TL;DR

**`32` closed with three instructions and a warning. All three were carried out inside ninety-six hours, and the warning was heeded.** The zero-loss filter went in where `32` argued it should go, not where Ben described it on 9/18; the contract grain was respecified, migrated, routed, templated, documented and constitution-bumped between Monday evening and Tuesday breakfast; and the creation question was answered in the FR doc. **The call then found one new thing, and it is a constraint the schema had just finished declining.**

- **⚑⚑⚑ THE CRM ID MUST BE GLOBALLY UNIQUE, AND `0001_initial.py:185-186` SAYS IT IS NOT (D12, D13, D14).** Ben raised it as an open design question — *"There's nothing blocking us from using the same CRM ID across multiple submissions with different details."* **Cheryl answered before he finished:** *"**That should not be allowed.** Yeah, that should not be allowed. **That CRM ID is unique**, so yeah."* Wendy could not construct a counter-case — *"I'm trying to think of a scenario where that would need to happen, and I can't come up with one"* — nor could Cheryl: *"No, I don't think so."* **What is built is the opposite, deliberately and in writing:** the `contract` table's comment reads *"CRM ID uniqueness within a submission is the service's case-insensitive check; **no global uniqueness**"*, `submission_service.py:334-340` refuses a duplicate only with *"is already a contract on this submission"*, and `017` **FR-003** and **P-16** both fix uniqueness at *"within its submission."* → **O33-1**. (§5)
- **AND THE CITATION THAT AUTHORISED IT DOES NOT RESOLVE. ⚑** The migration comment attributes the no-global-uniqueness choice to *"note 32 D30"*. **`32` D30 is the industry-database workaround.** No decision in `32`, in the 9/18 minutes, or in `30` or `31` says CRM IDs may repeat across submissions. **The constraint was relaxed on a citation to a decision about Japanese exposure databases.** → **O33-1**. (§5, §9)
- **⚑⚑ AND THE ANSWER SETTLES A QUESTION `017` HAD ALREADY ANSWERED THE OTHER WAY.** Ben's real question was architectural: *"Do we need to manage contracts independently somehow in a central location rather than just as a subsidiary of submissions? **I'm almost leaning towards the latter.** Contracts have become a very first class object in the application."* **Uniqueness removes the need:** *"CRM ID belongs to 1 submission only. Editing its details is in the context of that one submission. So you can't have somebody editing your CRM ID in another submission."* His verdict — *"**Good, that makes it a lot easier then.**"* **Wendy's model needs no registry at all: first one in wins, a collision is an error, and the analyst goes and finds out why.** *"Like if I try to enter in a CRM ID and it says you can't do that because somebody already used it, I'd go looking for that submission, go figure out why somebody else is using my CRM ID. **So I don't think that there's any more logic than that required.**"* → **O33-1**. (§5)
- **⚑⚑⚑ THE ZERO-LOSS FILTER IS BUILT WORKER-SIDE — `32` O32-2's WHOLE ARGUMENT, TAKEN (D1, D2, D3).** `32` warned that Ben's *"I'm going to call stats"* meant `get_analysis_stats` from the web layer, which Article 11 bars, and argued for computing the flag at retrieval beside `resolved.treaties` instead. **`9b703b5` (Monday 13:44) does exactly that:** `analysis_jobs.py:437-447` makes one treaty-scoped TY stats read per applied treaty inside `retrieve_analysis_results` and stores `has_loss` on each entry; `export_service.py:297-307` filters on the stored flag. **`016` T-18 states the reason in the spec:** *"The cart reads the stored flag and **never calls stats (Article 11)**."* **The constitution was never amended and never violated.** Ben described the same shape to the client unprompted: *"I'm not doing it live on the screen, but I'm calling stats endpoint during that time [of results sync]… **And I'm saving whether or not this treaty took loss.**"* (§2)
- **AND HE VERIFIED IT OUTWARD, WHICH IS WHAT `32` SAID THE EVIDENCE WAS MISSING.** `32` recorded that the requirement was sound but the demo that produced it was synthetic. Today Ben checked the filter against the source: two analyses (42596, 42597) each run with three treaties offered exactly one TY option, and he opened both in Risk Modeler's treaty-losses view, ticked all three, and showed that only the quota share treaty returned losses. *"**That's exactly what we see here.**"* A seven-treaty group (42550) offered two. **Neither user challenged it; Wendy asked only for the logic and accepted the answer.** (§2)
- **⚑⚑ THE BACKFILL GAP IS NOW A TESTING RISK, AND TESTING STARTS THIS WEEK.** `016` **T-13** is explicit: *"An analysis whose results were retrieved before this release has no `treaties` key, or entries without `has_loss`… and **is offered no TY** until its results are retrieved again: an entry without the key reads as no loss. **No backfill, the dev database is rebuilt.**"* **That is fail-closed, which is the opposite of what `32` O32-2(c) recommended**, and it is defensible as a pre-cutover rule — but **`32` D31/D32 put testers on the app this week.** A tester exercising TY against an analysis retrieved before 2026-09-21 sees no treaties and, by Cheryl's own diagnostic (*"if I don't see it in the list, that's already a flag for me"*), will read a silent absence as a modelling error. **`31` O31-11 is the same gap one level down and is still open.** → **O33-2**. (§3)
- **⚑ AND RESULTS RETRIEVAL NOW DEPENDS ON N+1 RISK MODELER CALLS AND A TESTPYPI RELEASE CANDIDATE.** `analysis_jobs.py:444-446`: a failed treaty stats read `return`s `JobResult.fail`, so **one bad call on one treaty fails the whole results retrieval with `loss_results` untouched** — a job that previously depended on five perspective reads now depends on five plus one per applied treaty. And the read needs a keyword `0.9.0` rejects: **`irp-integration 0.10.0rc1`, a TestPyPI pre-release** (`1df4761`, `pyproject.toml`, `make irp-testpypi`). **A release candidate from a test index is now on the critical path of every results retrieval, in the week the app goes to testers.** Ben hit a retrieval error live and moved on — *"This looks like a nice technical error that we can retry successfully. **I need to check that.**"* → **O33-2**. (§3)
- **CHERYL DECLINED THE FALLBACK AND ASKED FOR THE NUMBER ONE SCREEN EARLIER (D4, D5, D6).** Ben offered listing every treaty with the no-loss ones locked — *"Would you rather see the full list of treaties here without the option to select the ones that don't have loss?"* Cheryl: *"**No.**" Her reason is that the interrogation belongs before the export button, not on it: *"before I even say export, I can look at that and say, oh, there's seven treaties."* **What she wants there is the loss figure:** *"I think putting the **pure premium or the average annual loss** in here would be helpful… **It's fast to you look right away down the list and be like, OK, only I only have loss for two.**"* Wendy: *"I agree with that."* Ben: *"Yeah, I agree with that, too."* **The value is already stored — `has_loss` is derived from it — so this is a display change on the expanded results view, not a new read.** → **O33-3**. (§2)
- **⚑⚑ THE CONTRACT GRAIN WAS BUILT BETWEEN 23:00 AND 08:47 AND ACCEPTED WITHOUT A CHANGE (D9, D10, D15, D16).** `32` O32-5 called it *"one respec, one migration and six doc edits."* It is all of that: `10c0f91` the `contract` table, `633e27e` the create form and deal card, `97e68bf` spec 017 rewritten, `f37da12` the FR doc, data model, PRD **and constitution v4.2.2**, `9c31987` the export pre-fill (closing `017` T034 and `32` O32-6(c)), `448d99a` and `b1259a2` the expiration carry-down. **Ben walked the whole thing on a fresh database and nothing in it was contested.** *"I've changed the wording… **I'm formally calling this contracts now.**"* **The one structural question the client raised was uniqueness (D12), and it is not a change to the grain.** (§4)
- **AND THE PREVIEW WAS MADE THIS TIME — `32` O32-4's LESSON IS NOW A REQUIREMENT.** `37ddfd7` adds `docs/ui_previews/submission_contracts.html` **before** the templates, and `017` **FR-021** now *requires* it in the spec text: *"A rendered preview covering the create form with three rows, an Active deal's contract table with mixed statuses, a deal with no contract, a Completed deal… **is approved before templates or routes are written.**"* **The waiver that cost one build on 9/18 has become a written gate.** (§1, §9)
- **⚑ THE TREATY-YEAR DEFAULT WAS BUILT AT 23:26 MONDAY AND REVERSED AT ~10:20 TUESDAY (D8).** `c94538e` *"Fill treaty year from the data vintage on the submission form"*; `017` **P-20** and **FR-006**; `submission_form.html:120-122` renders the hint *"Defaults to the data vintage year. Change it if the treaty year differs."*; **`FUNCTIONAL_REQUIREMENTS.md:49` was rewritten to describe it at 08:25 this morning.** **Wendy rejected the premise on their own calendar** — *"everything we work on from now to the end of the year, 98% of it is treaty year 2027. And then the things you work on in the spring are treaty year 2027"* — and neither user could reconstruct the logic their existing tool uses (Cheryl: *"**Cheng must have logic built in there**"*). **Cheryl closed it:** *"**Maybe we just leave it blank and force people to fill it in.**"* Ben: *"Yeah, OK, can do that."* **Eleven hours from ship to reversal, and the FR doc recorded the reversed behaviour two hours before the call.** → **O33-4**. (§6)
- **DATA VINTAGE BECOMES REQUIRED, AGAINST `017` P-19's "OPTIONAL" (D7).** Ben asked both fields. On client: Wendy, *"Probably not."* On data vintage: Wendy, *"**I sort of think that should be required.** Do you agree?"*; Cheryl, *"Agreed."* **Built as `submission.data_vintage DATE NULL` (`0001_initial.py:157`), specified optional in P-19, FR-022 and the export pre-fill.** **Small change, three requirements.** → **O33-4**. (§6)
- **THE "DEAL" GROUP BECOMES "PROGRAM" (D11).** Ben flagged his own label — *"We do use deal as a term often, but I'm not necessarily sure if that makes sense there."* Wendy: *"**We kind of call it program** — probably be more appropriate label there."* Cheryl: *"I think that's fine."* **`submission_head.html:59` renders `Deal`; the card, its CSS classes and the FR doc all use "deal" as the noun for the whole entity.** **Decide whether this is a label change or a vocabulary change before doing it.** → **O33-5**. (§4)
- **SEARCH STAYS SUBMISSION-GRAINED — BEN ARGUED FOR CONTRACT ROWS AND WENDY TALKED HIM OUT OF IT IN FOUR TURNS (D17, D18).** Ben: *"it's almost to me seeming like **the contract is the core, is the base item we should be searching against**… I would almost like to see one row per contract."* Wendy used the decision made ten minutes earlier: because a CRM ID is unique to one submission, *"if I put in a CRM ID, I already know **I'm only going to get one record back**"*, and clicking in shows the rest. Ben: *"That's true. That's a good point."* **Cheryl held the one real exception** — an in-force *contract* list, where one submission can hold a bound and an unbound CRM ID — **and Wendy put it outside this screen:** the CRM system is the record of bound status, it lands in the Workbench tables in the background, and an event-response query returns any submission holding an in-force CRM ID, because *"I want to find in-force **data**… probably looking more for in-force data, Cheryl, than in-force records."* Cheryl: *"**Then we don't need this to be anything more than at the submission level.**"* **This confirms 9/18 D27 and `017` P-11/FR-013, which already put the contract grain in the extract rather than the list.** (§7)
- **⚑ BUT WHAT BEN TOLD THE CLIENT THE INCEPTION COLUMN SHOWS IS NOT WHAT THE CODE SHOWS.** On the call: *"And then the inception date is **the inception date of the first CRM ID that was created** for the submission."* **In code it is the maximum:** `submission_service.py:467` — `row.latest_inception_date = max(inceptions)` — and `017` **FR-007** and **P-17** both specify *"the latest inception across the submission's contracts"*, with P-17 stating outright that **no contract is primary.** **The whole D17 exchange rested on Ben's description of the column, and the description was wrong.** It does not change the decision (Wendy's argument is about CRM-ID uniqueness, not about which date shows), but **the client has been shown a column and told the wrong rule for it twice — 9/18 D21 leaned on "the first one listed" too.** → **O33-6**. (§7)
- **THE EXPORT PRE-FILL IS BUILT AND IT SIDESTEPS "FIRST LISTED" ENTIRELY (D16).** `9c31987` gives the export form a Contract select over the submission's contracts, pre-selected when there is exactly one; choosing one fills CRM ID and treaty inception; client and data vintage pre-fill from the submission; everything stays editable. Wendy checked the override explicitly — *"We talked about the client ID being overwriteable"*; Ben, *"Yeah, yes, and it is."* **9/18 D21's "pull it from the first one listed" is superseded by asking the analyst, which is `017` P-17's stated policy and a better answer than the convention.** **`32` O32-6(c) closes; `017` T034 closes.** (§4)
- **ANIL'S TWO INTEGRATION IDEAS: ONE BANKED, ONE DECLINED ON TIMING (D19, D20).** Having read the submission URL, he pointed out that a Power BI dashboard over the same SQL Server table can construct clickable links into the Workbench — *"you can navigate from a Power BI dashboard directly to your workbench."* Wendy welcomed the direction (*"we're in the middle of a CRM rewrite… that's the direction we're going for things is more integration"*). **His second — an API route resolving a CRM ID to its one submission, so the CRM can hold static links — is now trivially implementable under D12 and was declined purely on timing:** *"we don't use Salesforce, we use Dynamics, but I get your point… **It's all being replaced. It's all being rewritten.**"* Ben: *"It's a nice idea, though."* → **O33-7**. (§8)
- **⚑⚑ AND THE PROCESS FINDING OF THE SESSION IS NOT IN THE SESSION.** `017`'s decision table now carries `32` O32-4's two columns — *derived from* and *confirmed with the client on* — which is the fix `31` asked for and `32` repeated. **It immediately shows what it was built to show:** **P-03, P-06, P-10, P-11, P-16, P-17, P-18 and P-20 have the confirmation column blank**, and **ten rows cite their derivation as *"user 2026-09-21"* — a client session on Monday September 21 for which there is no transcript in `CIC/transcripts/`, no minutes in `CIC/minutes/`, and no design note in this folder.** `specs/014-results-export/` **P-22** and **P-27** cite the same date for the export-screen decisions. **The traceability column works and the thing it is pointing at has no record behind it.** → **O33-8**. (§1)

**The shape of the reconciliation this session forces.** `32` said the cost of the 9/18 reversal was bounded only because the migration is edited in place, and that the next one would not be. **There was no next one.** The contract grain was rebuilt overnight and the client confirmed every part of it; the treaty picker's filter went in at the point the note argued for and the constitution problem never materialised; the preview gate was honoured and then written into the spec as a requirement. **On the three things `32` could measure, the project did the measured thing.** What it did not do is stop building ahead of the record: **the Monday session that authorised ten requirements left nothing behind, the FR doc was amended at 08:25 to describe a treaty-year rule the 10:20 call removed, and a uniqueness constraint was relaxed in a migration comment on a citation that points at the wrong decision.** **None of these is a reversal; all three are the same failure `31` and `32` named — confidence recorded ahead of confirmation — expressed in the documentation rather than in the code.** The sequencing that follows is short and unusually cheap: **make the CRM ID globally unique** (**O33-1**) — one index, one service check, one error message with a link, and three requirement edits, all inside a table that is four days old; **decide what a tester sees when `has_loss` is absent** (**O33-2**) before the testers arrive, because fail-closed and Cheryl's diagnostic disagree; **drop the treaty-year default and make data vintage required** (**O33-4**), which is two form changes and four requirement edits; and **write up the 9/21 session or mark its decisions unconfirmed** (**O33-8**), because `017` is now the second spec in a row whose central rows cite a conversation nobody can read.

---

## 1. Method: the third overnight build, and the session with no record

**This note reconciles against two branches**, per `30` **O30-1**'s standing rule. `main` is at **`10e3c6a`** (PR #120, Anil's SQL connection-string fix, merged 9/21 13:05). The connected working tree is on `main`. Five feature worktrees exist, all outside the connected folder and all prunable:

| Branch | Head | Relevance |
|---|---|---|
| `014-results-export` | `988b38e` | merged to `main` 9/18 (PR #110) |
| **`016-ty-perspective-export`** | **`c2154d3`** | **the zero-loss filter and today's TY demo** |
| **`017-submission-data-and-search`** | **`b1259a2`** | **the contract grain and today's submission demo** |
| `121-dev-vs-prod-separation` | `10e3c6a` | at `main`; no commits of its own yet |
| `chore/production-deployment-required` | `aaf997f` | merged 9/17 (PR #116) |

**The timeline, all times as `git log` records them (UTC−05:00):**

| Time | Commit | What |
|---|---|---|
| **9/21 09:11–14:55** | `0801ec2`, `59739a1`, `227b15c` | **note `32` committed** (three times, under two filenames) |
| **9/21 13:44** | **`9b703b5`** | **the zero-loss filter** — *"Hide treaties that took no TY loss from the export cart"*, citing *"Design note 32 D8-D11"* |
| **9/21 15:01** | `1df4761` | **irp-integration pinned to 0.10.0rc1** for the treaty-scoped stats read |
| **9/21 15:19–16:09** | `d641bec`, `0909735`, `98bda94` | test tidying; **the zero-loss click-through recorded** (quickstart Story 1 step 8, Story 3 step 1) |
| **9/21 17:43–23:30** | `2b762ec`, `7afc437`, `c2154d3` | irp-integration upgrade merged; **Origin reads RMS or RDM, Engine beside it, HD refused on export screens**; the **2026-09-21 export-screen decisions recorded in spec 014** |
| **9/21 23:00** | `37ddfd7`, `10c0f91`, `633e27e`, `97e68bf` | **the contract grain** — the approved preview, the `contract` table, the create form and deal card, spec 017 rewritten |
| **9/21 23:26–23:36** | `c94538e`, `d5c5d0d`, `9c31987` | **the treaty-year default built**; Active-gated controls refresh; **the export pre-fill (T034)** |
| **9/22 08:25** | **`f37da12`**, `448d99a` | **the FR doc, data model, PRD and constitution v4.2.2**; expiration carry-down on the create form |
| **9/22 08:47** | **`b1259a2`** | expiration carry-down in the deal card |
| **~10:14–10:54 ET** | — | **the design session** |

**Three observations.**

**First, `32`'s pre-call list was cleared again, and this is the third consecutive time.** O32-2 (the filter), O32-5 (the grain), O32-6(a) and (c) (the view and the pre-fill), O32-8 (the label) and O32-9 (the expiration default) were all built before the call. **Two of them — the filter and the grain — were the two largest items in the note.**

**Second, the preview gate held, and then hardened.** `32` O32-4 recorded that waiving `docs/UI_WORKFLOW.md` rule 1 on 9/18 cost one build inside one day. **This time the preview came first** (`37ddfd7`, 23:00, before `633e27e`'s templates in the same minute-stamped batch), and **`017` FR-021 now writes the gate into the requirement itself**, naming the five states the preview must cover. **That is the cheapest possible correction and it was made.** T054's completion note records the order explicitly: *"T059–T060 done 2026-09-21 **after the preview was approved**."*

**Third — and this is the finding — `017`'s decision table now carries the split column `31` O31-1 and `32` O32-4 asked for, and the first thing it reveals is a hole.** The table's two rightmost columns are *derived from* and a confirmation date. **Eight of twenty P-rows have the confirmation column blank:** P-03 (the contract dates), P-06 (the export pre-fill), P-10 (the filter cap and CRM matching), P-11 (the extract grain), P-16 (the create form and within-submission uniqueness), P-17 (no primary contract, the sort), P-18 (contract-level filter evaluation) and P-20 (the treaty year). **And ten rows cite *"user 2026-09-21"* as their derivation** — P-02, P-03, P-05, P-06, P-14, P-15, P-16, P-17, P-18, P-19 — **as do `014` P-22 and P-27.**

**There is no record of 2026-09-21.** `CIC/transcripts/` runs `… 9-18-26.vtt` then `… 9-22-26.vtt`; `CIC/minutes/` runs `…9-18-26.md` then nothing; this folder ends at `32`. **`32` §12 predicted the session — *"Mon 21 Sep: Ben meets Cheryl in the morning for regular demo work"* — so it almost certainly happened and was probably productive.** But twelve approved requirements across two specs now rest on a conversation that left no artefact, and **four of today's decisions (D11 the label, D12 uniqueness, D7 data vintage, D8 the treaty year) touch rows derived from it.** → **O33-8**.

---

## 2. The zero-loss filter, built where `32` said to build it (D1–D6)

### 2.1 The mechanism, and the warning that did not come true

**`32` O32-2 was the note's most specific technical intervention.** Ben had said on 9/18 *"I'm going to call stats"*, `32` traced that to `get_analysis_stats` (`irp_gateway.py:1319-1325`), showed that **Article 11's interface contract bars result-retrieval `get_*` from the web layer** and that the v4.1.0 carve-out covers `get_analysis_metadata` alone, and argued for a different place:

> *"The treaty list the cart renders is **not** live: it is `describe_run`'s `resolved.treaties`, captured when the analysis's run details are read and stored on the analysis. **Compute the has-loss flag at the same point and store it beside the treaty.**"*

**`9b703b5` does that.** `app/workers/analysis_jobs.py:437-447`, inside `_retrieve_analysis_results_body`:

```python
for treaty in treaties:
    try:
        rows = irp_gateway.get_analysis_stats(
            analysis_id=int(row["irp_id"]), perspective_code=TY,
            exposure_resource_id=int(treaty["treaty_id"]),
            exposure_resource_type="TREATY")
    except Exception as exc:  # noqa: BLE001 — no partial write
        return runtime.JobResult.fail(
            f"treaty loss read failed for {treaty['treaty_number']}: {exc}")
    treaty["has_loss"] = bool(rows)
```

and `export_service.py:297-307` reads the stored flag, deduping by `(number, name)` so a group keeps a treaty when any member copy took loss:

```python
treaties = list({(t.get("treaty_number"), t.get("treaty_name")): t
                 for t in loss_results.get("treaties") or []
                 if t.get("has_loss")}.values())
if treaties:
    perspectives.append(TY)
```

**`016` T-18 states the constraint in the spec rather than leaving it implicit:** *"The cart reads the stored flag and **never calls stats (Article 11)**."* **The constitution was not amended for this and did not need to be.** **`32` O32-2's first and largest concern closes.**

**Its sub-question (a) — whether the wheel accepts a TY stats call — resolved in the affirmative, with a cost.** The read is scoped `exposureResourceType=TREATY`, which `irp-integration 0.9.0` hard-codes to `PORTFOLIO`; **`0.10.0rc1` is required and is a TestPyPI pre-release** (`1df4761`; plan §New dependencies). The keyword was *"validated by hand 2026-09-21"* per the commit message, and the quickstart click-through was run the same afternoon (`98bda94`). **The `tests/irp` sandbox check (plan §IRP sandbox, T-18) is still unrun.** → **O33-2**.

### 2.2 What the call added: verification, and one screen earlier

**D1–D3 — the filter demoed, and checked against the source.** Ben's framing was the 9/18 defect closed: *"The biggest issue from Friday was… we could export treaties that don't have losses. **So, I've implemented that validation.**"* He then did what `32` §5.4 said the requirement lacked — outward verification. Two analyses, each run with three treaties, each offered one TY option; both opened in Risk Modeler's treaty-losses view with all three ticked; only the quota share treaty returned losses. *"**That's exactly what we see here.**"* A group (42550) with seven underlying treaties offered two: *"This actually does the same logic that I do, and only shows me the two options… **Same logic for the groups.**"*

**D2 — Ben described the mechanism to the client, and described it correctly.** Wendy asked for it before accepting: *"do you know what the logic is?"* Ben: *"I'm saying give me the stats endpoint, which is… where I get pure premium value from, the AAL from… And if it has loss, it returns values… and if it doesn't, **it just returns empty**"* — and then, unprompted, the part that matters here: *"I'm actually doing this at the time of syncing the results data into the workbench. So if we imported an analysis or if we executed an analysis, **I'm not doing it live on the screen**, but I'm calling stats endpoint during that time. **And I'm saving whether or not this treaty took loss.**"* Wendy: *"Okay, thank you."* **That is the only architectural question a client has asked in this series, and the answer was the right one.**

**D6 — Cheryl re-derived the display semantics herself and left the boundary where `32` D11 put it.** *"I'm assuming that the NA versus not seeing is about the treaties attaching or not attaching at all… if they don't attach at all, then you don't get any columns for it. **If it attaches, but you get 0 loss, then you probably get that NA column, which is fine.**"* What she needs is not an explanation but a verdict she can form: *"we just need to know, like, **is it expected that there isn't any loss or not?** Because if a treaty didn't attach and I expected it to attach, that's a problem. I gotta go back and fix it… If I was not expecting it to take loss, then that's fine."* And going into Risk Modeler to settle it *"is not, to me, a big deal."* Wendy added the workflow guard: *"**I personally would not export results without looking to see that results were generated.**"*

**D4 — and she declined the fallback design on that basis.** Ben: *"Would you rather see the full list of treaties here without the option to select the ones that don't have loss, so that that's more clear?"* Cheryl: *"**No.**"* Her argument is that the expanded results view, before the export button, is where the looking happens: *"before I even say export, I can look at that and say, oh, there's seven treaties. Okay, great. And you can look at pre-cat… **I can pick working excess, I can pick quota shares. So if I pick working excess, and it's like, oh, gee, there's no losses there. Probably should have some losses there.**"*

**D5 — which produces the one new requirement of this half: the loss figure per treaty, one screen earlier.** Ben proposed it, knowing the value is already in hand: *"We can show some sort of loss metric for these treaties, because I'm saving that to be able to validate in the next screen, in the export screen. So **I could make it more clear which of these treaties have losses.** We want them at the individual treaty level in this expanded view. Something like an AAL value, at least."* Cheryl: *"I think putting the **pure premium or the average annual loss** in here would be helpful… **It's fast to you look right away down the list and be like, OK, only I only have loss for two.**"* Wendy: *"I agree with that."* Ben: *"Yeah, I agree with that, too."*

**⚑ Note what this costs and what it does not.** `has_loss` is `bool(rows)` — **the flag is stored, the figure is not.** Showing pure premium or AAL per treaty means either widening what `build_loss_results_extract` keeps from the same stats rows (free, worker-side, same call) or a second read (not free, and web-side would be the Article 11 problem again). **The first is obviously right and is a one-line change at `analysis_jobs.py:447`; make it deliberately rather than discovering it at build time.** → **O33-3**.

**Two loose ends from the same stretch.** Ben's output profile *"only gets gross pre-cat and ground up results"*, so he could not exercise the working excess and quota share perspectives Cheryl was clicking through — *"my output profile doesn't produce them… I'll see what that looks like for these examples too."* And showing which treaties an analysis was **run** against on the results screen (9/18 D3, built) is being extended on a separate branch — *"I'm actually doing this in a separate branch, taking it up separately from this branch, because **this is getting a little bit large**."*

---

## 3. ⚑⚑ The backfill gap is now a testing risk

**`016` T-13, approved 2026-09-21:**

> *"An analysis whose results were retrieved before this release has no `treaties` key, or entries without `has_loss` (retrieved before 2026-09-21), and **is offered no TY** until its results are retrieved again: an entry without the key reads as no loss. **No backfill, the dev database is rebuilt.** — Approved | Pre-cutover rule (no backwards compatibility)"*

and the code comment at `export_service.py:301-302` repeats it: *"An entry without `has_loss` (retrieved before the flag existed) reads as no loss."*

**As a pre-cutover rule this is correct and cheap.** As a state the app is about to be tested in, it is a trap, for three reasons that compound:

1. **`32` O32-2(c) argued the other way and gave the reason.** *"Fail open with a marker is the answer Cheryl's reasoning implies, since a silently hidden treaty is indistinguishable from a zero-loss one."* **T-13 chose fail-closed with no marker.** The two are not reconcilable by argument — they are reconcilable by making the unknown state visible, which neither does.
2. **Cheryl's diagnostic makes the silence actively misleading.** Her whole acceptance of D4 rests on *"**If I don't see it in the list, that's already a flag for me.** That if I expected to see it in the list and I didn't see it in the list, **that something went wrong for me**"* (9/18 D11). **A tester on a pre-9/21 analysis gets exactly that signal, from a cause that has nothing to do with their modelling.**
3. **`31` O31-11 is the same gap one level down and is still open.** Treaties were only recorded from 9/16, so pre-existing analyses have no treaty list at all — **and `32` predicted this exact compounding**: *"a has-loss flag stored the same way inherits the same backfill gap."*

**The pre-cutover premise needs one check, not a redesign.** If CIC's environment starts from an empty database at cutover, T-13 holds and nothing is owed. **If testers exercise the deployed build against analyses retrieved before 2026-09-21, the premise is already false**, and `32` D31/D32 put testers on it this week. → **O33-2**.

**And two fragilities arrived with the read.** `analysis_jobs.py:444-446` fails the whole job on any single treaty's stats read — *"no partial write"* — so **results retrieval for an analysis with a dozen applied treaties now has thirteen ways to fail instead of one**, each of them a live Risk Modeler call. And the read needs **`irp-integration 0.10.0rc1` from TestPyPI**. **A release candidate on a test index, on the critical path of results retrieval, in the week of tester onboarding, is worth naming out loud to whoever owns the deployment.** Ben saw a retrieval-side error during the demo and deferred it: *"This looks like a nice technical error that we can retry successfully. **I need to check that.**"* → **O33-2**.

---

## 4. The contract grain, built overnight and accepted whole (D9, D10, D15, D16)

**`32` O32-5 scoped it as one respec, one migration and six doc edits. That is what landed.**

| What `32` O32-5 asked | What `b1259a2` has |
|---|---|
| `submission.deal_status_code` → the contract | **`contract.contract_status_code`** (`0001_initial.py:195-196`), FK to **`contract_status_kind`** (`:133-142`, seeded Won / Lost / In Process at `:1137-1142`). The kind table survived the move, renamed. |
| `submission.treaty_type_code` → the contract | **`contract.treaty_type_code NOT NULL`** (`:192`), FK `:204`. Gone from `submission`. |
| `submission.inception_date` / `.expiration_date` → deleted | **Both gone.** `contract.inception_date NOT NULL` and **`contract.expiration_date NOT NULL`** (`:193-194`) — **expiration required, per 9/18 D15, closing `32` O32-9.** |
| New `submission` data-vintage date | **`submission.data_vintage DATE NULL`** (`:157`) — **nullable; D7 makes it required.** See §6. |
| `v_submission_crm_id` rewritten | **Replaced by `v_contract`** (`:215-224`), one row per contract joined to its submission. **`32` O32-6(a) closes.** |
| `ix_submission_list_order` re-keyed | **Deleted.** The list's default order is now *"the latest contract inception descending, then name"* (`017` **FR-007**, **P-17**), computed over the correlated contract rows rather than indexed on a submission column. **`32` O32-6(b) closes by removal — the performance question it raised is unanswered rather than resolved.** → **O33-6** |
| `017` T034, the export pre-fill | **`9c31987`, closing T034 and `32` O32-6(c).** See D16 below. |
| FR doc, `DATA_MODEL.md`, `PRD.md` | **`f37da12`** — FR doc lines 44–63, 72, 115–116, 119 and the export pre-fill row; `DATA_MODEL.md` §4; `PRD.md` §7.2a. |
| *(not asked)* | **`.specify/memory/constitution.md` v4.2.2** — Article 4's in-place status list names `contract.contract_status_code`; no rule change; changelog cites *"note 32 D17"*. **Correct citation, correctly scoped as a PATCH.** |

**D9 / D10 — what the client saw, and did not contest.** Ben walked a fresh database end to end: *"I've changed the wording. You should just call this CRM IDs — **I'm formally calling this contracts now.** Contracts have a CRM ID. They have a treaty type of the list of treaty types. They have an inception date. When we choose an inception date, **that expiration date gets auto-populated to one year minus one day.** And they have three status options."* Cheryl checked cardinality on the creation screen — *"is that limited to just one on the initial screen and then here's where you add more, or you can add more right away?"* — and Ben confirmed several at creation. **`017` FR-004 and P-16 as built; nothing was challenged.**

**D11 — the one label change.** Ben flagged his own: *"The submission attributes, which I've added as a header called **the deal**. I don't really know if that lines up. We do use deal as a term often, but I'm not necessarily sure if that makes sense there."* Wendy: *"**We kind of call it program** — probably be more appropriate label there."* Cheryl: *"I think that's fine."* **`submission_head.html:59` renders `<span class="deal-group__label">Deal</span>`, and "deal" is the noun throughout — `deal-card`, `deal-field`, `deal_field()`, `#deal-head`, `017` P-14 ("one deal card"), FR-021, and the FR doc's "the deal's shared-drive folder".** **The ask is one group label. Whether the vocabulary follows it is a separate decision and should not be made by a find-and-replace.** → **O33-5**.

**D15 — the remove control stays as it is.** Wendy missed it, then defended it: *"I get why they're hidden, 'cause you don't wanna accidentally click on it either… **Nope, that's good.**"* Ben had offered to change it — *"probably should make these not hidden."* **`contract_table.html` renders the row's Remove as an `icon-btn icon-btn--xs` with `title="Remove"` and `aria-label="Remove {{ c.crm_id }}"`; the labelling is already right, so this is a CSS decision and the client has declined to ask for it.** No requirement change.

**D16 — the export pre-fill, built and accepted.** `9c31987` gives the form a Contract select over `list_contracts` (option data attributes `data-crm-id`, `data-inception`, pre-selected when there is exactly one), with client and data vintage pre-filled from the submission and everything editable. Ben demoed it with two contracts on deliberately different inception dates. Wendy checked the one thing she cared about: *"We talked about the client ID being overwriteable"*; Ben, *"Yeah, yes, and it is."* Ben's summary — *"**All of this is overrideable**"* — and his own close: *"I don't think I have any other callouts for this screen and for export. **Those were all asks from Friday.**"*

**⚑ And this quietly retires 9/18 D21.** That decision said the export's inception should *"pull it from the first one listed"* on Wendy's cat-treaty-first convention, and `32` O32-6 warned that two decisions in one day were resting on that convention. **`017` P-17 instead states that no contract is primary and that *"any place that needs one contract's value asks the analyst or aggregates"*, and the built form asks.** **That is the better answer and it removes the convention from the critical path — but nobody told the client the convention had stopped being load-bearing, and Ben re-invoked it on this very call** (*"You'd always enter the cat treaty first or something like that, right?"* — Wendy: *"That's the intent, but people do things not always the same way"*). See §7 and **O33-6**.

---

## 5. ⚑⚑⚑ The CRM ID must be globally unique — and the migration says it is not (D12, D13, D14)

### 5.1 What happened

Ben raised it as an open question against the model he had built nine hours earlier:

> **Ben:** *"I have a question. **There's nothing blocking us from using the same CRM ID across multiple submissions** with different details."*
> **Cheryl:** *"**That should not be allowed.** Yeah, that should not be allowed. **That CRM ID is unique**, so yeah."*

He then put the consequence to them as the architectural question it actually was — *"Who is the owner of the CRM ID? … The treaty type changes, inception or expiration date changes, contract status changes, which will definitely change. Should that be editable anywhere? Or should the first submission that uses this contract be able to edit it, or **do we need to manage contracts independently somehow in a central location rather than just as a subsidiary of submissions? I'm almost leaning towards the latter. Contracts have become a very first class object in the application.**"*

**Cheryl's hesitation was about multi-owner programs and Wendy dissolved it with existing practice.** Cheryl: *"we had talked about you do one program with multiple contracts, in which case we could potentially have different owners. **I might be working on the cat and Wendy might be working on the per risk. That does happen.**"* Wendy: *"I think in those cases you would set up **two entirely separate submissions because they have different client IDs in our system**… Or even if you're working on both of them, you'd be referencing the same EDM and RDM, but you'd have separate CRM IDs."* Cheryl, twice: *"Yep, yep, you're right."*

**And Wendy's answer to the ownership question needs no registry at all:**

> *"The first one in — I put in a CRM ID, then that's the unique CRM ID that you're checking against in your database, right? **Like there doesn't have to be any other sort of master list.** Like if I try to enter in a CRM ID and it says you can't do that because somebody already used it, I'd go looking for that submission, go figure out why somebody else is using my CRM ID. **So I don't think that there's any more logic than that required.**"*

Asked directly whether submissions should ever share a CRM ID — *"Should submissions be able to share CRM IDs at all? Which they currently can, because it was kind of just a tag, but it's evolving, right?"* — **neither could construct a case.** Wendy: *"I'm trying to think of a scenario where that would need to happen, and I can't come up with one."* Cheryl: *"No, I don't think so."*

**D13 — so contracts stay inside their submission.** Ben: *"**CRM ID belongs to 1 submission only.** Editing its details is in the context of that one submission. So you can't have somebody editing your CRM ID in another submission. You won't have that scenario."* And his verdict on the architecture question he had been leaning the other way on: *"**Good, that makes it a lot easier then.**"*

**D14 — with one refinement.** *"When we go to add a CRM ID that already exists elsewhere, that should be blocked. **And it might be useful to even give you a link straight to that submission, so you can go check it out.**"* Wendy: *"Sure… **Oh, I agree. That's great.**"*

### 5.2 What is built, and the citation that authorised it

**`0001_initial.py:182-186`, written 9/21 at 23:00:**

> *"── contract (0..N per submission; one per CRM ID) ──*
> *The CRM ID is the contract (FR doc line 57). Treaty type, the term and the Won / Lost / In Process status live here, not on the submission (spec 017 P-02, P-03, P-15). **CRM ID uniqueness within a submission is the service's case-insensitive check; no global uniqueness (note 32 D30).**"*

**`contract.crm_id` is `NVARCHAR(255) NOT NULL` with no unique index of any kind** — not global, not per-submission; the only index on the table is `ix_contract_submission_id` (`:210`). **The check is in application code**, `submission_service.py:334-340`:

```python
if crm_id.casefold() in taken:
    raise ContractInvalid(
        f"{taken[crm_id.casefold()]} is already a contract on this submission.",
        index=index)
```

where `taken` is built per submission. **The requirements say the same thing three times:** `017` **FR-003** (*"a CRM ID (required, free text without format validation, **unique within its submission** case-insensitively)"*), **P-16** (*"A contract requires its CRM ID, unique within the submission case-insensitively"*), and **`FUNCTIONAL_REQUIREMENTS.md:57`** (*"plain text, unique within the submission"*), amended at 08:25 this morning.

**⚑ And the citation does not resolve.** *"note 32 D30"* — `32`'s decision IDs are the 9/18 minutes' D1–D32, and **D30 is *"Industry databases are handled with a dedicated-submission workaround this iteration, not a new flow."*** Nothing in `32`, in the 9/18 minutes, or in `30` or `31` addresses cross-submission CRM-ID reuse. `32` **O32-5** quotes FR line 57's *"Standard case is one; **multiple contracts can share one Submission**"* — which is the opposite proposition (many contracts per submission, not many submissions per contract) and is consistent with global uniqueness. **The most likely reading is a transposition while writing the comment at 23:00; the effect is that a constraint the client had never been asked about was relaxed on a reference to a decision about Japanese exposure databases.**

### 5.3 What it costs to fix

**Small, and smallest right now.** The `contract` table is four days old and the migration is still edited in place and rebuilt — `32`'s "one piece of luck" still holds. **What changes:**

- **A unique index on `contract.crm_id`**, case-insensitive (SQL Server's default collation is case-insensitive, so a plain unique index does it on MSSQL; the SQLite mirror needs `COLLATE NOCASE` or a computed lower-case column — `tests/iteration1_mirror.py` is the place this bites).
- **`submission_service.py:334-340`** widens from a per-submission dict to a database lookup, and its message changes from *"is already a contract on this submission"* to one naming the other submission, **with a link (D14)**.
- **`017` FR-003, P-16** and **FR doc line 57** change *"within its submission"* to *"across the Workbench."*
- **A trimming rule is implied and unstated.** `017` **P-10** already trims and case-folds CRM IDs for *filtering*; **uniqueness should use the same normalisation or the filter and the constraint will disagree.**
- **⚑ One question nobody asked: what happens to existing data?** Pre-cutover the answer is "nothing, the database is rebuilt" (the T-13 precedent). **Say so explicitly, because unlike T-13 this one has a failure mode after cutover** — a duplicate created before the index exists blocks the migration that adds it.

**And one thing gets easier.** **Anil's CRM redirect route (D20) becomes trivially correct under global uniqueness** — *"find the submission for that CRM. **There should be only one.**"* It is declined on timing, not on feasibility. → **O33-1**, **O33-7**.

---

## 6. The treaty-year default, reversed eleven hours after it shipped; data vintage required (D7, D8)

**D8 — the reversal.** `c94538e`, 9/21 at 23:26, *"Fill treaty year from the data vintage on the submission form"*. `017` **P-20**: *"Treaty year stays on the submission; the form fills it from the data vintage and the server from the earliest contract inception when blank."* **FR-006** the same. `submission_form.html:120-122` renders the hint *"Defaults to the data vintage year. Change it if the treaty year differs."* **And `FUNCTIONAL_REQUIREMENTS.md:49` was rewritten to describe it at 08:25 on 9/22** — *"Treaty year defaults from the data vintage on the form and from the earliest contract inception on save, and stays editable. **Changed 9/22 (spec 017 P-20).**"*

**The call, ~10:20:**

> **Ben:** *"Treaty year at the submission level gets populated to the year of the data vintage automatically. You can change it."*
> **Wendy:** *"**I would change it.** Because… everything we work on from now to the end of the year, 98% of it is treaty year 2027. And then the things you work on in the spring are treaty year 2027… until you get back into the fall, then it's [2028]."*
> **Ben:** *"You don't want that default logic."*
> **Wendy:** *"So the logic is if it's in the fall… I would — I don't know, Cheryl, **do you want to put a default there or do you want to just force people to enter it?**"*
> **Cheryl:** *"we've had just a drop-down list in the workflow today… it does usually just default to generally the following calendar year. **There must be a logic. Cheng must have logic built in there.**… So I don't know. **Maybe we just leave it blank and force people to fill it in.**"*
> **Ben:** *"Yeah, OK, can do that."*

**⚑ Note what actually happened: the default is not wrong in mechanism, it is wrong in source.** The data vintage is an *as-of date for the exposure data*, and the treaty year is a *renewal season*; they coincide for part of the year and diverge for the rest, which is why Wendy's example has autumn-2026 work carrying treaty year 2027. **Cheryl's alternative — the following calendar year, with a cut-over date in late autumn — is a real rule that exists in CIC's current tool, and neither user could state it.** So the decision is to drop the default, not to replace it. **`P-20`'s server-side half — filling from the earliest contract inception when the field is left blank — was not discussed at all**, and it has the same defect for the same reason. **Drop both halves, or ask Cheng for the rule; do not keep the inception-derived one on the grounds that only the vintage-derived one was named.** → **O33-4**.

**This also reverses further back than `017`.** FR line 49's change note records that P-20 *"Reverses design note 08 §2.1 D4, which carries the reason and the open re-confirmation with CIC."* **D8 now reverses the reversal**, so note 08's original position — no automatic derivation — stands again, four days after it was overturned. **Record the round trip in the FR doc rather than silently editing line 49 a third time.**

**D7 — data vintage becomes required.** Ben asked about both new fields in the same breath:

> **Ben:** *"We can pick a client. Client is not required. **Does this need to be required?**"* → **Wendy:** *"Probably not."*
> **Ben:** *"Data vintage as a submission level attribute exists."* → **Wendy:** *"**I sort of think that should be required.** Do you agree?"* → **Cheryl:** *"Agreed."* → **Ben:** *"Okay, that can be required."*

**Built optional in three places:** `submission.data_vintage DATE **nullable=True**` (`0001_initial.py:157`), `017` **P-19** (*"one **optional** date on the submission"*) and **FR-022** (*"an **optional** data vintage date"*). **The client field stays optional, which matches `017` P-04 and closes that half.**

**⚑ One consequence to check before making it NOT NULL:** the export's data vintage is **required** and pre-fills from the submission (`017` FR-011, FR-022; `014` FR-002). **Making the submission's copy required removes the "pre-fill from an empty source" case, which is a simplification** — but `d641bec` (9/21 15:19) is titled *"Pin the vintage default to empty in the Finish stop test"*, so **there is a test asserting the empty case.** Change the field, the requirement and that test together. → **O33-4**.

---

## 7. Search stays submission-grained — and the column the client was told about (D17, D18)

**Ben opened the topic against his own build.** *"**Searching is giving me a headache.** Searching was nice when we didn't care about contracts so much. But now we do."* His difficulty is real and is exactly what `017` FR-007 specifies: a submission row can show one contract's worth of detail, so the row carries the CRM IDs as *"first + N more"* on hover, the distinct treaty types the same way, and one inception date. *"I wouldn't classify that as necessarily super useful."* **His proposal: one row per contract.** *"it's almost to me seeming like **the contract is the core, is the base item we should be searching against** for this. And the results are going to be submissions, but I would almost like to see one row per contract so that I can see the full details and that I can properly search by inception date for any contract."*

**Wendy dismantled it with the decision made ten minutes earlier (D12).** *"I'm not sure you need to show one row per contract, because **if I'm searching for a CRM ID, we've already established that is unique to 1 submission.**… So if I put in a CRM ID, I already know **I'm only going to get one record back**, right? And I'm going to click into that submission and see that there's whatever, multiple contracts in there. **And that's okay. That's what we would expect.**"* Ben: *"That's true. That's a good point… Correct. Correct."*

**Cheryl held the exception and then released it.** *"it's really about the purpose of this searching field, right? So if the purpose is for me to narrow down the list so I can find the submission, go in and do my work, **what you have right now is fantastic**… **If the purpose is to create a list of in force contracts, then we do need to go back to the contract to the CRM ID level**, because within any one of these, you might have one that was bound and one that was not bound."*

**D18 — Wendy put that outside this screen, and gave the architecture for it.** *"we're going to be doing a lot of updating in the background tables… **We have a CRM. That's a system of record. That's where we track deals that are bound and not bound.** That will get updated behind the scenes in the SQL table, so that when I come in here… I want to find **in-force data**… probably looking more for in-force **data**, Cheryl, than in-force **records**. Like, if there's 10 CRM IDs and any one of them is in force, that submission is going to come back to me. And so, when an event response scenario, you're going to find yours that have any CRM ID in that submission that's in force."* Cheryl: *"Does this screen need to meet that need?… **Then we don't need this to be anything more than at the submission level. That's — I think then that's fine.**"* **And Wendy carried the same argument to the archive case:** *"if I'm looking for contracts that are five years old or have been expired for five years, **any individual CRM ID under that will also be expired**… because we just don't have contracts that last that long."*

**This confirms what is built.** `017` **P-18** / **FR-016** evaluate contract-level filters against *one* contract row together — the "Won Aggregate XOL beside an In Process Per Risk XOL" case (spec acceptance 4) — and `submission_filter_clauses` (`submission_service.py:917-931`) implements it as a single shared `EXISTS` over `contract`. **The contract grain lives in `017` FR-013 / P-11's `v_contract` extract, which is exactly where D18 says CIC's own SQL will read it, and in 9/18 D25's bulk update.** **9/18 D27 is reaffirmed; no requirement changes.**

**⚑ But the column Ben described is not the column that exists.** He told the room: *"And then the inception date is **the inception date of the first CRM ID that was created** for the submission."* **In code it is the maximum** — `submission_service.py:467`, `row.latest_inception_date = max(inceptions)` — and `017` **FR-007** (*"the latest inception across the submission's contracts"*) and **P-17** (*"The list's default sort is the latest contract inception descending… **No contract is primary**"*) both specify it that way. **Ben then built a chain of reasoning on the wrong description**, asking whether the "main contract" or "core contract" concept from Friday applied — *"You'd always enter the cat treaty first or something like that, right?"* — and Wendy answered about the convention (*"That's the intent, but people do things not always the same way… that's how we would direct people"*), which is the second time in three sessions that CIC has been asked to ratify a first-listed rule the code does not implement (9/18 D21 was the first, and §4 shows the export now asks instead).

**Nothing about D17 turns on it** — Wendy's argument is about uniqueness, not about which date appears — **but the client has now twice been shown behaviour described as "the first one" and twice agreed to reasoning built on it.** **Say "latest" the next time the column is on screen, and check whether latest is what they want**; P-17's confirmation column is blank. → **O33-6**.

**And the default-sort performance question `32` O32-6(b) raised is unanswered rather than resolved.** `ix_submission_list_order` is deleted — no index on `submission` survives except `assigned_analyst_id` and `cedant_name` — and the order is now a correlated aggregate in the `ORDER BY` itself (`submission_service.py:389`, `:412`):

```sql
ORDER BY COALESCE((SELECT MAX(c.inception_date) FROM contract c
                   WHERE c.submission_id = s.id), …) DESC, s.name
```

with only `ix_contract_submission_id` beneath it, and the same expression reused as the `inception` filter key (`:843`). *(Note in passing that insertion order does exist in the code — `:453` reads a submission's contracts `ORDER BY c.inserted_at, c.id` to render the table — so "the first one entered" is available; the list column simply does not use it.)* **`32` predicted this: *"Either the list sorts on a correlated contract date (not indexable the same way) or the submission keeps a denormalised copy."* The first was chosen without the trade-off being written down.** **The SQL Server tier is unrun (T056), so nobody has seen this sort against a server, and Ben is building a seeding script this week that will produce exactly the data to test it with.** → **O33-6**.

---

## 8. Power BI, the CRM redirect, and why both are cheap now (D19, D20)

**Anil's first contribution in five sessions, and both ideas are one-line consequences of the URL scheme.** Having asked Ben to click into a submission — *"I want to see what the URL looks like… so it has the resource ID, so submission slash submission ID is what gets us there"* — he made the general point: **the submission ID is on the SQL Server table, so a Power BI dashboard over the same table can construct the link and click through into the Workbench.** *"you can navigate from a Power BI dashboard directly to your workbench."* Wendy welcomed it: *"we're in the middle of a CRM rewrite, and so that's the direction we're going for things is more integration."*

**D20 — the second idea is now trivially correct and was declined on timing.** *"we could create an API route that redirects… **for the CRM ID.** All that does is find the submission for that CRM. **There should be only one.** If we can find one and only one submission for that CRM, it'll redirect to that. If there isn't more than one, which should not happen, or none, it returns an error."* **Under D12 the "should be only one" is a database constraint rather than an assumption** — the ambiguous case Anil had to write an error path for stops existing. Wendy: *"we don't use Salesforce, we use Dynamics, but I get your point… **I don't think there's anything that we would do today because it's all being replaced. It's all being rewritten.**… we're not investing any time there at all right now in the existing product."* Ben: *"It's a nice idea, though."* Anil offered to walk the new CRM team through it later.

**Record both, build neither.** **The Power BI link needs nothing from the Workbench at all** — it is a statement about a URL that already exists, and CIC can act on it whenever they like. **The redirect route is perhaps twenty lines and should be held until the CRM rewrite lands**, at which point D12 will have made it correct by construction. → **O33-7**.

---

## 9. Repo reconciliation

Unless a row says otherwise, **`016` at `c2154d3`** and **`017` at `b1259a2`** are the branches; `main` is `10e3c6a`. See §1.

| Piece | State after 9/22 |
|---|---|
| **Branch scope** | `main` at `10e3c6a` carries `014`, `015`, `101` and Anil's connection-string fix (PR #120). **`016` at `c2154d3` carries the zero-loss filter and the 9/21 export-screen changes; `017` at `b1259a2` carries the contract grain.** Neither is merged. `121-dev-vs-prod-separation` exists at `main` with no commits. |
| **`016` P-13 / FR-002 / T-18 (the zero-loss filter)** | **BUILT 9/21 13:44, DEMOED AND VERIFIED AGAINST RISK MODELER, ACCEPTED (D1–D3).** **`32` O32-2's main finding CLOSES and its recommendation was taken**: the read is in `retrieve_analysis_results` (`analysis_jobs.py:437-447`), the cart reads the stored flag (`export_service.py:297-307`), and **T-18 records the reason in the spec — *"never calls stats (Article 11)"*.** |
| **Constitution Article 11** | **NOT AMENDED AND NOT VIOLATED.** The carve-out `32` warned would be needed was not needed. |
| **`016` T-13 (the backfill rule)** | **⚑⚑ FAIL-CLOSED WITH NO MARKER, AGAINST `32` O32-2(c).** Pre-9/21 entries read as no loss; *"No backfill, the dev database is rebuilt"*, approved as a pre-cutover rule. **Testing starts this week (9/18 D31–D32). Verify the premise or add a marker.** **`31` O31-11 is the same gap for the treaty list itself and is still open.** |
| **`analysis_jobs.py:444-446`** | **⚑ N+1 FAILURE SURFACE.** One treaty stats read per applied treaty, any failure fails the whole results retrieval with `loss_results` untouched. |
| **`irp-integration 0.10.0rc1`** | **⚑ TESTPYPI PRE-RELEASE ON THE CRITICAL PATH.** Needed for `exposure_resource_type` on `get_stats`; `0.9.0` hard-codes `PORTFOLIO`. `make irp-testpypi`. **In the week of tester onboarding.** |
| **`016` plan §IRP sandbox / T-18, T-16** | **UNRUN.** `uv run pytest tests/irp --run-irp -k treaty` inside `linux-box` still owed. The quickstart click-through (Story 1 step 8, Story 3 step 1) was run 9/21 (`98bda94`). |
| **`016` FR-017 / O-04 (`_combine_treaty_rows`)** | **NOT RAISED. The combination logic and O-04 both survive** (`export_service.py:611` comment; FR-017 keeps *"the largest of the combined rows until O-04 is closed"*). **`31` O31-5's instruction — run the group sandbox check before removing anything — is neither executed nor reversed, fifth session running.** Cheng still owes the aggregation query. |
| **Pure premium / AAL per treaty (D5)** | **NEW, and nearly free.** `has_loss` is `bool(rows)` — **the figure the flag is derived from is discarded.** Keep it in the same worker read; do not add a second call and do not call stats from the form. |
| **`017` P-02 / FR-002 (contract status)** | **BUILT (`contract.contract_status_code`, `contract_status_kind` Won/Lost/In Process). `32` O32-5 CLOSES for this attribute.** |
| **`017` P-03 / FR-003 / FR-005 (the contract term)** | **BUILT.** Inception and expiration on the contract, **both NOT NULL**; expiration defaults to inception plus one year less a day; a new row carries down from the row above (`448d99a`, `b1259a2`). **9/18 D15, D20, D22 all built; `32` O32-9 CLOSES.** |
| **`017` P-15 (treaty type on the contract)** | **BUILT** (`contract.treaty_type_code NOT NULL`, FK to `treaty_type_kind`). **FR doc line 48 amended to *"Each contract has exactly one treaty type"*. `32` O32-5's FR:48 collision resolved.** |
| **`v_contract` (`0001_initial.py:215-224`)** | **BUILT, replacing `v_submission_crm_id`. `32` O32-6(a) CLOSES.** One row per contract with the submission's name, cedant, client, treaty year, data vintage and Modeling status. **`017` FR-013 / P-11; the surface 9/18 D25's bulk update writes through.** |
| **`ix_submission_list_order`** | **⚑ DELETED, not re-keyed.** The default order is a correlated *latest contract inception* with no supporting index. **`32` O32-6(b) closes by removal; the trade-off it named was not written down. SQL Server tier unrun.** |
| **`017` T034 / FR-011 (the export pre-fill)** | **BUILT AND ACCEPTED (D16)** — `9c31987`, a Contract select pre-selected at one contract, filling CRM ID and treaty inception; client and data vintage from the submission; all editable. **`32` O32-6(c) CLOSES. 9/18 D21's "first one listed" is superseded by asking the analyst (P-17).** |
| **`017` P-05 / FR-010 ("Client ID")** | **BUILT AND CONFIRMED ON SCREEN (Ben: *"I renamed it Client ID"*). `32` O32-8 CLOSES.** |
| **`017` P-04 (client optional)** | **CONFIRMED (D7).** Wendy: *"Probably not."* |
| **`017` P-19 / FR-022 (data vintage optional)** | **⚑ CHANGED BY D7 — required.** Built `nullable=True`. Three requirement edits plus the "empty vintage" test pinned by `d641bec`. |
| **`017` P-20 / FR-006 (the treaty-year default)** | **⚑⚑ REVERSED BY D8, eleven hours after `c94538e` shipped and two hours after `FUNCTIONAL_REQUIREMENTS.md:49` was rewritten to describe it.** Leave the field blank; **the server-side "earliest contract inception" half was not discussed and has the same defect.** Note 08 §2.1 D4's original position stands again. |
| **`017` FR-003 / P-16 / FR doc:57 (CRM-ID uniqueness)** | **⚑⚑⚑ CONTRADICTED BY D12 — must be global, is per-submission.** No unique index exists at all; the check is `submission_service.py:334-340`, per submission, case-folded. **The migration comment attributes this to *"note 32 D30"*, which is the industry-database decision — the citation does not resolve.** |
| **Duplicate-CRM-ID link-through (D14)** | **NOT BUILT.** The refusal message names the CRM ID and the current submission only. |
| **`017` P-14 / FR-021 (the deal card and the preview gate)** | **⚑ BUILT WITH THE PREVIEW FIRST** (`37ddfd7` before `633e27e`), **and the gate is now written into FR-021 itself**, naming five required states. **`32` O32-4's second half is discharged in the best possible way: the waiver's cost became a requirement.** |
| **The "Deal" group label (D11)** | **→ "Program".** `submission_head.html:59`. **"Deal" is also the noun for the entity throughout the CSS, macros, `017` P-14 and the FR doc — scope the change before making it.** |
| **`017` P-17 / FR-007 (no primary contract; latest inception)** | **BUILT; ⚑ MISDESCRIBED TO THE CLIENT AS "the first CRM ID that was created".** `submission_service.py:467` takes `max()`. **P-17's confirmation column is blank.** |
| **`017` P-18 / FR-016 (one contract satisfies all contract filters)** | **BUILT** (`submission_filter_clauses`, one shared `EXISTS`). **Confirmed in principle by D17–D18; P-18's confirmation column is blank.** |
| **`017` P-09 / FR-018 (in force)** | **BUILT on the contract, with the Won leg Cheryl found live on 9/18.** **D18 confirms the screen's scope: submission-grained, with CIC's own SQL reading `v_contract`.** |
| **`017` P-16 / FR doc:52 (required at creation)** | **ANSWERED — name and cedant only — and demonstrated rather than asked.** FR line 52 cites *"note 32 O32-7"*. **`32` said "ask it directly on Monday: it is one question." It was answered in the FR doc; the call showed a submission created with no contract and nobody objected. P-16's confirmation column is blank. Treat O32-7 as closed by demonstration, not by consultation.** |
| **`017` decision table** | **⚑ `32` O32-4's SPLIT COLUMN IMPLEMENTED — and eight of twenty rows have the confirmation column blank, and ten cite *"user 2026-09-21"*, a session with no transcript, no minutes and no note.** `014` P-22 and P-27 cite the same date. |
| **`.specify/memory/constitution.md`** | **v4.2.2, amended 2026-09-22 08:25.** Article 4's in-place status list names `contract.contract_status_code`; PATCH, no rule change, **changelog cites *"note 32 D17"* correctly.** |
| **`FUNCTIONAL_REQUIREMENTS.md`** | **AMENDED 9/22 08:25 (T063): lines 44–63, 72, 115–116, 119, 629.** **Line 57 promoted from aside to definition — *"the CRM ID is the contract"* — which is what `32` O32-5 asked for.** **Line 49 is already stale (D8); line 47's data-vintage sentence and line 57's uniqueness clause become stale under D7 and D12.** |
| **`DATA_MODEL.md`** | **§4 rewritten 9/22 (Submission, Contract, `contract_status_kind`, `v_contract`, the sort expression) with a changelog entry.** **Still no TY entry, no manifest, no staging tables** (`25` O25-1, `29` O29-7, `31`, `32`). |
| **`analysis_result_meta.perspective_code`** | **Still *"GR / RL / WX / QS / GU"*** against a live set of `GU, GR, RL, RP, TY`. **Unchanged for four notes.** |
| **`014` P-22 / P-27 / Origin (D2–D4, D10–D12 of `31`)** | **BUILT 9/21** (`7afc437`: Origin reads RMS or RDM, Engine beside it, columns follow the results grid, timestamps last, HD refused on export screens). **`31` O31-6 CLOSES in the export table; the portfolio/template columns half (9/18 D12, `32` O32-14) was not raised today.** |
| **Test tiers** | **`017`: unit tier 2,020 passed (baseline 2,010). SQL Server tier NOT RUN (T056 unverified; rebuild needed).** `016`: sandbox check unrun. |
| **Deployment / testing** | **NOT DISCUSSED as an item.** Ben: *"I'm gonna be working on that stuff, and deployment related activities this afternoon."* Wendy: *"I will try to ping Nagi while we're in that call… **The more we poke, just keep poking.**"* **No status, no date, no tester session scheduled — four days after 9/18 D31–D32 put testers on the calendar.** |
| **Accumulation / tagging / Analyze Re / October 1 / industry databases / Risk Modeler search** | **NOT RAISED.** |
| **`CONTEXT.md`, `docs/adr/`** | **Both still absent.** |

---

## 10. Schedule

**Ben set Wednesday's agenda himself, in the last ninety seconds.**

> *"tomorrow afternoon I have a call. What I want to show you there is **the ask for bulk updating submissions or CRM IDs with a status** — that SQL query that you'd like to run manually. I want to demo that mechanism. And I'm also building **a dev convenience script to populate a ton of submissions and contracts** so that we can do more comprehensive testing of the searching… **So I'll have a ton of data and we can really dive deep on if this searching is working properly or not.** So that's what I'd like to show tomorrow, and our, of course, asks from today."*

| Day | Note |
|---|---|
| **Tue 22 Sep (afternoon)** | Ben on deployment-related activities. **Wendy pinging Nagi from her own call**; both to share updates. |
| **Wed 23 Sep (afternoon)** | **The bulk status update (9/18 D25) demoed**, the seeding script, and today's asks. |

**⚑ The seeding script is the right tool arriving at the right time for a question nobody asked it to answer.** §7 leaves the submissions list sorting on a correlated *latest contract inception* with no index, against an unrun SQL Server tier. **A script that produces "a ton of data" is exactly what tests that, and Ben is building it for search correctness. Point it at the sort as well.** → **O33-6**.

**Status: not restated, eighth consecutive note. Yellow stands from `23` D1.** **Ross, Randy and Cheng were not named. Nagi was named once, as someone Wendy would chase.** **`29` O29-14 — October 1 — is now seven working days out and has gone unmentioned for seven sessions.**

**Tone.** Brisk and settled, and the shortest session of the phase by half. **Its best moment is the ninety seconds in which Ben asks whether contracts need a central home, Cheryl answers "that should not be allowed" before he finishes the question, Wendy fails to invent a counter-example, and a schema constraint is decided — the second consecutive session in which a data-model decision is made by the client, in the client's vocabulary, in under two minutes.** Ben's *"Good, that makes it a lot easier then"* is the same note as 9/18's *"I don't mind as long as we get it right"*, and it is worth keeping for the same reason.

---

## 11. Carried-forward (not advanced this session)

- **`32` O32-3 — cap the three expanded-view lists and show members for imported groups.** **Not raised**, except that D5 adds a fourth thing to the same treaty list (the loss figure), so **build them together**. `analysis_results_inline.html:38-40`, `:42-46`, `:48-52` are all still uncapped.
- **`32` O32-10 — searching Risk Modeler directly; no RDM sync.** Not raised. Still its own future spec.
- **`32` O32-11 — the two library filters `017` cannot express (unlinked, and excluding industry sets), and the tagging collision underneath.** Not raised. **`017`'s out-of-scope list now names them explicitly as held (*"note 32 D28–D30, held"*), which is the right disposition.**
- **`32` O32-12 — the industry databases and the market-share workflow.** Not raised.
- **`32` O32-14 / `31` O31-6 — portfolio and template columns for imported analyses; grid consistency.** **The export-table half shipped 9/21 (`7afc437`); the portfolio/template half was not raised.**
- **`31` O31-5 / `016` O-04 — the combination logic and the exposure value.** **Not raised, fifth session.** The logic and FR-017 both survive; **the group sandbox check is still unrun and Cheng still owes the aggregation query.**
- **`31` O31-11 — pre-existing analyses have no recorded treaties.** **Not raised, and now the parent of O33-2's backfill problem.**
- **`31` O31-8 — the mixed-engine rule.** Not raised; **partially overtaken by `7afc437`'s "HD refused on export screens".** Check whether it is now satisfied.
- **`31` O31-9 — the administrative filter on the model-version list.** Not raised.
- **`29` O29-8 / `31` O31-12 — the Cheng batch, five items.** Not chased.
- **`29` O29-6 — the TY trade-off document.** Not raised.
- **`29` O29-7 / `25` O25-1 — the treaty-grained manifest and staging tables in `DATA_MODEL.md`.** **Still absent, and `DATA_MODEL.md` §4 was rewritten today without adding them.**
- **`29` O29-9 / O29-10 — tagging and the spec 012 FR-017 collision.** **Fifth consecutive session unraised.**
- **`29` O29-1 — Analyze Re.** Not named.
- **`29` O29-14 — October 1.** **Seven working days out, unmentioned for seven sessions.**
- **`30` O30-13 — accumulation.** Not mentioned. Still *Not implemented | In scope*, still never discussed in any session.
- **`18` D13 — retention and archiving policy.** Parked; **9/18 D26's expired-deal search still depends on it and was reaffirmed today by Wendy's "any individual CRM ID under that will also be expired."**

---

## 12. Open questions & follow-ups

- **O33-1** — **⚑⚑⚑ Make the CRM ID globally unique, block re-use with a link to the owning submission, and fix the citation that relaxed it.** D12, D13, D14. **The decision:** Ben — *"There's nothing blocking us from using the same CRM ID across multiple submissions with different details"*; Cheryl — *"**That should not be allowed.** Yeah, that should not be allowed. **That CRM ID is unique.**"* Asked whether submissions should ever share one, **neither user could construct a case** (Wendy: *"I can't come up with one"*; Cheryl: *"No, I don't think so"*). **What is built is the opposite, in writing:** `0001_initial.py:185-186` — *"CRM ID uniqueness within a submission is the service's case-insensitive check; **no global uniqueness (note 32 D30)**"* — with **no unique index on `contract.crm_id` at all**, the check living in `submission_service.py:334-340` (*"is already a contract on this submission"*), and `017` **FR-003**, **P-16** and **FR doc line 57** all fixing uniqueness at *within its submission*. **⚑ The citation does not resolve: `32` D30 is the industry-database workaround.** No decision anywhere in `30`, `31`, `32` or the 9/18 minutes permits cross-submission reuse. **What to change:** a unique index on `contract.crm_id` (case-insensitive — plain on MSSQL, `COLLATE NOCASE` or a computed column in the SQLite mirror, `tests/iteration1_mirror.py`); the service check widens to a database lookup; **the refusal message names the other submission and links to it (D14)** — Ben, *"it might be useful to even give you a link straight to that submission"*, Wendy, *"Oh, I agree. That's great."*; FR-003, P-16 and FR doc line 57 change *"within its submission"* to *"across the Workbench"*; **and the normalisation must match `017` P-10's filter normalisation (trimmed, case-folded) or the constraint and the filter will disagree.** **One question nobody asked: what happens to duplicates created before the index exists.** Pre-cutover the T-13 precedent answers it (the database is rebuilt) — **say so explicitly, because this one has a post-cutover failure mode T-13 does not.** **And record the architectural consequence, because it is the reason the decision matters:** Ben was *"almost leaning towards"* a central contract registry, and uniqueness removed the need — *"CRM ID belongs to 1 submission only. Editing its details is in the context of that one submission… **Good, that makes it a lot easier then.**"* (§5) *Ben — `0001_initial.py`, `submission_service.py`, `017` FR-003/P-16, FR doc line 57.*
- **O33-2** — **⚑⚑ Decide what a tester sees when `has_loss` is absent, before the testers arrive — and name the two fragilities that arrived with the read.** D1, D2, `016` T-13. **The good news first, and it is the largest item in this note: `32` O32-2's recommendation was taken.** The flag is computed worker-side in `retrieve_analysis_results` (`analysis_jobs.py:437-447`) and the cart reads stored data (`export_service.py:297-307`); **T-18 writes the reason into the spec — *"The cart reads the stored flag and never calls stats (Article 11)"* — and the constitution needed no amendment.** Ben described the same shape to the client unprompted. **The open part:** T-13 makes an entry without the flag read as **no loss**, with **no backfill** and no marker, justified as a *"pre-cutover rule."* **`32` O32-2(c) argued for fail-open with a marker, on Cheryl's own diagnostic** — *"If I don't see it in the list, that's already a flag for me… that something went wrong for me"* — **so a tester on a pre-2026-09-21 analysis will read an infrastructure gap as a modelling error.** **9/18 D31–D32 put testers on the app this week.** **Two questions, both cheap:** (a) **does CIC's environment start from an empty database at cutover?** If yes T-13 holds and nothing is owed; if testers touch older analyses it does not. (b) **Is a marker cheaper than the support call?** — "results retrieved before treaty loss was recorded; re-retrieve to export at TY" is one string. **`31` O31-11 is the same gap for the treaty list itself and is still open; they should be answered together.** **And two fragilities to name to whoever owns deployment:** `analysis_jobs.py:444-446` **fails the whole results retrieval on any single treaty's stats read** (*"no partial write"*), turning a five-call job into a five-plus-N-call job with N+5 ways to fail; and the read needs **`irp-integration 0.10.0rc1`, a TestPyPI pre-release** (`1df4761`), **now on the critical path of every results retrieval in the week of tester onboarding.** **Ben hit a retrieval error live and deferred it** — *"This looks like a nice technical error that we can retry successfully. **I need to check that.**"* **Also still owed: the `tests/irp` sandbox check** (`016` plan §IRP sandbox, T-18, T-16). (§2, §3) *Ben — `016` T-13, `analysis_jobs.py`, `pyproject.toml`, deployment.*
- **O33-3** — **Show pure premium or AAL per treaty in the expanded results view — and keep the figure in the worker read rather than adding a call.** D5. **Cheryl:** *"I think putting the **pure premium or the average annual loss** in here would be helpful. To me, that would be useful information. **It's fast to you look right away down the list and be like, OK, only I only have loss for two.**"* Wendy: *"I agree with that."* Ben: *"Yeah, I agree with that, too"* — having proposed it himself: *"I could make it more clear which of these treaties have losses. **We want them at the individual treaty level in this expanded view.**"* **⚑ The mechanical note: `has_loss` is `bool(rows)` (`analysis_jobs.py:447`), so the figure that produced the flag is thrown away.** Keep `purePremium` (and/or the OEP `aal`) from the same stats rows in `loss_results.treaties[]`, in the same worker call — **a second read would cost N more calls and a web-side read would be the Article 11 problem O33-2 just avoided.** **Build it with `32` O32-3's cap on the same list**, since both change the same three `<ul>`s in `analysis_results_inline.html`. **Context Cheryl gave for why this screen is the right one (D4):** she declined the alternative of listing no-loss treaties as unselectable — *"**No**"* — because *"before I even say export, I can look at that and say, oh, there's seven treaties… I can pick working excess, I can pick quota shares. So if I pick working excess, and it's like, oh, gee, there's no losses there. **Probably should have some losses there.**"* **And Wendy's guard, which is the requirement's real justification:** *"**I personally would not export results without looking to see that results were generated.**"* **Also owed from the same stretch: Ben's output profile produces only gross pre-cat and ground up, so working excess and quota share are untestable on his fixtures** — *"my output profile doesn't produce them."* (§2) *Ben — `analysis_jobs.py`, `analysis_results_inline.html`, `016` FR-002; with `32` O32-3.*
- **O33-4** — **⚑ Drop the treaty-year default entirely and make data vintage required — and fix FR line 49, which was rewritten this morning to describe the behaviour the call removed.** D7, D8. **Treaty year:** built `c94538e` at 23:26 on 9/21 (`017` P-20, FR-006, `submission_form.html:120-122`, hint *"Defaults to the data vintage year"*), **documented into `FUNCTIONAL_REQUIREMENTS.md:49` at 08:25 on 9/22, and removed at ~10:20.** Wendy's reason is a calendar the default cannot model: *"everything we work on from now to the end of the year, 98% of it is treaty year 2027. And then the things you work on in the spring are treaty year 2027… until you get back into the fall."* Cheryl knew a rule exists and could not state it — *"it does usually just default to generally the following calendar year. **There must be a logic. Cheng must have logic built in there**"* — and closed it: *"**Maybe we just leave it blank and force people to fill it in.**"* Ben: *"Yeah, OK, can do that."* **⚑ Remove both halves.** P-20 also fills the year server-side from the earliest contract inception when the field is blank; **that half was never discussed and has the same defect (an inception date is not a renewal season).** **Do not keep it on the grounds that only the vintage-derived half was named.** **And note the round trip:** FR line 49 records that P-20 *"Reverses design note 08 §2.1 D4"* — **D8 reverses the reversal, so note 08's position stands again four days later.** **Either ask Cheng for CIC's actual rule (Cheryl named him; it is one question on an existing tool) or leave the field blank permanently — but write down which.** **Data vintage:** Wendy — *"**I sort of think that should be required.** Do you agree?"*; Cheryl — *"Agreed"*; Ben — *"Okay, that can be required."* **Built `submission.data_vintage DATE NULL` (`0001_initial.py:157`), specified optional in P-19 and FR-022.** **Client stays optional** (Wendy: *"Probably not"*), which matches P-04. **⚑ Check the export path when you make it NOT NULL:** the export's data vintage is required and pre-fills from the submission (`017` FR-011, `014` FR-002), and **`d641bec` pins an empty-vintage case in the Finish stop test** — field, requirement and test move together. (§6) *Ben — `017` P-19, P-20, FR-006, FR-022; FR doc lines 47, 49; `0001_initial.py:157`.*
- **O33-5** — **Rename the "Deal" group to "Program" — and decide whether the noun follows the label.** D11. **Ben raised his own doubt:** *"The submission attributes, which I've added as a header called the deal. I don't really know if that lines up. **We do use deal as a term often, but I'm not necessarily sure if that makes sense there.**"* **Wendy:** *"**We kind of call it program** — probably be more appropriate label there."* **Cheryl:** *"I think that's fine."* **The ask is one string: `submission_head.html:59`, `<span class="deal-group__label">Deal</span>`.** **⚑ But "deal" is the project's noun for the whole entity** — `deal-card`, `deal-groups`, `deal-field`, the `deal_field()` macro, `#deal-head`, `deal-head__created`, *"This deal is … read-only"*, `017` **P-14** (*"one deal card"*), **FR-021**, and the FR doc's *"Directory path = the deal's shared-drive folder"* and *"the deal — top-level entity"* in the migration. **Wendy named the label, not the model.** **Change the label; leave the internal vocabulary alone unless CIC asks, and note that "program" is the client's word for a bundle of contracts, which is what the submission actually is — so if the noun ever does move, it moves for a good reason and should move everywhere at once.** (§4) *Ben — `submission_head.html`, `017` P-14.*
- **O33-6** — **⚑ Tell the client the right rule for the inception column, and test the new default sort with the seeding script.** D17, and `32` O32-6(b). **What Ben said:** *"And then the inception date is **the inception date of the first CRM ID that was created** for the submission."* **What exists:** `submission_service.py:467`, `row.latest_inception_date = max(inceptions)`; `017` **FR-007**, *"the latest inception across the submission's contracts"*; **P-17**, *"**No contract is primary.** The list's default sort is the latest contract inception descending, then name; a submission with no contract sorts by its creation date in the same key."* **This is the second time the client has ratified reasoning built on a first-listed rule the code does not implement** — 9/18 D21 was the first, and §4 records that the export now asks the analyst instead (P-17, `9c31987`), which is better and which **nobody told CIC.** **Say "latest" the next time the column is on screen and confirm it is what they want; P-17's confirmation column is blank.** **⚑ And the sort has a second problem `32` predicted and nobody wrote down:** `ix_submission_list_order` is **deleted**, not re-keyed, so the default order is a correlated aggregate over `contract` with only `ix_contract_submission_id` beneath it. `32` framed the choice — *"Either the list sorts on a correlated contract date (not indexable the same way) or the submission keeps a denormalised copy"* — **and the first was taken silently.** **The SQL Server tier is unrun (T056, rebuild needed), so this has never executed against a server.** **Ben is building a dev script tomorrow to *"populate a ton of submissions and contracts"* for search testing — point it at the sort and the in-force predicate as well as at correctness, because it is the only chance this week to find a plan problem before the testers do.** (§7, §10) *Ben — `017` FR-007, P-17; `submission_service.py`; T056.*
- **O33-7** — **Bank the Power BI deep link; hold the CRM redirect until the rewrite lands.** D19, D20. **Anil, on the URL:** *"it has the resource ID, so submission slash submission ID is what gets us there… if there is ever a desire to build a Power BI dashboard on top of that SQL Server table, you could easily build that out and even create a clickable link… **you can navigate from a Power BI dashboard directly to your workbench.**"* **This needs nothing from the Workbench — it is a statement about a URL that already exists, and CIC can act on it whenever they like.** Wendy: *"that's great because we're in the middle of a CRM rewrite, and so **that's the direction we're going for things is more integration**… That's the ultimate goal, is they all talk to each other."* **The redirect route:** *"an API route that redirects… for the CRM ID. All that does is find the submission for that CRM. **There should be only one.**"* **⚑ Under O33-1 that assumption becomes a database constraint, so the ambiguous case Anil wrote an error path for stops existing — the route is perhaps twenty lines and correct by construction.** **Declined on timing, not merit:** Wendy — *"we don't use Salesforce, we use Dynamics, but I get your point… **It's all being replaced. It's all being rewritten**… we're not investing any time there at all right now in the existing product."* Ben: *"It's a nice idea, though."* **Put both in the PRD's future section with the CRM-rewrite dependency attached; Anil offered to walk the new CRM team through the integration.** (§8) *Anil / Ben — `PRD.md` future.*
- **O33-8** — **⚑⚑ Write up the 2026-09-21 session, or mark the requirements that cite it as unconfirmed.** **`32` O32-4 asked for the decision table to carry *derived from* and *confirmed with the client on* as separate columns. `017` now does, and the first thing it shows is the hole it was built to show:** **P-03, P-06, P-10, P-11, P-16, P-17, P-18 and P-20 have the confirmation column blank** — among them the contract term, the export pre-fill, the extract grain, the create form's required set, the list sort, the filter-evaluation rule and the treaty year, **four of which this session touched and one of which (P-20) it reversed.** **And ten rows cite their derivation as *"user 2026-09-21"*: P-02, P-03, P-05, P-06, P-14, P-15, P-16, P-17, P-18, P-19 — as do `014` P-22 and P-27.** **There is no transcript in `CIC/transcripts/`, no minutes in `CIC/minutes/` and no note in this folder for 2026-09-21.** `32` §12 scheduled the session (*"Ben meets Cheryl in the morning for regular demo work"*), so it happened and was evidently productive — **but twelve approved requirements across two specs now rest on a conversation with no artefact, and this is the second spec in a row in that position.** **Two things, both cheap:** (a) **write the 9/21 session up from whatever exists** — Ben's own recollection, the commit messages, `014`'s P-22/P-27 revision notes — **as a short note `32a` or an appendix here, and say plainly which decisions it carried**; (b) **fill or explicitly blank the confirmation column**, because a blank that means "not yet shown to the client" and a blank that means "shown on 9/21, unrecorded" are different risks and currently look identical. **The rule `31` and `32` both asked for is now implemented and working — this is the first session where it produced a finding, which is the argument for keeping it.** (§1) *Ben — `017` and `014` decision tables, this folder.*

**Advances / closes (from prior sessions):** `32` **O32-2** the zero-loss filter — **CLOSED, and closed the right way**: built worker-side per the note's recommendation, `016` T-18 records the Article 11 reasoning, demoed and verified against Risk Modeler; **its sub-questions (a) and (b) resolved** (treaty-scoped stats via `0.10.0rc1`; no backfill, pre-cutover rule) **and (c) decided against the note's recommendation** → **O33-2**. `32` **O32-5** the contract grain — **CLOSED**: spec, migration, routes, templates, FR doc, `DATA_MODEL.md`, `PRD.md` and constitution v4.2.2, all between 23:00 Monday and 08:47 Tuesday, and accepted on the call without a change. `32` **O32-6** the view, the index and the pre-fill — **CLOSED in all three parts**: `v_contract` replaces `v_submission_crm_id`; `ix_submission_list_order` deleted; T034 built (`9c31987`) — **with the sort's performance trade-off unrecorded** → **O33-6**. `32` **O32-7** required-at-creation — **CLOSED BY DEMONSTRATION, not by consultation**: name and cedant only (`017` P-16, FR doc line 52), shown on the call, not objected to, never asked. `32` **O32-8** the Client ID label — **CLOSED**: *"I renamed it Client ID."* `32` **O32-9** expiration required with the one-year-minus-a-day default — **CLOSED**: `contract.expiration_date NOT NULL`, carry-down built (`448d99a`, `b1259a2`), and the grid has the column headers Ben promised. `32` **O32-4** the decision-date split and the preview waiver — **BOTH DISCHARGED**: the split column exists (and immediately produced **O33-8**), and the preview gate was honoured **and written into `017` FR-021 as a requirement**. `31` **O31-6** Origin and the export grid — **CLOSED for the export table** (`7afc437`, `014` P-22/P-27); **the portfolio/template half (`32` O32-14) not raised.** `32` **O32-3** the capped lists and imported-group members, **O32-10** Risk Modeler search, **O32-11** the library filters and tagging, **O32-12** the industry databases, **O32-13** deployment and testing, **O32-14** the grid columns; `31` **O31-5**, **O31-8**, **O31-9**, **O31-11**; `29` **O29-1**, **O29-6**, **O29-7**, **O29-8**, **O29-9/O29-10**, **O29-14**; `30` **O30-13** — **NOT RAISED.**

**Next session: Wednesday 23 September, afternoon — the bulk status update and search testing on seeded data.** **Two things should be settled before the code for either moves.** **CRM-ID uniqueness (O33-1) is one index, one lookup, one message and three requirement edits, against a table four days old and a migration still edited in place** — it is the cheapest it will ever be, and it is also the precondition that makes 9/18 D25's bulk update well-defined, since **a CRM extract carrying one status per CRM ID has no meaning if a CRM ID can name two submissions.** **Do it before demoing the bulk update, not after.** And **the backfill question (O33-2) needs an answer this week rather than next**, because the testers 9/18 promised are the population T-13's pre-cutover premise excludes. **The session itself breaks the pattern of the last three: nothing was reversed that had been confirmed, and the one reversal — the treaty-year default — was of a behaviour the client had never seen.** **What has not changed is the gap between when a thing is written down and when anyone confirms it: `017`'s FR doc was amended at 08:25 to describe a rule the 10:20 call deleted, and a uniqueness constraint was relaxed at 23:00 on a citation to a decision about industry databases.** **The code is now moving faster than the record of why it moves, and `017`'s own decision table is the instrument that shows it.**
